# 이미지 생성 세션 절차 (ChatGPT 데스크탑 앱)

예약 작업 지시문이 **이 파일을 읽고 그대로 따르게** 되어 있다.
절차를 고칠 일이 생기면 예약 작업을 건드리지 말고 **이 파일만 고친다**
(예약 작업은 PC에 묶여 있어서 지시문을 바꾸려면 Tifania의 승인이 매번 필요하다).

## 0. 전제 — 하나라도 안 맞으면 아무것도 하지 않고 daily에 이유만 적고 끝낸다

1. **지금이 20:00~08:00 KST 사이**인가. 아니면 끝.
2. **PC가 유휴 상태인가** — `GetLastInputInfo`로 **300초 이상**. Tifania가 PC를 쓰고 있으면
   타이핑이 엉뚱한 창(카카오톡 등)으로 새어 들어간다. 실제로 그런 적 있다. 아니면 끝.
3. **ChatGPT 데스크탑 앱이 떠 있는가.** 없으면 `Windows-MCP`의 `App`으로 띄운다.
4. **`docs/design/art-requests.md`의 "대기 중"에 항목이 남아 있는가.** 없으면 끝.

## 1. 화면이 보이는지 먼저 확인한다 — 이게 제일 자주 막히는 지점

`Screenshot`을 한 번 찍어 본다. `screen grab failed` 나 `액세스가 거부되었습니다 (0x80070005)`가
나오면 **거기서 멈춘다.** 보지 못하는 상태로 타이핑하거나 클릭하지 않는다.
daily에 "화면 캡처 불가로 중단"이라고 적고 끝낸다. 되풀이해서 시도하지 않는다.

> 2026-09-15까지 나흘 연속 이것 때문에 0장이었다. 윈도우 설정 > 개인 정보 > **"스크린샷과 앱"**
> 에서 Claude 데스크탑 앱을 켜 주면 풀린다. 이미 켜 뒀지만, 또 막히면 같은 곳을 본다.

## 2. 한 장 뽑는 절차 (항목 하나당 반복, 대략 3분에 한 장)

1. **새 대화를 연다.** 앞 대화가 남아 있으면 앞 그림이 섞여 나온다. 한 대화에 한 장.
2. **프롬프트는 클립보드로 붙여 넣는다.** `Clipboard`로 넣고 `Ctrl+V`.
   키보드로 직접 타이핑하지 않는다 — 창이 바뀌면 그대로 새어 나간다.
3. 엔터. 그림이 나올 때까지 기다린다(보통 30~60초). 중간에 캡처로 확인한다.
4. **저장은 오른쪽 클릭 → "사본 다운로드".**
   - 그림 아래 동그란 버튼은 **공유**(X·링크드인·레딧)다. 그걸 누르면 안 된다.
   - "사본 다운로드"를 누르면 **"다른 이름으로 저장" 대화상자**가 뜬다.
     파일은 다운로드 폴더로 저절로 가지 **않는다.**
   - 주소 칸에 목표 경로를 **클립보드로 붙여 넣는다**:
     `E:\Unity\PlanetRacer\PlanetRacer\Assets\Resources\Art\<하위폴더>\<파일명>.png`
   - 하위 폴더는 `Icons` / `Planets` / `Cutscenes` / `Rigs`.
     **`Assets/Art/`가 아니라 `Assets/Resources/Art/`다** — 아트 확인 화면(`?art=1`)이
     런타임에 긁으려면 `Resources` 아래여야 한다.

## 3. 넣기 전에 반드시 검사한다

    python tools/check_alpha.py <저장한 파일>

네 가지를 본다 — RGBA인가 / 네 모서리가 투명한가 / 투명 영역이 10~95%인가 /
마젠타 잔상이 없는가. **통과(exit 0)해야만 그 파일을 커밋한다.**

실패하면 그 파일은 지우지 말고 그대로 두되 커밋하지 않는다. daily에
"`<파일명>` 투명 검사 실패 — <출력 줄>"이라고 적고 **다음 항목으로 넘어간다.**
같은 항목을 그 자리에서 다시 뽑느라 시간을 쓰지 않는다.

> 배경 제거(`tools/strip_bg.py`)는 더 이상 기본 경로가 아니다. 2026-09-16부터
> 프롬프트에 `real alpha channel` + `no background color`를 넣어 **처음부터 투명 PNG로 받는다.**
> 키잉은 테두리에 분홍 잔상을 남긴다(icon-key.png에서 2813픽셀 확인).
> 배경이 붙어 온 그림이 생기면 그때만 `strip_bg.py`를 톨러런스 60으로 쓴다.

## 4. 장부 정리

통과한 항목은 `docs/design/art-requests.md`에서 "대기 중" → "들어온 것"으로 옮기고
`- [x]`로 바꾸고 `- 들어간 곳: <경로> (커밋 <해시>)`를 붙인다.
**프롬프트는 지우지 말고 그대로 남긴다** — 나중에 변형할 때 그 프롬프트에서 출발한다.

## 5. 커밋

git 작업 전에 `.git/**/*.lock`을 Windows-MCP PowerShell로 지운다(device_bash는 파일을 못 지운다).
한글 커밋 메시지는 `.commitmsg`에 heredoc으로 써서 `git commit -F .commitmsg`.

## 6. daily 한 줄

몇 장 뽑았는지, 실패한 게 있으면 무엇이 왜 실패했는지. 0장이면 0장이라고 적는다.
