# 2026-09-13 (일)

## 한 일

이번 밤 세션은 backlog 위에서부터 세 항목을 끝까지 갔다. `docs/feedback.md`는 비어 있었다.

- **D02-N/D02-M 밸런스 CSV → 코드 파이프라인**
  - `docs/design/balance/{planets,courses,parts}.csv` 초안(값은 지금 `DefaultData.cs`와 동일 — 앞으로 이 CSV가 기준이 된다)
  - `Packages/.../Runtime/BalanceCsv.cs`: CSV를 Planet/Course/Part로 읽는 코어 유틸(서버·에디터·테스트가 전부 이 코드 하나로 읽는다)
  - `Assets/Editor/ImportBalance.cs`(메뉴 `GemRacer/2. 밸런스 CSV 가져오기`) → `Assets/Data/Balance.asset`(`BalanceTable` ScriptableObject)로 저장. 같은 메뉴 다시 눌러도 덮어쓰기만 함(멱등)
  - 곡괭이·화물칸 같은 채굴 장비 비용 공식은 아직 코어에 없어서 이번 CSV에서는 뺐다 — D05-N에서 공식이 생기면 추가
  - `Core.Tests`에 CSV=DefaultData 일치 테스트 3개 추가
- **W-03 UI Toolkit 반응형 골격**
  - `Assets/UI/Root.uxml`+`Root.uss`: 3D 뷰 자리(viewport-area) + HUD 자리(hud-area, 상태바+버튼 3개)
  - `Assets/Scripts/UI/ResponsiveLayout.cs`: 화면 폭<높이면 "portrait", 아니면 "landscape" 클래스를 루트에 붙인다(미디어 쿼리 대신). 가로 960×540과 태블릿 1280×800이 둘 다 "가로"라 이 두 클래스만으로 기준점 셋 다 커버됨
  - `Assets/Editor/BootstrapResponsiveUI.cs`(메뉴 `GemRacer/5. 반응형 UI 테스트 씬 만들기`) → `Assets/Scenes/ResponsiveUITest.unity`, Build Settings에 자동 등록
  - W-04(가로에서 3D 뷰 비율)는 이 골격이 이미 위/아래 ↔ 왼쪽/오른쪽 배치를 갖추고 있어서 절반은 끝났다 — 남은 건 자리 표시자 대신 진짜 게임 카메라를 넣는 것뿐이라 실제 게임 화면(D04 이후)이 생길 때 마저 하기로 하고 이번엔 체크 안 함
- **D03-N/D03-M 세이브 시스템**
  - `Core/SaveData.cs`(순수 클래스: 버전·현재 행성·마지막 저장 시각(UTC epoch초, 서버시각 교체 지점 주석)·채굴차 레벨·원석/정제 광물·보유·장착 부품) + `MiningRigSave`
  - `Assets/Scripts/Save/SaveService.cs`: `Application.persistentDataPath`에 JsonUtility로 저장. 임시 파일에 다 쓰고 이름 바꾸는 방식(원자적 쓰기), 읽기 실패 시 새 세이브로 폴백
  - `Core.Tests`에 라운드트립 테스트 2개 추가
  - P0(D00~D03) 관문 확인 문서 `docs/design/p0-gate.md` 작성
- `Core.Tests`: **통과 14 / 실패 0** (`cd Core.Tests && dotnet run`)
  - 이 클라우드 환경엔 원래 `dotnet`이 없었다 — `apt-get install dotnet-sdk-8.0`으로 설치하고 돌렸다. 다음 세션 환경이 새로 생성되면 또 없을 수 있음(문제는 아니고 기록만)

## 웹에서 확인할 것

1. [ ] GitHub Actions "webgl" 워크플로가 초록불로 끝났는가(Actions 탭)
2. [ ] 기존 배포 주소(https://planetracer-daz.pages.dev)가 이전과 똑같이 뜨는가 — 지금 첫 화면은 여전히 레이스 카메라 실험 씬(빨간 행성)이어야 정상, 이번 커밋들 때문에 화면이 깨지거나 하얗게 나오면 안 됨

## 이번 건 중 웹에 안 보이는 것

- D02(밸런스 CSV), D03(세이브 시스템)은 원래 눈에 보이는 화면이 없는 데이터/저장 작업
- W-03의 `ResponsiveUITest` 씬은 Build Settings에는 넣었지만 **시작 씬이 아니라서** 폰으로 주소만 열어서는 안 보인다. 씬을 골라 들어가는 화면이 아직 없어서다. 궁금하면 Unity를 켰을 때 `GemRacer > 5. 반응형 UI 테스트 씬 만들기` → Play, Game 뷰 크기를 세로 540×960 → 가로 960×540 → 태블릿 1280×800 순으로 바꿔 가며 3D 뷰 자리와 버튼 3개가 위/아래 ↔ 왼쪽/오른쪽으로 바뀌는지 봐 주면 됨(급하진 않음)

## 막힌 것·결정이 필요한 것

- **컴파일 확인 못 한 파일들** — Unity 에디터가 없는 클라우드 세션이라 아래는 실제로 컴파일해 보지 못했다. 문법·API 이름은 표준 사용법대로 썼지만 다음에 Unity 켤 때 콘솔부터 확인해 달라.
  - `Assets/Editor/ImportBalance.cs`, `Assets/Editor/BootstrapResponsiveUI.cs`
  - `Assets/Scripts/Data/BalanceTable.cs`, `Assets/Scripts/UI/ResponsiveLayout.cs`, `Assets/Scripts/Save/SaveService.cs`
  - 반대로 `Packages/.../BalanceCsv.cs`와 `Core/SaveData.cs`는 `Core.Tests`가 그대로 컴파일해서 실행까지 했으니 이 둘은 실제로 검증된 코드다.
- **PanelSettings 런타임 테마** — `BootstrapResponsiveUI.cs`가 프로젝트에서 `ThemeStyleSheet`를 찾아 자동으로 붙이려 하는데, 프로젝트에 하나도 없으면 경고만 남기고 넘어간다. 이 경우 버튼이 스타일 없이(기본 회색 각진 모양으로) 나올 수 있다 — 안 예뻐 보여도 기능엔 문제없다.
- **T-03(수익 모델), T-05(GitHub 시크릿 5개)** — 여전히 Tifania가 할 일로 남아 있다. T-05가 안 되어 있어도 지금까지는 CI가 잘 돌아간 것 같으니(W-02 성공 기록) 이미 등록되어 있을 수도 있다 — 확인만 해 주면 됨.

## 아침 피드백 (Tifania가 적는 곳)
-

---

## (이어서) 9/13 오전 10시 주말 세션

주말 규칙대로 밀린 것부터 봤다. `docs/feedback.md`는 여전히 비어 있었다(`- [ ]`도 `- [?]`도 없음) —
처리할 새 피드백이 없다는 뜻이라 그다음 순서인 "지난 daily 파일의 막힌 것"으로 넘어갔다.

- **가장 먼저 확인한 것 — 지난 밤 10시 세션이 남긴 미확인 빌드**. `docs/daily/2026-09-12.md`
  마지막 항목이 "run #17 webgl 빌드가 세션 종료 시점까지 진행 중이라 결과를 못 봤다, 다음 세션이
  제일 먼저 볼 것"이었다. GitHub Actions로 확인한 결과 **run #17은 취소됐지만(같은 브랜치에 바로
  다음 커밋이 올라가 concurrency 그룹에 밀림) 그 코드를 포함한 run #18(head 40ec7c4)이 대신 돌아
  전 단계 성공**했다 — Unity 빌드(D05-N의 `UpgradePanel.cs` UI Toolkit 바인딩·`MiningController.cs`
  변경분 포함) → 파일 크기 확인 → Cloudflare Pages 배포 → 배포 확인까지 전부 초록불.
  실제 배포 주소는 `https://07c3c49a.planetracer-daz.pages.dev`(그날의 dev 프리뷰, 별칭
  `https://dev.planetracer-daz.pages.dev`)이고 5개 확인 항목(js/wasm/data/loader/html) 전부 통과했다.
  즉 D05-N/D05-M 항목의 "Unity 에디터 없어 컴파일 확인 못 함" 메모는 이 지점에서 해소됐다 —
  다만 Play 모드에서 실제로 버튼이 눌리고 레벨이 오르는지는 여전히 눈으로 봐야 한다(daily
  2026-09-12.md의 "Unity에서 직접 확인해 줄 것" 체크리스트 그대로 유효).
- **판단이 필요한 항목 재점검** — `docs/decisions.md`의 두 미정 항목(T-03 수익 모델,
  claude/dev→main 승격 시점)은 지난 9/12 오전 세션이 이미 A안/B안과 대가로 정리해 뒀고, 그 뒤로
  새로 발견된 판단거리는 없었다. 그대로 Tifania 답 대기.
- **decisions.md 표 정리** — "(미정) UI 세로 단일 레이아웃" 행이 실제 결정(9/11, 반응형 한 벌 —
  "세로 단일"이 아니라 세로 기준+가로/태블릿 재배치)과 어긋나 있던 걸 발견해 바로잡았다. 내용
  자체가 바뀐 건 아니고 기록만 실제와 맞춘 것.

## 웹에서 확인할 것 (9/13 오전 세션분)

- 없음. 이번 세션은 기존 빌드 결과 확인과 문서 정리만 했고 새로 올린 화면이 없다.

## 막힌 것·결정이 필요한 것 (9/13 오전 세션분)

- 여전히 없음. T-03·branch 승격 두 건은 `docs/decisions.md`에서 Tifania 답 대기 중.

## 아침 피드백 (Tifania가 적는 곳, 이어서)
-

---

## (이어서) 9/13 오후 3시 주말 세션

주말 오후 규칙대로 backlog에서 큰 구조 변경/리팩터링에 해당하는 항목을 골랐다. `docs/feedback.md`는
여전히 비어 있었다. `dotnet`은 이 세션도 없어서 `apt-get install dotnet-sdk-8.0`으로 새로 설치했다
(연속 세 번째 — 매번 새 클라우드 환경이라 그런 것 같다, 문제는 아니고 기록만).

- **W-04 게임 화면 통합 (+ 진행 중 발견한 버그 하나)**
  - 코드를 훑어보니 D04(MiningController, 3D 채굴)·W-03(Root.uxml HUD 자리 표시자)·D05(업그레이드
    패널)가 전부 **서로 다른, 게다가 셋 중 둘은 실제로 존재하지도 않는 씬**에 흩어져 있었다 —
    `GemRacer/5`·`GemRacer/6` 메뉴는 Unity 에디터가 있어야 눌리는데, 지금까지 이 프로젝트를 만진
    세션은 전부 클라우드(에디터 없음)였어서 `ResponsiveUITest.unity`/`UpgradeTest.unity`는 한 번도
    생성된 적이 없다(`ProjectSettings/EditorBuildSettings.asset`에 `TestPlanet`·`RaceCameraSpike`
    둘만 등록되어 있는 걸로 확인). 즉 지금 웹에서 볼 수 있는 화면은 여전히 D01.5의 레이스 카메라
    실험 씬 하나뿐이었다는 뜻.
  - `Assets/Editor/BootstrapMainGame.cs`(`GemRacer/7. 메인 게임 씬 만들기`)로 이 셋을 하나의 실제
    플레이 가능한 씬으로 합쳤다 — 3D 채굴(행성+채굴차+카메라) 위에 Root.uxml HUD와 업그레이드
    패널을 얹는다. `Assets/Scripts/UI/MainHud.cs`(신규)가 상태바(행성 이름·원석 수)와 화물칸
    게이지를 실제 값으로 채우고, viewport-area에 `.live` 클래스를 붙여 자리 표시자 배경/문구를
    지운다(Root.uss에 `.viewport-area.live` 추가) — 그러면 뒤에서 돌고 있는 진짜 카메라가 그대로
    보인다. Root.uxml/Root.uss 자체는 안 건드려서 `ResponsiveUITest` 씬은 여전히 원래 자리
    표시자 그대로 쓸 수 있다. HUD의 "채굴" 버튼은 채굴이 이미 자동이라 할 일이 없어서 업그레이드
    패널을 여닫는 용도로 재활용했고, "제작"·"레이스"는 화면 자체가 없어서(D08/D09) 비활성화해 뒀다.
    이 씬을 Build Settings **0번**으로 등록해서, 다음에 Unity에서 이 메뉴를 한 번 눌러 커밋하면
    웹 배포 시작 화면이 실험 씬 대신 이걸로 바뀐다.
  - **버그 발견·수정**: `MiningController`가 `SurfaceMover.speed`를 부트스트랩이 준 고정값(3)에
    묶어 놓고 있어서, 엔진을 업그레이드해도(코어 `RigSpeed` 공식은 실제로 올라간다) 화면상
    채굴차는 그대로 느린 속도로 돌고 있었다 — 업그레이드 패널의 "다음: 속도 X m/s" 문구가
    화면과 안 맞았다는 뜻. `Update()`에서 매 프레임 `SurfaceMover.speed = MiningSimulator.RigSpeed(...)`로
    맞추게 고쳤다.
  - 화물칸 게이지(`MiningController.CargoCapacityMinerals` 신규, Root.uxml에 `cargo-gauge-track`/
    `-fill`)를 붙이는 과정에서 **판단이 필요한 것을 하나 더 발견**했다 — 화물칸 상한이 지금
    오프라인 캐치업에만 적용되고 접속 중(실시간)에는 무제한이라, 게이지가 접속 중엔 사실상
    장식이 된다. `docs/decisions.md`에 T-06으로 A안/B안 정리해 뒀다.
  - Unity 에디터가 없어 이번에도 컴파일 확인은 못 했다. **다음에 Unity 켤 때 최우선으로 볼 것**
    (아래 체크리스트).
- **W-06 WebGL 로딩 시간 예산 확인 — 이제 매 배포마다 자동으로 잰다**
  - `tools/measure_web_load.js`(신규, 외부 패키지 없음): 배포된 Build 파일들의 실제
    Content-Length를 재서 대역폭 구간별(LTE 약함 3Mbps/보통 8Mbps/좋음 25Mbps) 다운로드 시간을
    계산하고 10초 예산(CLAUDE.md/backlog W-06)과 비교한다.
  - `.github/workflows/webgl.yml`의 "배포 확인" 다음 단계로 넣었다(`continue-on-error` — 지금은
    예산을 넘겨도 빌드를 막지 않는다). 다음 push부터 Actions 로그에서 실측치를 볼 수 있다.
  - 이 클라우드 세션 자체는 아웃바운드 네트워크 정책상 `*.pages.dev`로 못 나가서(직접 확인:
    `curl`도 프록시 단계에서 403) 스크립트를 직접 돌려 실측은 못 했다 — 대신 그 실패 케이스를
    이용해 "요청이 실패하면 거짓으로 '예산 안쪽' 통과 판정을 내던" 로직 버그를 먼저 잡았다(전부
    실패하면 합계가 0이 되어 무조건 통과로 보였을 것). 지금은 하나라도 실패하면 종료 코드 2로
    분명히 실패 처리한다.
  - `docs/decisions.md`의 9/11 기록(wasm 8.4MB + data 5.8MB, 압축 후 총 14MB)으로 손계산만
    해 보면: **LTE 약함(3Mbps) ≈ 38초, 보통(8Mbps) ≈ 14초로 이미 10초 예산 초과, 좋음(25Mbps)만
    ≈ 4.5초로 통과**(다운로드 시간만이고 파싱·초기화는 안 뺀 값이라 실제 체감은 더 나쁠 수 있다).
    새 backlog 항목(W-09)으로 남겨 뒀다 — 급한 건 아니지만 화면이 늘어나기 전에 봐 두는 게 낫다.
- `Core.Tests`: 코어 로직은 이번 세션에서 안 건드려서 그대로 **통과 30 / 실패 0** 확인만 함.

## 웹에서 확인할 것 (9/13 오후 세션분)

- **지금은 없음.** `BootstrapMainGame.cs`는 Unity 에디터가 있어야 실행되는 부트스트랩 코드일
  뿐이라, 이번 push만으로는 `MainGame.unity` 파일 자체가 아직 안 생긴다 — Build Settings도
  실제로는 안 바뀌었다. Unity를 켜서 `GemRacer/7. 메인 게임 씬 만들기`를 한 번 눌러 커밋해야
  비로소 웹 시작 화면이 바뀐다(아래 체크리스트).
- W-06 CI 스텝은 예외다 — 이건 push되는 즉시 다음 Actions 실행부터 바로 동작한다. 로그에서
  "로딩 시간 예산 확인" 스텝을 보면 됨.

## Unity에서 직접 확인해 줄 것 (다음에 Unity 켤 때, 이번 건은 좀 급함 — 웹 시작 화면이 바뀌는 작업)

1. `git pull`(claude/dev) → 콘솔에 컴파일 에러 없는지 확인. 특히 `MainHud.cs`의 `UIDocument`/
   `Length.Percent`/`DisplayStyle` 사용과 `MiningController.cs`에 추가한 `CargoCapacityMinerals`.
2. `GemRacer > 7. 메인 게임 씬 만들기` 클릭 → Play.
3. 확인 포인트
   - [ ] 채굴차가 행성을 돌다가 광맥 앞에서 멈추고, 화면 아래 원석 숫자가 올라간다(임시 OnGUI는 꺼져 있다).
   - [ ] 화면 아래(세로)/오른쪽(가로) HUD에 행성 이름·원석 수·화물칸 게이지 바가 보인다.
   - [ ] 3D 뷰 자리(viewport-area)가 자리 표시자 색·문구 없이 실제 카메라 화면 그대로 보인다.
   - [ ] "업그레이드" 버튼(원래 "채굴" 버튼)을 누르면 곡괭이/화물칸/엔진 패널이 열리고, 다시 누르면 닫힌다.
   - [ ] "제작"·"레이스" 버튼은 비활성화(회색)로 보이고 툴팁에 "아직 준비되지 않음"이 뜬다.
   - [ ] 엔진을 업그레이드한 뒤 채굴차가 실제로 더 빨리 도는지 본다(이번에 고친 버그).
   - [ ] 세로(540×960)/가로(960×540)/태블릿(1280×800)에서 HUD가 위·아래 ↔ 왼쪽·오른쪽으로 재배치된다.
   - [ ] 문제없으면 `MainGame.unity`와 새 스크립트들의 `.meta` 파일을 커밋에 포함해 push — 그래야
     다음 웹 배포부터 이 화면이 시작 화면이 된다(지금은 Build Settings가 실제로 안 바뀐 상태).

## 막힌 것·결정이 필요한 것 (9/13 오후 세션분)

- **T-06 (신규, docs/decisions.md)**: 화물칸 상한을 접속 중에도 적용할지. A안/B안 정리해 둠.
- **W-09 (신규, backlog)**: 에셋 크기. 지금 계산상 LTE 약함/보통 구간에서 10초 예산을 넘긴다.
  급하진 않지만 화면이 늘어나기 전에 손볼 것.
- T-03(수익 모델)·claude/dev→main 승격 시점 두 건은 여전히 Tifania 답 대기 중(변동 없음).

## push 직후 확인 (9/13 오후, run #20)

이 세션이 끝나기 전에 Actions 결과가 나와서 바로 확인했다.

- **빌드 성공 → 이번 세션 코드 전부 컴파일 확인됨.** `BootstrapMainGame.cs`/`MainHud.cs`/
  `MiningController.cs` 변경분을 Unity가 실제로 컴파일해서 webgl 빌드가 성공했다. 위
  "Unity에서 직접 확인해 줄 것" 1번(컴파일 에러 확인)은 이걸로 해소 — 남은 건 Play 모드
  동작(버튼·게이지·엔진 속도)뿐이다.
- **배포 확인 5/5 통과**, 그 다음 새로 넣은 "로딩 시간 예산 확인" 스텝도 실제로 돌아서
  **실측치**를 냈다: `https://51b6c2f6.planetracer-daz.pages.dev` 기준 wasm 8.09MB + data
  5.62MB + framework 0.07MB = 합계 13.78MB. LTE 약함(3Mbps) 36.8초·LTE 보통(8Mbps) 13.8초로
  10초 예산 초과, LTE/5G 좋음(25Mbps)만 4.4초로 통과 — 오후 초반에 9/11 기록으로 손계산한
  값(38초/14초/4.5초)과 거의 일치해서, 그 계산 방식 자체도 믿을 만하다는 뜻. backlog
  W-06/W-09에 실측치로 반영해 둠.
- 웹 시작 화면 자체는 아직 안 바뀌었다(이번 push가 `EditorBuildSettings.asset`을 안 건드려서
  — 예상대로다). `GemRacer/7`을 Unity에서 눌러야 실제로 바뀐다.

## 아침 피드백 (Tifania가 적는 곳, 이어서 2)
-

---

## (이어서) 9/13 저녁 6시 주말 세션 (매시간 예약으로 바뀐 뒤 첫 회)

이 세션부터 예약이 하루 네 번(09/14/19/23시)에서 **매시간**으로 바뀌었다. `git log`로 확인해 보니
직전 커밋이 3시간 전이라 겹칠 걱정은 없었다. `docs/feedback.md`는 비어 있었고, 직전 세션(오후 3시)이
남긴 "이어서 할 것"도 따로 없어서 backlog 순서대로 봤다.

- **문서가 실제와 어긋난 것부터 고침(CLAUDE.md 7번 규칙)**: CLAUDE.md와 backlog.md 둘 다 주말이
  "하루 네 번, 한 번에 3~4항목"이라고 적혀 있었는데 이번 예약 프롬프트는 매시간이라고 되어 있었다.
  두 문서 다 "매시간, 한 번에 1~2항목"으로 고쳤다. 안 고치면 다음 세션이 옛 문서를 믿고 한 번에
  너무 많이 벌여서 세션끼리 겹칠 수 있었다.
- **backlog 위에서부터 순서대로 셋을 검토했고, 셋 다 Unity 에디터 없이는 위험 판단이 서서 미뤘다**
  (CLAUDE.md의 "확실하지 않은 API는 쓰지 않는다" 원칙):
  - W-05(스크린샷 자동화) — 지난 세션이 이미 API 불확실 사유로 보류해 둔 상태 그대로.
  - W-09(에셋 크기 줄이기) — Managed Stripping Level을 올리면 SaveService의 JsonUtility 리플렉션
    직렬화가 깨질 위험, 텍스처 포맷은 기기별 지원이 갈려 잘못 고르면 화면이 바로 깨질 위험. 둘 다
    검토 내용을 backlog에 남겨 뒀다(다음 Unity 세션이 Medium부터 시험하도록).
  - D06-N(광맥 비주얼) — 코드를 보니 지금 "광맥"은 실제 좌표가 없는 순전한 시간 추상 개념이라,
    단순 장식이 아니라 채굴차 이동 로직 자체를 구면 각도 계산으로 건드려야 하는 항목이었다. 잘못하면
    채굴차가 표면을 벗어나는 등 폰으로 열자마자 보이는 회귀가 될 수 있어 에디터 없이는 손 안 댐.
    이 발견 내용을 backlog D06-N 항목에 남겨서, 다음에 잡을 사람이 "장식만 얹으면 된다"고 오해하지
    않게 해 뒀다.
- **D07-M (시계 되감기 0 처리 테스트) — 완료**. 확인해 보니 `MiningSimulator.Offline`은 이미
  `Math.Max(0, elapsedSeconds)`로 막고 있어서 구현은 그대로 두고, 그 동작이 `DiscoverOffline`까지
  사슬로 이어지는지 회귀 테스트로 고정했다. 음수 경과·경계값(정확히 0)·아주 큰 경과(300년치, 오프라인
  캐치업 버그로 실제 나올 수 있는 극단값) 4개 테스트 추가. `Core.Tests`: **통과 34 / 실패 0**.
  D07-N(오프라인 보상 화면)이 나중에 생겨도 이 경계는 이미 테스트가 지키고 있다.

## 웹에서 확인할 것 (9/13 저녁 세션분)

- 없음. 이번 건은 문서 정리 + Core.Tests 추가뿐이라 화면에 보이는 변화가 없다.

## 막힌 것·결정이 필요한 것 (9/13 저녁 세션분)

- 새로 생긴 것 없음. W-09/D06-N의 판단 근거만 backlog에 추가해 뒀고, 실제 결정은 Unity 에디터가
  있는 세션 몫으로 남김.

## 아침 피드백 (Tifania가 적는 곳, 이어서 3)
-

---

## (이어서) 9/13 저녁 7시 주말 세션

앞 세션(저녁 6시) 마지막 커밋이 52분 전이라 안 겹쳤다. `docs/feedback.md`는 비어 있었고, 저녁 6시
세션이 남긴 "이어서 할 것"도 없어서 backlog 순서대로 봤다 — 저녁 6시 세션이 W-05/W-09/D06-N을
Unity 에디터 없이는 위험하다고 판단해 미룬 바로 다음 항목인 **D07-N(오프라인 보상 화면)**을 잡았다.

- **D07-N 오프라인 보상 화면 — 완료, 그리고 작업 중 더 근본적인 걸 하나 발견**: 코드를 보니
  `MiningController`가 지금까지 `SaveService`를 아예 호출한 적이 없었다 — `Assets/Scripts/Save/`가
  D03-N에서 만들어진 뒤로 한 번도 실제로 안 쓰이고 있었던 것. 즉 지금까지 웹에서 뭘 올려도 Play할
  때마다 채굴차는 항상 레벨 1, 원석은 항상 0으로 시작했다는 뜻이다(재방문 시 진행 상황이 하나도 안
  남는다는, 방치형 게임 기획 자체가 안 서는 문제). 오프라인 보상 화면은 애초에 "세이브가 있어야"
  의미가 있는 화면이라 이걸 먼저 고쳤다.
  - `MiningController.Awake()`가 이제 `SaveService.Load()`로 행성·채굴차 레벨·원석을 복원한다.
    로드한 `SaveData` 객체를 필드로 들고 있다가 `Save()`가 이 컨트롤러가 다루는 필드(행성 id·
    LastSeenUnixSeconds·Rig·RawMinerals)만 갱신해서 쓴다 — 통째로 새 `SaveData`를 만들어 쓰면
    나중에 D08-N이 보유/장착 부품 필드를 쓰기 시작했을 때 여기서 덮어써 날아갈 뻔했다(미리 막아 둠).
    저장은 30초마다 자동 + `OnApplicationPause`/`OnApplicationQuit` 시.
  - 오프라인 보상 계산: 마지막 저장 시각(UTC epoch초)과 지금 UTC 시각 차를 경과로 보고, 이미 L-04에서
    만들어 둔 `ExplorationSimulator.DiscoverOffline`(광물+보물 한 번에)을 그대로 쓴다 — 새 알고리즘을
    짤 필요가 없었다. 30초 미만 경과(에디터에서 Play 재시작하는 정도)면 화면 자체를 안 띄운다.
  - 코어에 작은 조각 둘 추가: `TreasureDiscovery.MineralValue`(발견 시점에 `TreasureDef.MineralValue`를
    복사해 둬서 화면이 defs 목록을 다시 뒤지지 않아도 됨), `ExplorationSimulator.MineableValue`(발견
    목록 중 지금 도구로 캘 수 있는 것만 골라 정제 광물 환산치를 합산 — "받기"가 실제로 주는 값).
    제련(Refinery) 로직이 아직 없어서 이 값도 `TrySpendRawMinerals`와 같은 이유로 일단 원석에
    합친다(주석에 TODO 남김).
  - `Assets/UI/OfflineReward.uxml`+`.uss`(화면 전체를 덮는 반투명 배경 + 가운데 카드 — 세로/가로
    구분 없이 그냥 가운데 뜨면 되는 화면이라 다른 화면들과 달리 `.landscape` 분기가 없다) +
    `Assets/Scripts/UI/OfflineRewardPanel.cs`(경과·인정·버린 시간, 획득 원석, 발견한 보물 요약,
    받기 버튼 — `MiningController.PendingOfflineReward`가 null이면 스스로 숨는다). `BootstrapMainGame.cs`
    (`GemRacer/7`)에 업그레이드 오버레이(sortingOrder 10)보다 더 위(20)로 얹어서, 돌아왔을 때
    업그레이드 패널보다 먼저 보이게 했다.
  - **알려진 한계(코드 주석에도 남김)**: 안 받은 보상은 세이브 파일이 아니라 메모리에만 있다 —
    화면을 안 보고 앱을 끄면(그 사이 자동 저장이 한 번이라도 있었으면) 다음 실행 때 그 보상은
    사라진다. 첫 구현이라 범위를 좁혔고, 플레이테스트에서 "보상이 자꾸 없어진다"는 피드백이 나오면
    그때 `SaveData`에 받지 않은 보상을 들고 다니는 필드를 추가할 것.
  - `Core.Tests`에 3개 추가(MineralValue가 defs와 일치, MineableValue가 CanMineNow만 합산, 빈
    목록이면 0) — **통과 37 / 실패 0**.
  - Unity 에디터가 없어 컴파일 확인은 다음 세션 몫. 특히 `MiningController.Awake()`가 이제 파일
    I/O(`SaveService.Load`)를 한다 — 세이브 파일이 아예 없는 첫 실행 경로(새 `SaveData()`가
    기본값으로 잘 도는지)를 꼭 먼저 봐 줄 것.

## 웹에서 확인할 것 (9/13 저녁 7시 세션분)

- 지금은 없음. `OfflineRewardPanel.cs`/`MiningController.cs` 변경은 `MainGame.unity`가 실제로
  존재해야(=Unity에서 `GemRacer/7`을 한 번 눌러 커밋해야) 웹에 반영된다 — 아직 그 상태가 아니다
  (저녁 6시 세션분과 같은 이유).

## Unity에서 직접 확인해 줄 것 (다음에 Unity 켤 때 — 위 "Unity에서 직접 확인해 줄 것" 체크리스트에 추가)

1. `git pull`(claude/dev) → 콘솔에 컴파일 에러 없는지. 특히 `MiningController.cs`의
   `SaveService`/`DateTimeOffset` 사용, `OfflineRewardPanel.cs`의 `UIDocument`/`DisplayStyle`.
2. `GemRacer > 7. 메인 게임 씬 만들기` → Play → 원석이 좀 쌓일 때까지 기다림 → Stop.
3. `Application.persistentDataPath`에 `save.json`이 생겼는지 확인(Windows면
   `%userprofile%\AppData\LocalLow\<회사명>\PlanetRacer\save.json` 근처).
4. 30초 넘게 기다렸다가 다시 Play → 오프라인 보상 화면이 뜨는지, 숫자가 말이 되는지(경과 시간이
   대략 기다린 시간과 비슷한지), "받기"를 누르면 화면이 닫히고 원석에 반영되는지.
5. 문제없으면 `MainGame.unity`와 새 스크립트/UXML/USS의 `.meta` 파일을 커밋에 포함해 push.

## 막힌 것·결정이 필요한 것 (9/13 저녁 7시 세션분)

- 새로 생긴 것 없음.

## 아침 피드백 (Tifania가 적는 곳, 이어서 4)
-

---

## (이어서) 9/13 저녁 8시 주말 세션

앞 세션(저녁 7시) 마지막 커밋이 53분 전이라 안 겹쳤다. `docs/feedback.md`는 비어 있었고 "이어서 할 것"도
따로 없어서 backlog 순서대로 봤다 — W-05/W-09/D06-N(Unity 에디터 필요 판단, 6시 세션에서 이미 정리)과
D06-M(D06-N 선행 필요)을 지나 **D08-N(레이싱카 부품 제작 UI)**을 잡았다.

- **D08-N 레이싱카 부품 제작 UI — 완료**: 코어 데이터(Part/PartSlot/DefaultData.QuartzStarterParts/
  PartCostC)는 이미 L-03·D02 세션에서 준비돼 있었다 — 실제로 제작·장착하는 로직만 없었다.
  - `Core/PartCraft.cs`(신규): `PartCraft.Cost(grade)`(C등급만 정의, 나머지는 예외)·`CanCraft`(중복
    제작 방지) + `PartEquip.TryEquip`/`Unequip`. Part.Slot이 제작 시점에 고정이라 엉뚱한 슬롯에
    못 끼운다 — "중복 장착 방지"가 로직이 아니라 구조로 보장된다는 걸 테스트로 확인.
  - `MiningController`에 `OwnedPartIds`·`Car`(RacingCar)·`TryCraftPart`/`TryEquipPart`/`UnequipPart`
    추가. `SaveData.OwnedPartIds`/`EquippedPartIds`는 D03-N 때 이미 필드만 만들어 두고 아무도 안
    쓰고 있었던 것(D07-N 때 RawMinerals/Rig가 그랬던 것과 같은 패턴) — 이번에 `Awake`/`Save`가 실제로
    읽고 쓰게 연결했다. 6칸 순서는 `SlotOrder`(PartSlot enum 선언 순서)로 고정.
  - `Assets/UI/Crafting.uxml`+`.uss`(쿼츠 C등급 5종 한 줄씩, Upgrade.uxml과 같은 반응형 패턴) +
    `Assets/Scripts/UI/CraftingPanel.cs`(버튼 하나가 상태별로 제작→장착→해제를 겸함). `MainHud.cs`의
    "제작" 버튼을 실제로 연결(그동안 비활성화 상태였다). `BootstrapMainGame.cs`(`GemRacer/7`)에 제작
    오버레이(sortingOrder 11) 추가.
  - `Core.Tests`에 6개 추가(제작 성공/중복 방지, 미정의 등급 예외, 미보유 장착 실패, 장착 슬롯
    배타성, 슬롯 교체 시 보유 목록 유지, 해제) — **통과 43 / 실패 0**.
  - Unity 에디터가 없어 컴파일 확인은 다음 세션 몫(아래 체크리스트에 추가).

## 웹에서 확인할 것 (9/13 저녁 8시 세션분)

- 지금은 없음. `MainGame.unity`가 실제로 존재해야(=Unity에서 `GemRacer/7`을 눌러 커밋해야) 웹에
  반영된다 — 저녁 6·7시 세션분과 같은 이유로 아직 그 상태가 아니다.

## Unity에서 직접 확인해 줄 것 (다음에 Unity 켤 때 — 위 체크리스트에 이어서)

6. `GemRacer > 7. 메인 게임 씬 만들기`를 다시 누른 뒤(이번 세션이 제작 오버레이를 추가했다), 원석을
   15개 이상 모으고 "제작" 버튼을 누른다 — 부품 5개 목록이 뜨는지, 하나 제작하면 "보유 중"으로
   바뀌는지, "장착"을 누르면 "장착됨"으로 바뀌는지 확인.
7. 장착한 상태로 Stop → Play를 다시 하면 장착 상태가 그대로 남아 있는지(세이브 라운드트립) 확인.

## 막힌 것·결정이 필요한 것 (9/13 저녁 8시 세션분)

- 새로 생긴 것 없음.

## 아침 피드백 (Tifania가 적는 곳, 이어서 5)
-

---

## (이어서) 9/13 밤 9시 주말 세션

앞 세션(저녁 8시) 마지막 커밋이 53분 전이라 안 겹쳤다. `docs/feedback.md`는 비어 있었고 "이어서 할 것"도
따로 안 남아 있어서 backlog 순서대로 봤다 — W-05/W-09/D06-N/D06-M은 전부 "Unity 에디터로 직접 봐야
안전"이라고 지난 세션들이 이미 판단해 둔 항목들이라 이번에도 그대로 건너뛰고, 다음 항목인
**D08-M(제작 비용 차감·세이브 라운드트립 테스트)**을 잡았다.

- **D08-M 완료**: `Core.Tests`에 2개 추가.
  - "제작: 비용만큼 정확히 차감되고, 부족하면 아무 것도 안 바뀐다" — `PartCraft.Cost`만큼 정확히
    빠지는지, 모자랄 때 부분 차감 없이 그대로인지 확인.
  - "통합: 제작→장착→세이브 직렬화→역직렬화→복원까지 한 바퀴" — `MiningController.TryCraftPart`/
    `Save`/`LoadParts`/`EquippedIdsInSlotOrder`가 실제로 하는 일(제작→장착→SlotOrder 순서로
    `SaveData.EquippedPartIds` 6칸 채우기→JSON 왕복→id로 `AvailableParts`에서 찾아 슬롯 복원)을
    코어 조각만으로 그대로 재현했다. `Assets/Scripts`는 UnityEngine을 참조해서 Core.Tests가 직접
    못 부르니(CLAUDE.md 1번), 테스트 안에 MiningController.cs의 `SlotOrder`와 같은 순서의 배열을
    복제해 두고 "어긋나면 MiningController.cs를 고칠 때 여기도 같이 봐야 한다"는 주석을 남겼다.
    장착 안 하고 보유만 한 부품(서스펜션)이 복원 후에도 계속 빈 슬롯으로 남는지까지 확인 —
    D08-N 완료 노트가 "LoadParts를 봐 달라"고 남겨 둔 위험 지점이었다.
  - 이 클라우드 환경도 `dotnet`이 없어서 `apt-get install dotnet-sdk-8.0`으로 새로 설치했다(9/12
    밤·9/14 새벽 세션과 같은 패턴 — 환경이 매번 새로 뜨는 듯). `dotnet run` **통과 45 / 실패 0**.
  - **문서 오류 발견**: `docs/daily/2026-09-14.md`(내일 날짜)에 9/12 새벽 03:10 세션(L-01~L-03) 내용이
    잘못 들어가 있다 — 커밋 시각(2026-09-11 18:10 UTC = 9/12 03:10 KST)으로 보아 그 세션이 날짜를
    이틀 밀려 계산한 듯하다. 지금 세션 범위 밖이라(과거 로그 재배치는 git blame을 흔들 뿐 실익이
    적음) 고치진 않고 여기 기록만 남긴다 — 다음에 그 근처를 만지는 세션이 참고할 것.

## 웹에서 확인할 것 (9/13 밤 9시 세션분)

- 없음. 이번 건은 `Core.Tests` 테스트 추가뿐이라 화면이 없다.

## 막힌 것·결정이 필요한 것 (9/13 밤 9시 세션분)

- 새로 생긴 것 없음.

## 이어서 할 것

backlog 다음 순서는 D09-N(레이스 출전 화면)이다. Unity 에디터 없이도 UXML 부트스트랩 코드로 만들
수 있는 화면이라 다음 세션이 바로 집어도 된다 — 단 코스 구성 비율 표시에 필요한 밸런스 데이터가
`docs/design/balance/courses.csv`/`DefaultData`에 이미 있는지 먼저 확인할 것.
