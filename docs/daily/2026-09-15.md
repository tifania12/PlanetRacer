# 2026-09-15 (화)

## 오늘 웹에서 확인할 것

1. **`RangeError` 없이 첫 화면(튜토리얼 말풍선·HUD)이 뜨는지** — 스택 크기 수정이 9/14에 처음
   production(`main`)에 나갔다. 여전히 죽으면 예외 메시지의 관리 코드 스택으로 지점을 잡는다.
2. 행성이 흰색 + 파란 하늘로 보이는지(어두운 빨강이면 URP HDR Output 수정이 안 먹은 것) — 되면
   HUD(하단 버튼 다섯 개)도 같이 보이는지, 화물칸 다 찼을 때 "정제로 돌리시겠어요?" 화면(M-04)도.
3. "Made with Unity" 스플래시는 여전히 나올 것(알려진 문제, 판단 대기) — 새로 적을 필요 없음.

## 02:05 세션

- 시작 시 앞 세션(01:05, `4f16978`) 커밋이 55분 전이라 안 겹쳐서 진행.
- `docs/feedback.md` 세 줄은 여전히 "배포 주소를 눈으로 열어서 확인"만 남은 것들이라 이 클라우드
  세션이 처리할 수 없다(CLAUDE.md에 이미 적힌 구조적 한계) — 새로 시도하지 않고 그대로 뒀다.
  `GemRacer/7`(M-04 씬 반영)도 에디터가 있어야 해서 건너뛰고, 앞 세션이 남긴 대로 backlog의
  다음 코드 항목인 **M-06(Entitlements)**으로 갔다 — 순수 core라 에디터 없이도 안전하게 끝낼
  수 있는 항목이었다.
- 한 일: **M-06 코어 `Entitlements.cs`** 신규. `PurchaseState`(영구 구매 레벨/bool + 기간제
  만료 시각 `long?`)를 `Entitlements.Effective(state, nowUnixSeconds)` 하나로 계산해서 "구매·구독
  상태를 한 곳에서 계산, UI는 결과만 읽는다"(monetization.md 6장)를 코드로 옮겼다. 핵심은
  화물칸 확장(영구, 0~3단계 ×1~×3)과 구독(×1.5)이 겹칠 때 **곱하지 않고 `Math.Max`** —
  monetization.md 2-5 "더 큰 값 적용, 중복 차감 없음"을 그대로 옮긴 것(3단계 구매자가 구독까지
  하면 ×4.5가 아니라 ×3이어야 한다). Steam 서포터 팩은 만료 없는 구독과 동일 취급, 광고 제거는
  개별 구매·구독 OR, 오프라인 상한 연장(4h→12h)은 구독 혜택 목록에 없어서 그 구매 하나로만
  결정(구독과 무관), 채굴 가속 패스(×2 산출)는 화물칸·구독과 완전히 독립적으로 계산했다. "매일
  정제 광물 지급"은 하루 한 번이라는 청구 타이밍이 상태값(마지막 지급 날짜)이 필요해 순수 계산
  함수엔 안 맞아서 지금은 구독 여부만 알려 주는 플래그로 남기고 실제 청구 로직은 안 건드림(코드에
  TODO). `OfflineCapHours`/`BonusFuelCapacity`도 계산만 있고 `MiningController`/`RaceFuel`에
  실제로 연결하진 않았다(TODO) — 상점 화면(M-07)이 아직 없어서 지금 구매를 만들어 낼 곳이 없다,
  그래서 이번엔 계산 로직까지만. `Core.Tests`에 9개 추가(기본값 전부 꺼짐, 화물칸 단계별 배율과
  범위 밖 클램프, 구독 켜짐/만료 경계값, 화물칸 중복 방지를 양쪽 방향 다, Steam 영구 취급, 광고
  제거 OR, 오프라인 연장이 구독과 무관함, 가속 패스 독립) — `dotnet run` **통과 103 / 실패 0**.
- 확인 필요: Unity 에디터가 없어 컴파일 확인은 다음 세션 몫. `UnityEngine`을 참조하지 않는 순수
  C#(Packages/Runtime)이라 위험은 낮다고 보지만, `Entitlements.cs`에 아직 `.meta` 파일이 없다 —
  에디터가 여는 다음 세션에서 생기는 대로 커밋해 줄 것(이전에도 OfflineRewardPanel.cs 등에서
  같은 패턴).
- main 승격: 이번 커밋(`c05b73d`)의 Actions 빌드(run #80)가 이 세션이 끝나는 시점에 아직
  `queued` 상태라 승격 조건 2번(직전 빌드 성공)을 못 채운다 — 다음 세션이 그 결과부터 보고
  성공했으면 승격할 것(급하지 않음, `Core.Tests`/core/문서만 바꿔서 화면 영향은 없다). 참고로
  바로 앞 커밋(`4f16978`, M-05)의 run #79는 성공이었다.
- 이어서 할 것: feedback.md 세 줄은 여전히 에디터·pages.dev 접근 가능한 세션 몫. 코드 쪽은
  `GemRacer/7`(에디터 세션이 M-04를 씬에 반영) + M-05 나머지 절반(Mobile Notifications 패키지)이
  여전히 에디터 있는 세션 몫으로 남아 있다. 에디터가 없는 세션이 다음이면 backlog 순서상 M-07
  (상점 화면)로 넘어가되, M-07은 UI Toolkit 화면이라 `Assets/Editor`의 부트스트랩 코드부터 짜야
  하고 씬 반영 자체는 에디터 세션 몫이라는 걸 감안할 것 — 코드(UXML/USS/Panel.cs)까지는 이 구조로도
  가능했던 D08~D14 패턴을 그대로 따르면 된다.

### 03:12 세션

- 시작 시 앞 세션(02:05, `43b8157`) 커밋이 55분 전이라 안 겹쳐서 진행. `docs/feedback.md` 세 줄은
  여전히 배포 주소를 눈으로 열어야 하는 것들뿐이라 처리 못함(구조적 한계, CLAUDE.md에 이미 적힘).
- **main 승격**: `dotnet run` 통과 103/실패 0 확인, `claude/dev` 최신 커밋(`43b8157`)의 Actions
  run #81이 **성공**으로 끝난 것 확인(바로 앞 커밋 run #80은 곧이어 푸시된 daily 커밋에 밀려
  `cancelled`로 끝났지만, HEAD의 빌드가 성공했으니 문제 없음), fast-forward 가능 — 세 조건 다
  만족해 `main`을 `76eaa7e..43b8157`로 승격.
- 한 일: backlog 다음 순서 **M-07(상점 화면)**을 집었다. UI Toolkit 화면 자체는 에디터가 있어야
  씬에 반영되니, 이번 세션은 core 뼈대만 끝냈다(다음에 에디터 세션이 화면만 그리면 되게).
  `ShopSkuId`/`ShopItem`(core, 신규) — 스킨(2-7)·시즌 패스(2-6)는 종류/트랙이 안 정해져서 뺀
  아홉 개 SKU. `BalanceCsv.ParseShopItems` + `docs/design/balance/shop.csv`(가격표, 다른 밸런스
  표와 같이 `DefaultData.ShopItems()`와 값이 같은지 테스트로 고정). `ShopPurchase.Apply(state,
  skuId, now)` 신규 — 영구 항목(화물칸 확장 등)은 낮은 단계를 다시 사도 안 내려가게 Math.Max,
  기간제(가속 패스·구독)는 활성 중 재구매 시 지금이 아니라 만료 시각부터 기간이 이어 붙게(선결제
  손해 안 보게). `SaveData`에 `PurchaseState` 저장 필드 6개 + `ToPurchaseState()`/
  `ApplyPurchaseState()`(M-06 `Entitlements.PurchaseState`가 `long?`을 쓰는데 JsonUtility가
  nullable을 못 다뤄서 0↔null 변환 — 다른 필드들과 같은 이유로 0을 "산 적/구독한 적 없음"
  센티널로 썼다). `Core.Tests`에 6개 추가(CSV=DefaultData 일치, 화물칸 단계 안 내려감, 스타터
  팩=1단계, 기간제 이어 붙임 2케이스, 영구 bool 세 개, SaveData 왕복) — **통과 109 / 실패 0**.
  이 클라우드 환경도 `dotnet`이 없어서 apt 미러가 한 번 404 났다가 `apt-get update` 후 설치됨
  (매 세션 반복되는 패턴).
- 확인 필요: Unity 에디터가 없어 컴파일 확인은 다음 세션 몫. 셋 다 `UnityEngine`을 참조하지 않는
  순수 C#(Packages/Runtime)이라 위험은 낮다. 새 파일 3개(`ShopCatalog.cs`, `ShopPurchase.cs`)에
  아직 `.meta`가 없다 — 에디터가 여는 다음 세션에서 생기는 대로 커밋할 것(이전에도 같은 패턴).
- 웹에서 확인할 것: 없음. 이번 건은 전부 core 데이터·계산 코드라 화면이 없다.
- 이어서 할 것: M-07 나머지(`Assets/UI/Shop.uxml`/`.uss` + `ShopPanel.cs`, `MiningController`
  진입점 배선, M-04 `CargoFullPanel`에 상점 버튼 추가) — 전부 backlog M-07 항목에 상세히 남겨
  뒀다. 실제 결제 SDK는 P3라 지금은 "누르면 바로 `ShopPurchase.Apply`" 식 디버그 구매로 시작해도
  됨. 같이 처리하면 좋은 것: M-06이 남긴 TODO(`Entitlements.OfflineCapHours`/`BonusFuelCapacity`를
  `MiningController`/`RaceFuel`에 실제로 연결) — 상점이 실제 구매를 만들어 내야 의미가 있다.
  `GemRacer/7`(M-04 씬 반영)·M-05 나머지 절반(Mobile Notifications 패키지)도 여전히 에디터
  세션 몫으로 남아 있음.
- main 승격: 이번 M-07 커밋(`6881b88`)은 세션이 끝나는 시점에 Actions 빌드가 아직 안 끝나서
  승격 조건 2번을 못 채운다 — 다음 세션이 그 결과부터 확인하고 성공하면 승격할 것(화면 변화
  없는 core 전용 커밋이라 급하지 않음).

### 04:17 세션

- 시작 시 앞 세션(03:12, `e4b55b4`) 커밋이 55분 전이라 안 겹쳐서 진행.
- main 승격 먼저 처리: `dotnet`이 이 컨테이너에도 없어서 `apt-get install -y dotnet-sdk-8.0`으로
  설치(매 세션 반복되는 패턴, CLAUDE.md에 이미 적힘) 후 `dotnet run` **통과 109 / 실패 0** 확인.
  `claude/dev` HEAD(`e4b55b4`)의 Actions run #84가 **성공**으로 끝난 것도 확인(바로 앞 run #83은
  이어진 daily 커밋 푸시에 밀려 `cancelled`였지만 HEAD의 빌드가 성공했으니 문제 없음).
  fast-forward 가능 — 세 조건 다 만족해 `main`을 `43b8157..e4b55b4`로 승격.
- `docs/feedback.md` 두 줄(RangeError 스택 크기 수정/URP HDR 셰이더 수정)은 여전히 배포 주소를
  눈으로 열어야 하는 것들이라 처리 못함(CLAUDE.md에 이미 적힌 구조적 한계). 세 번째 줄(Unity
  스플래시)은 이미 판단 대기(`- [?]`)로 넘어가 있어 손 안 댐.
- 한 일: **M-07 상점 화면 + 배선(2세션째)**. 1세션째(`6881b88`)가 끝낸 core 뼈대(ShopSkuId·
  ShopPurchase·SaveData) 위에, 화면(`Shop.uxml`/`.uss`, `ShopPanel.cs` — LootBoxPanel과 같은
  고정 아홉 줄 구조, DefaultData.ShopItems() 순서로 이름·가격을 채움)과 `MiningController`
  배선을 끝냈다. `DebugPurchase(skuId)` 신규(실제 결제 SDK 전이라 누르면 바로 ShopPurchase.Apply
  적용 — daily 03:12 세션이 남긴 대로), `_purchases`(PurchaseState) 세이브 왕복, `Entitlements`/
  `Purchases` 프로퍼티. `CargoCapacityMinerals`에 `Entitlements.CargoMultiplier`를 곱해서 화물칸
  확장·구독이 HUD 게이지·온라인 클램프·오프라인 보상 세 곳 전부에 실제로 먹게 했다 — 코어
  `ClampToCargoCapacity`는 이 배율을 몰라서 세 곳 다 `Mathf.Min(.., CargoCapacityMinerals)`로
  직접 자르는 방식으로 바꿨다(core 파일 자체는 하나도 안 건드림 — `Core.Tests` 그대로 109/0).
  `MiningYieldMultiplier`(가속 패스)는 접속 중 원석 산출에만 곱함(오프라인은 core 시그니처 변경이
  필요해 다음으로 미룸). HUD에 여섯 번째 "상점" 버튼(`Root.uxml`/`Root.uss`/`MainHud.cs`, 다른
  다섯 버튼과 같은 패턴), `CargoFullPanel`에 "상점 보기" 보조 버튼(레이스 나가기가 여전히 1순위),
  `BootstrapMainGame.cs`에 상점 UIDocument 생성 코드(소트 오더 21, 항상 맨 위).
  판단이 필요한 것 하나 발견 — `Entitlements.OfflineCapHours`(오프라인 상한 연장 상품)가 M-01로
  화물칸 상한이 원석 기준으로 바뀐 뒤 정확히 뭘 해야 하는지 애매해져서 `docs/decisions.md`에
  A/B/C안으로 정리하고 지금은 C안(노출은 하되 효과 배선은 보류)으로 둠. `BonusFuelCapacity`
  (RaceFuel 시그니처 변경 필요)·`AutoRefineryAlwaysOn`·`DailyRefinedMineralsGrant`도 아직 미배선 —
  전부 decisions.md에 남은 목록으로 정리.
- 확인 필요: Unity 에디터가 없어 컴파일 확인은 다음 세션 몫. 새 파일 셋(`ShopPanel.cs`, `Shop.uxml`,
  `Shop.uss`)에 `.meta`가 아직 없다 — 에디터가 여는 다음 세션에서 생기는 대로 커밋할 것(이전과
  같은 패턴). **에디터가 있는 세션은 `GemRacer > 7. 메인 게임 씬 만들기`를 다시 눌러 씬에
  반영해야 웹에서 보인다** — 그전까지는 이번 커밋도 스크립트만 추가될 뿐 화면엔 변화가 없다.
- 웹에서 확인할 것: 없음(위와 같은 이유 — 씬 반영 전까지는 화면 변화 없음). "오늘 웹에서 확인할
  것" 블록은 그대로 둠(RangeError/URP HDR 확인이 여전히 최우선).
- 이어서 할 것: `GemRacer/7` 씬 반영(에디터 세션 몫, M-04·M-07 둘 다 아직 안 반영됨) 다음으로,
  에디터가 없는 세션이 이어받으면 backlog 순서상 M-08(스타터 팩 노출 로직 — 이 상점 화면을
  전제로 하니 자연스러운 다음 항목)로 넘어갈 것. `docs/decisions.md`의 오프라인 상한 연장 판단은
  Tifania 몫으로 남겨 둠.
- main 승격: 이번 M-07 화면 커밋은 세션이 끝나는 시점에 Actions 빌드가 아직 안 끝나서 승격
  조건 2번을 못 채운다 — 다음 세션이 결과 확인 후 승격할 것.

### 05:05 세션

- 시작 시 앞 세션(04:17, `e4b55b4`) 커밋이 47분 전이라 안 겹쳐서 진행.
- **main 승격 먼저 처리**: `dotnet`이 이 컨테이너에도 없어서 `apt-get install -y dotnet-sdk-8.0`로
  설치(매 세션 반복 패턴) 후 `dotnet run` **통과 109 / 실패 0** 확인. `claude/dev` HEAD(`0715c3b`,
  M-07 화면 커밋)의 Actions run #86이 **성공**으로 끝난 것 확인, fast-forward 가능 — 세 조건 다
  만족해 `main`을 `e4b55b4..0715c3b`로 승격.
- `docs/feedback.md` 두 줄(RangeError 스택 크기/URP HDR 셰이더)은 여전히 배포 주소를 눈으로
  열어야 하는 것들이라 처리 못함(CLAUDE.md에 이미 적힌 구조적 한계). 스플래시 줄은 판단 대기로
  이미 넘어가 있어 손 안 댐.
- 한 일: **M-08 스타터 팩 노출 로직**. core `StarterPackOffer.ShouldShow(hasReachedCargoCapBefore,
  declined, cargoExpansionLevel)` 신규 — monetization.md 2-1 "노출은 첫 상한 도달 직후 한 번"을
  판정 함수 하나로: 상한에 한 번이라도 닿았고, 거절한 적 없고, 화물칸 확장을 아직 아무 경로로도
  안 가진 경우에만 true. `SaveData`에 `HasReachedCargoCapBefore`(M-04 `CargoJustFilled`는 엣지
  트리거라 원석이 상한 아래로 내려가면 꺼지지만, 이건 세이브에 영구 보존돼야 "첫 상한 도달"을
  나중에도 알 수 있다)·`StarterPackOfferDeclined` 두 필드 추가(구매한 경우는 CargoExpansionLevel로
  이미 판별되니 따로 안 둠). `MiningController`에 `ShouldShowStarterPackOffer` 프로퍼티(Entitlements
  프로퍼티와 같은 결 — 화면은 이것만 읽는다) + `DeclineStarterPackOffer()`(TutorialStep 등과 같은
  "_save 직접 갱신 + Save()" 패턴), `Update()`의 상한 엣지 트리거 자리에서 `HasReachedCargoCapBefore`도
  같이 켬. 화면은 새 패널을 안 만들고 기존 `CargoFullPanel`(M-04, "정제로 돌리시겠어요?" 화면) 안에
  강조 칸을 추가하는 쪽을 택했다 — 어차피 상한에 닿는 순간 같이 뜨는 화면이라 자연스럽고, 새
  UIDocument를 또 만들면 컴파일 확인 없이 배선할 지점이 늘어난다. `CargoFull.uxml`에
  `starter-pack-callout`(메시지 + "스타터 팩 보기"/"괜찮아요" 버튼), `CargoFull.uss`에 테두리로
  구분되는 스타일(상점 버튼보다 강조, 레이스 나가기보다는 아래), `CargoFullPanel.cs`가
  `ShouldShowStarterPackOffer`를 보고 이 칸만 접었다 폈다 한다(전체 화면은 M-04 로직 그대로) —
  이름·가격은 `DefaultData.ShopItems()[0]`(ShopSkuId.StarterPack, 0번)에서 읽어 하드코딩 안 함.
  "괜찮아요"는 화면 전체를 안 닫는다(레이스 나가기·상점 보기는 여전히 유효). `Core.Tests`에 5개
  추가(안 닿음/셋 다 맞음/거절함/화물칸 확장 1·3단계 보유/경계값 음수) — **통과 114 / 실패 0**.
  `docs/backlog.md`의 M-07도 실제로는 지난 두 세션에서 끝났는데 체크가 안 돼 있던 걸 발견해서
  같이 `[x]`로 고침(CLAUDE.md 7번 — 문서가 실제와 어긋나면 발견한 세션이 고친다).
- 확인 필요: Unity 에디터가 없어 컴파일 확인은 다음 세션 몫. `CargoFullPanel.cs`에
  `GemRacer.Core`(DefaultData 쓰려고) using을 새로 추가했다 — 다른 곳과 이름이 안 겹치는지 특히
  확인. `.meta`는 새 파일이 core 스크립트 하나(`StarterPackOffer.cs`)뿐이라 에디터가 여는 다음
  세션에서 생기는 대로 커밋할 것.
- 웹에서 확인할 것: 없음. `GemRacer/7`(메인 게임 씬 반영)이 아직 에디터 세션 몫으로 안 돌아서
  이번 UI 변경도 스크립트만 추가될 뿐 화면엔 안 보인다. "오늘 웹에서 확인할 것" 블록은 그대로 둠.
- 이어서 할 것: `GemRacer/7` 씬 반영(M-04·M-07·M-08 전부 아직 안 반영, 에디터 세션 몫) 다음으로,
  에디터 없는 세션이 이어받으면 backlog 순서상 **M-09(보상형 광고 자리 4곳 — SDK 연동 없이 자리와
  하루 한도 카운터만, core로 시작 가능)**로 넘어갈 것.
- main 승격: 이번 M-08 커밋은 세션이 끝나는 시점에 Actions 빌드가 아직 안 끝나서 승격 조건 2번을
  못 채운다 — 다음 세션이 결과 확인 후 승격할 것.

### 06:10 세션

- 시작 시 앞 세션(05:05, `4cb742d`) 커밋이 54분 전이라 안 겹쳐서 진행.
- **main 승격 먼저 처리**: `dotnet`이 이 컨테이너에도 없어서 `apt-get install -y dotnet-sdk-8.0`로
  설치(매 세션 반복 패턴) 후 `dotnet run` **통과 114 / 실패 0** 확인. `claude/dev` HEAD(`4cb742d`,
  M-08 커밋)의 Actions run #88이 **성공**으로 끝난 것 확인, fast-forward 가능 — 세 조건 다 만족해
  `main`을 `0715c3b..4cb742d`로 승격.
- `docs/feedback.md` 두 줄(RangeError 스택 크기/URP HDR 셰이더)은 여전히 배포 주소를 눈으로 열어야
  하는 것들이라 처리 못함(CLAUDE.md에 이미 적힌 구조적 한계). 스플래시 줄은 판단 대기로 이미
  넘어가 있어 손 안 댐.
- 한 일: **M-09 보상형 광고 하루 한도 카운터**. core `RewardAd.cs` 신규 — `RewardAdSlot` 4종
  (오프라인 2배/상자 1개 더/상한 2배 1시간/연료 +3) + `RewardAdState`(자리별 오늘 시청 횟수) +
  `RewardAdTracker`(`CanWatch`/`RemainingToday`/`RecordWatch`, "하루" 경계는 UTC 자정이 아니라
  넘겨받은 시간대 오프셋만큼 민 자정 — 시간을 인자로만 받는 원칙을 시간대까지 지켰다). `SaveData`
  필드 5개 + 왕복 함수. `MiningController`에 `_rewardAds` 세이브 왕복 + `CanWatchRewardAd`/
  `RemainingRewardAdsToday`/`RecordRewardAdWatched`(카운터만 올림) — 시간대는 `KstOffsetSeconds`
  (9시간) 상수 고정(TimeZoneInfo는 WebGL에서 플랫폼별로 갈려 위험해서 피함). `Core.Tests`에 8개
  추가(한도 초과 방어, 날짜 리셋, KST 자정 vs UTC 자정 구분, 음수 로컬 시각 floor 나눗셈 경계) —
  **통과 121 / 실패 0**. 자세한 내용은 backlog.md M-09에 남김.
- 확인 필요: Unity 에디터가 없어 `MiningController.cs` 컴파일 확인은 다음 세션 몫. 새 core 파일
  (`RewardAd.cs`)에 아직 `.meta`가 없다 — 에디터가 여는 다음 세션에서 생기는 대로 커밋할 것.
- 웹에서 확인할 것: 없음. 이번 건은 카운터만 잇는 core+MiningController 변경이라 화면이 없다
  (실제 광고 버튼 자리는 아직 안 만듦 — backlog M-09 "남은 것" 참고). "오늘 웹에서 확인할 것"
  블록은 그대로 둠.
- 이어서 할 것: M-09 나머지 — 네 화면(오프라인 보상/레이스 결과/화물칸 가득 참/연료 부족)에
  실제 광고 버튼 UI 추가 + 보상 적용 로직("상한 2배 1시간"은 만료 시각을 들고 있어야 해서 설계가
  더 필요, 연료·상자는 단순 가산이라 쉬움). 그 외엔 여전히 `GemRacer/7` 씬 반영(M-04·M-07·M-08
  전부 아직, 에디터 세션 몫)이 최우선으로 밀려 있음.
- main 승격: 이번 M-09 커밋은 세션이 끝나는 시점에 Actions 빌드가 아직 안 끝나서 승격 조건 2번을
  못 채운다 — 다음 세션이 결과 확인 후 승격할 것.

### 06:30 세션 (아침 정리, 작게 도는 세션)

- 시작 시 앞 세션(06:10, `c4450cf`) 커밋이 27분 전이라 안 겹쳐서 진행. 새 기능은 안 건드림 —
  검증·정리만.
- `dotnet`이 이 컨테이너에도 없어서 `apt-get install -y dotnet-sdk-8.0`로 설치(매 세션 반복
  패턴) 후 `dotnet run` **통과 121 / 실패 0** 확인.
- `claude/dev` HEAD(`c4450cf`, M-09 커밋)의 Actions run #90이 **성공**으로 끝난 것 확인.
  fast-forward 가능 — 세 조건 다 만족해 **main 승격**: `4cb742d..c4450cf`.
- `docs/feedback.md` 세 줄 확인: 위 두 줄(RangeError 스택 크기/URP HDR 셰이더)은 여전히 배포
  주소를 눈으로 열어야 확인되는 것들이라 이 클라우드 세션이 처리 못함(CLAUDE.md에 이미 적힌
  구조적 한계, egress 정책으로 `*.pages.dev` 403) — **오늘 아침 Tifania가 직접 확인해야 함**.
  스플래시 줄은 이미 판단 대기(`- [?]`)로 넘어가 있어 손 안 댐.
- "오늘 웹에서 확인할 것" 블록 대조: 세 항목 다 지금 feedback.md·backlog 상태와 맞고, 밤 세션이
  메뉴·파일 이름을 바꾼 곳도 없어서 그대로 둠. M-07/M-08/M-09는 core+스크립트만 추가된 상태라
  `GemRacer/7` 씬 반영(에디터 세션 몫) 전까지 웹 화면에 안 보이는 것도 각 세션 기록과 일치.

### 07:05 세션

- 시작 시 앞 세션(06:30 정리, `45aef77`) 커밋이 26~27분 전이라 안 겹쳐서 진행.
- **main 승격 먼저 처리**: `dotnet`이 이 컨테이너에도 없어서 `apt-get install -y dotnet-sdk-8.0`로
  설치(매 세션 반복 패턴) 후 `dotnet run` **통과 121 / 실패 0** 확인. `claude/dev` HEAD(`45aef77`,
  06:30 정리 세션이 남긴 daily 커밋)의 Actions run #92가 **성공**으로 끝난 것 확인, fast-forward
  가능 — 세 조건 다 만족해 `main`을 `c4450cf..45aef77`로 승격(06:30 세션 시점엔 이 커밋의 빌드가
  아직 안 끝나 있었다).
- `docs/feedback.md` 두 줄(RangeError 스택 크기/URP HDR 셰이더)은 여전히 배포 주소를 눈으로
  열어야 하는 것들이라 처리 못함(CLAUDE.md에 이미 적힌 구조적 한계). 스플래시 줄은 판단 대기로
  이미 넘어가 있어 손 안 댐.
- 한 일: **M-09 나머지 — 보상형 광고 네 자리 실제 지급 로직 + 화면 배선**(지난 세션이 카운터만
  잇고 남긴 절반). core `RewardAd.cs`에 `RewardAdBoost` 신규 — 네 자리 중 "화물칸 가득 참"(1시간
  동안 상한 2배)만 지속 시간이 있어서 만료 시각 하나로 배율을 계산(`CargoCapMultiplier`)하고,
  다시 볼 때는 지금부터가 아니라 원래 만료 시각부터 이어 붙인다(`ExtendCargoCapDoubleHour` —
  ShopPurchase.Apply의 기간제 이어 붙임과 같은 정책, 선결제 손해 안 봄). 나머지 세 자리(오프라인
  2배/상자 1개 더/연료 +3)는 누르는 순간 끝나는 보상이라 상태가 따로 없다. `SaveData`에
  `CargoCapDoubleHourExpiresUnixSeconds` 필드 추가.
  `MiningController`: `ClaimOfflineReward`를 `ApplyPendingOfflineReward(multiplier)` 공유 로직으로
  리팩터링하고 `ClaimOfflineRewardDoubled()`(2배) 추가, `WatchAdForExtraLootBox(type)`·
  `WatchAdForCargoCapDouble()`·`WatchAdForFuelRefill()` 신규 — 전부 `CanWatchRewardAd`로 오늘
  한도를 먼저 확인하고 `RecordRewardAdWatched`로 카운트를 올린 다음 실제 보상을 준다.
  `CargoCapacityMinerals`가 `RewardAdBoost.CargoCapMultiplier`도 곱하게 해서 상점 배율·구독
  배율과 함께 HUD 게이지·온라인 클램프·오프라인 보상 전부에 실제로 반영된다.
  화면 배선 네 곳: `OfflineRewardPanel`/`OfflineReward.uxml`에 "광고 보고 2배 받기"(오늘 한도
  남았을 때만), `CargoFullPanel`/`CargoFull.uxml`에 "광고 보고 1시간 상한 2배"(켜진 중이면 버튼
  대신 남은 시간 표시로 바뀜), `RaceEntryPanel`/`RaceEntry.uxml`의 entry-view에 "광고 보고 연료
  +3"(연료 부족할 때만), result-view에 "광고 보고 상자 1개 더"(상자를 받은 판일 때만, 한 판에
  한 번만 — 방금 이긴 코스의 등급을 `RaceBoxReward.ForTier`로 화면이 이미 알고 있는 값을 그대로
  넘긴다). `Core.Tests`에 `RewardAdBoost` 테스트 2개 추가(배율 경계값, 꺼진/켜진 상태에서 이어
  붙임) — **통과 123 / 실패 0**.
- 확인 필요: Unity 에디터가 없어 컴파일 확인은 다음 세션 몫. 새 파일은 하나도 없고(기존
  스크립트·UXML·USS 수정뿐) `.meta` 문제는 없음 — 다만 UI 바인딩(`Q<Label>("double-ad-label")`
  등 새 `name` 속성 6개)이 실제 UXML과 이름이 맞는지는 에디터에서 Play로 확인해 줄 것.
  **에디터가 있는 세션은 `GemRacer > 7. 메인 게임 씬 만들기`를 다시 눌러야** 이번 변경(과
  M-04·M-07·M-08도 여전히)이 웹에 반영된다 — 그전까지는 스크립트만 추가될 뿐 화면엔 변화가 없다.
- 웹에서 확인할 것: 없음(씬 반영 전까지는 화면 변화 없음, 위와 같은 이유). "오늘 웹에서 확인할
  것" 블록은 그대로 둠(RangeError/URP HDR 확인이 여전히 최우선).
- 이어서 할 것: `GemRacer/7` 씬 반영(에디터 세션 몫, M-04·M-07·M-08·M-09 전부 아직 안 반영)이
  최우선. 그다음으로 에디터 없는 세션이 이어받으면 backlog 순서상 **M-10(시즌 패스 데이터
  구조)**로 넘어갈 것 — M-09까지 마쳤으니 코드 쪽 M-0x 줄은 이번이 마지막이다.
- main 승격: 이번 M-09 나머지 커밋은 세션이 끝나는 시점에 Actions 빌드가 아직 안 끝나서 승격
  조건 2번을 못 채운다 — 다음 세션이 결과 확인 후 승격할 것.

### 10:55 — 웹에서 실제로 뜬다. 오늘 잡은 것 세 개가 한꺼번에 확인됐다

`0d13aff` 배포본을 브라우저로 열어 확인했다. **IndexedDB를 비우고** 열었다 — 어제 캐시에 속아
세 번 헛봤기 때문에 이제 이 절차를 지킨다.

보이는 것:

- **한글이 나온다.** 위에 "쿼츠 행성"과 "원석 0.0", 아래에 버튼 다섯 개
  "업그레이드 / 제작 / 레이스 / 상자 / 설정". 전부 Pretendard로 제대로 찍힌다
- **행성이 밝은 흰색이고 하늘이 보인다.** 어제 어두운 빨강이던 것이 `Skybox/Procedural`을
  Always Included에 넣으면서 풀렸다
- **스택 오버플로가 없다. 콘솔 에러 0.** UI를 uGUI로 옮기면서 사라졌다 —
  UIDocument 여덟 개가 원인이었다는 가설이 맞았다
- 버튼 다섯 개가 회색인 건 정상이다. 연결할 패널이 아직 안 옮겨졌다

`stripEngineCode`를 다시 켠 것도 통했다. 빌드가 exit 139 없이 끝났다 —
애초에 그걸 끈 이유가 UI Toolkit 모듈 때문이었으니 앞뒤가 맞는다.

**어제 배운 것 두 개를 남긴다.**

1. GitHub Actions **목록 페이지의 상태 아이콘을 믿지 않는다.** 실행 페이지를 직접 열어
   "Build failed with exit code"를 찾는다. 어제 이걸로 세 번 속았다
2. 배포 확인은 **IndexedDB를 비우고** 한다. 안 그러면 옛 wasm이 그대로 돌아서
   고친 것이 반영 안 된 것처럼 보인다

feedback.md의 세 항목(스택 오버플로·URP 셰이더·HUD 미표시)을 전부 닫았다. backlog U-09도 닫았다.

### 13:25 — 웹에서 버튼이 눌린다. 배속도 먹는다

`ddc4c58` 배포본을 `?fast=20` 으로 열어 확인했다(IndexedDB 비우고).

- **튜토리얼 말풍선이 뜬다.** "1 / 4"과 환영 문구, 파란 "다음" 버튼
- **"다음"을 실제로 클릭했더니 2/4로 넘어갔다.** 문구도 화물칸 설명으로 바뀌었다.
  **웹 빌드에서 버튼이 눌린 첫 사례다.** 새 Input System + InputSystemUIInputModule 배선이
  끝까지 통한다는 뜻이기도 하다
- **20배속이 먹는다.** 열고 20초 만에 원석 26.0, 클릭 직후 34.0. 정상 속도면 20초에 2 오른다.
  화물칸 게이지도 눈에 띄게 찬다

이제 "바라만 보는" 상태는 끝났다. 남은 것은 U-02 업그레이드다 — 그게 붙으면
원석을 실제로 쓰고 채굴이 빨라지는 것까지 확인할 수 있어서 핵심 고리가 닫힌다.

### 17:10 — 아트 확인은 폴더가 아니라 게임에서

Tifania 의견: 이미지를 폴더에서 한 장씩 열어 보는 게 아니라, **아침에 게임을 열었을 때**
"이건 고쳐야겠다"가 보여야 한다. 맞는 말이라 파이프라인을 거기까지 이었다.

문제는 아이콘 대부분이 아직 들어갈 화면이 없다는 것이다(업그레이드·제작·상자가 아직
안 옮겨졌다). 그래서 **게임 안에 아트 확인 화면**을 뒀다.

    https://planetracer-daz.pages.dev/?art=1
    https://planetracer-daz.pages.dev/?art=1&fast=20   (같이 써도 된다)

`Resources/Art` 아래 스프라이트를 전부 긁어서 이름과 함께 격자로 뿌린다. 스크롤 된다.
**새 이미지가 들어와도 코드를 고칠 필요가 없다** — 폴더에 넣으면 나온다.
실제 화면들이 다 옮겨지면 이 화면은 없애도 된다.

그래서 이미지 저장 위치를 `Assets/Art/`에서 **`Assets/Resources/Art/`**로 바꿨다.
Resources 아래에 있어야 런타임에 긁을 수 있다. 요청 32건의 경로를 전부 고쳤다.

**배경 제거도 자동으로 돌리게 바꿨다.** 앞서는 "Tifania가 보고 정한다"로 뒀는데,
게임에서 보고 판단하는 방식이면 마젠타가 남아 있으면 안 된다.

URL 파싱은 에디터에서 다섯 가지로 확인했다 — `?art=1`, `?art=1&fast=20`, 없음,
`?fast=20`만, `?art=0`. 전부 의도대로 갈린다.

### 18:12 세션 (평일 저녁, 매시간 도는 첫 차례)

- 시작 시 앞 세션(17:10, `21eddd7`) 커밋이 66분 전이라 안 겹쳐서 진행. `main`과 `claude/dev`가
  이미 같은 커밋이라 승격할 것도 없었다.
- `docs/feedback.md`에 `- [ ]`(처리할 것) 없음 — 전부 `[x]` 또는 이미 판단 대기(`[?]`, Made with
  Unity 스플래시)로 넘어가 있음.
- backlog를 훑다가 **M-09 체크박스가 안 돼 있는 걸 발견**(07:05 세션이 이미 네 자리 다 실제
  지급까지 끝냈는데 줄만 진행 중으로 남아 있었다) — CLAUDE.md 7번대로 이번 세션이 `[x]`로 고치고
  요약을 다시 씀.
- 한 일: backlog 순서상 다음 항목 **M-10 시즌 패스 데이터 구조**. core `SeasonPass.cs` 신규 —
  `SeasonPassTier`/`SeasonPassState`/`SeasonPassProgress`(`LevelForXp`/`CanClaim`/`Claim`/`AddXp`).
  이름이 기존 "행성 통행증 구독"(M-06, monetization.md 2-5)과 헷갈리기 쉬워서 파일 상단에 크게
  구분 주석을 남겼다 — 이건 2-6 "시즌 패스"(4주 배틀패스, 무료/유료 트랙)다. `DefaultData.
  SeasonPassTiers()` 10티어 초안 — 무료 트랙은 채굴차 부품·상자·원석만, 유료 트랙은 정제
  광물·화물칸 임시 확장·스킨 id만 넣고, 유료 트랙에 전투력 보상이 섞이면 실패하는 테스트로
  monetization.md의 "절대 팔지 않는 것"을 코드에도 박았다. `SaveData` 필드 4개 + 왕복 함수.
  `Core.Tests`에 10개 추가 — **통과 133 / 실패 0**. 상세 설계 근거는 backlog.md M-10에 다 적어 둠.
- 확인 필요: Unity 에디터가 없어 컴파일 확인은 다음 세션 몫. 새 core 파일(`SeasonPass.cs`)에
  아직 `.meta`가 없다(에디터가 여는 다음 세션에서 생기는 대로 커밋할 것, 계속 반복되는 패턴).
- 웹에서 확인할 것: 없음. 이번 건은 core 데이터·계산 코드뿐이라 화면이 없다.
- 이어서 할 것: M-10 나머지(화면, XP 지급 시점 결정 — 레이스 승리마다인지 등, 유료 트랙 구매
  SKU, 시즌 4주 경계 스케줄링)는 전부 다음으로 남겨 뒀다. 그 외엔 여전히 `GemRacer/7` 씬 반영
  (M-04·M-07·M-08·M-09 전부 아직 안 반영, 에디터 세션 몫)이 최우선으로 밀려 있다.
- main 승격: 이번 M-10 커밋은 세션이 끝나는 시점에 Actions 빌드가 아직 안 끝나서 승격 조건
  2번을 못 채운다 — 다음 세션이 결과 확인 후 승격할 것.

### 19:05 세션 (평일 저녁, 매시간 도는 두 번째 차례)

- 시작 시 앞 세션(18:12, `7dec9ed`) 커밋이 52분 전이라 안 겹쳐서 진행.
- `docs/feedback.md`에 `- [ ]`(처리할 것) 없음 — 전부 처리됨/판단 대기 상태.
- **main 승격**: 18:12 세션이 미룬 M-10 커밋(`7dec9ed`)의 Actions 빌드(#109)가 이번 세션 시작
  시점엔 이미 성공으로 끝나 있었다. fast-forward 확인 후 `main`을 `7dec9ed`로 승격.
- 한 일: backlog 순서상 D18-N(리텐션 훅) 중 **"하루 첫 접속 보상"** 쪽. core `DailyLoginReward.cs`
  신규 — 마지막 접속일+연속 접속 일수를 `RewardAdTracker.DayIndex`(기존 KST 자정 판정 재사용)로
  관리, 오늘 이미 받았으면 방어적으로 통과, 어제 받았으면 스트릭 +1, 하루 건너뛰면 스트릭만 1로
  리셋(완전 초기화 아님). 보상은 7일 주기표(5→30 원석, 7일차가 6배라 완주 유인). `SaveData`
  필드 2개 + 왕복 함수. **화물칸 알림 쪽(같은 D18-N의 나머지 절반)은 M-05와 같은 이유로 손 안
  댐** — Unity Mobile Notifications 패키지가 필요한데 클라우드 세션이 Package Manager를
  건드리면 URP 셰이더 사고처럼 빌드가 죽을 위험이 있어서다. 덤으로 backlog D18-M(알림 예약 시각
  계산 테스트)이 M-05에서 이미 끝나 있던 걸 발견해 체크 누락을 고쳤다(M-09 때와 같은 패턴).
  `Core.Tests`에 7개 추가 — **통과 140 / 실패 0**.
- 확인 필요: Unity 에디터가 없어 컴파일 확인은 다음 세션 몫. 새 core 파일(`DailyLoginReward.cs`)에
  아직 `.meta`가 없다.
- 웹에서 확인할 것: 없음. 이번 건도 core 데이터·계산 코드뿐이라 화면이 없다.
- 이어서 할 것: D18-N 나머지는 전부 에디터 세션 몫 — 하루 첫 접속 보상 팝업 화면(간단한 카드
  하나, `Claim` 결과로 원석 지급) + 화물칸 알림(Mobile Notifications 패키지 추가부터).
  그 외엔 여전히 `GemRacer/7` 씬 반영(M-04·M-07·M-08·M-09·M-10 전부 아직 안 반영)이 최우선으로
  밀려 있다. main 승격: 세션 시작 시점의 `7dec9ed`(M-10)는 이번 세션이 이미 승격 완료. 이번
  세션이 새로 커밋한 `066f837`(D18-N)은 push 시점에 Actions 빌드가 아직 안 끝나서 승격 조건
  2번을 못 채운다 — 다음 세션이 빌드 결과 확인 후 승격할 것.

### 19:20 세션 (Unity 배선 세션 — 에디터가 있는 차례)

- Unity가 이미 켜져 있었다(13:02 시작). Tifania가 쓰던 중일 수 있으니 **이 세션은 닫지 않았다.**
  열려 있던 MainGame 씬은 저장된 상태였고 Play도 아니어서 그대로 이어받았다.
- 먼저 컴파일 확인. 밤 세션들이 에디터 없이 쓴 코드(`CargoFullPanel`·`ShopPanel`·`Entitlements`·
  `RewardAd`·`SeasonPass`)가 **에러 0으로 컴파일된다.** M-04·M-06·M-07·M-08·M-09·M-10에 줄줄이
  달려 있던 "컴파일 확인은 다음 세션 몫"을 이걸로 닫는다. 없던 `SeasonPass.cs.meta`도 이번에
  생겨서 같이 커밋했다.
- **`GemRacer/7`을 그냥 누르면 안 되는 상태였다.** 이 메뉴는 씬을 처음부터 다시 만드는데,
  9/15에 uGUI로 옮긴 것들(HUD·튜토리얼 U-01, 아트 확인 화면, `?fast=` 배속)은 **씬에만 있고
  부트스트랩에는 없었다.** 그대로 눌렀으면 U-01이 조용히 사라지고 화면이 옛 UI Toolkit HUD로
  되돌아갔다 — 한글이 빈칸으로 나오고 9/14에 웹을 죽였던 스택 오버플로 경로도 되살아난다.
  밤 세션들이 이 메뉴를 세 번 미뤄 둔 게 결과적으로 다행이었다.
- 그래서 `BootstrapMainGame.cs`를 먼저 고쳤다. 씬을 다 세운 뒤에 `BootstrapHudUgui.Build()`·
  `BootstrapTutorialUgui.Build()`·`BootstrapArtViewer.Build()`를 부르고, 옛 UI Toolkit 루트
  열 개를 전부 끄고(지우지 않는다 — ugui-migration.md 그대로), `GameFlow`에 `DebugTimeScale`을
  붙이고, `MainHudUgui`의 `target`·`audioHub`를 채운다. 이제 이 메뉴를 다시 눌러도 같은 결과가
  나온다(CLAUDE.md 3번 "멱등").
- 그리고 실행. 씬에 **상점 오버레이(M-07)와 화물칸 가득 참 오버레이(M-04·M-08)** 가 들어왔다.
  둘 다 UI Toolkit판이라 꺼진 채로 들어간다 — U-10에서 uGUI로 옮길 때 켜진다.
- Play 13초 — **예외 0**. 게임 뷰 스크린샷으로 눈으로 확인: 한글이 다 나오고(상태바 "쿼츠 행성 /
  원석 0.0", 튜토리얼 2/4 말풍선), 흰 행성 + 파란 하늘, 채굴차가 지면 위에 있다. 아래 버튼
  다섯 개는 회색(꺼짐) — U-02~U-07이 아직 안 옮겨져서 의도대로다.
- Play 중에 TMP 동적 폰트 아틀라스에 한글 글리프가 구워져서 `Pretendard-Regular SDF.asset`도
  같이 바뀌었다. 커밋에 넣었다 — 웹 빌드에 글리프가 미리 들어가는 쪽이 낫다.
- 씬 저장 + `File/Save Project`까지 하고 MainGame이 Build Settings 0번인 것도 다시 확인.
- 웹에서 확인할 것: **달라지는 게 없어야 정상이다.** 상점·화물칸 화면은 꺼진 채라 아직 안 보이고,
  화면 구성은 지난 배포와 같다. 혹시 버튼 줄이 두 겹으로 보이거나 한글이 빈칸이면 이 커밋 탓이니
  `docs/feedback.md`에 한 줄 적어 주면 된다.
- 이어서 할 것: U-02(업그레이드 화면 uGUI 이사)가 다음 차례다. 그게 되면 HUD 첫 버튼이 살아난다.
>>>>>>> 25eb1f0 ([D08-25] GemRacer/7 씬 반영 (M-04·M-07·M-08) + 그 메뉴가 uGUI 이사를 지우던 문제 수정)
