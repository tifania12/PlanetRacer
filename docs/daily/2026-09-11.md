# 2026-09-11 (금) — 시작

## 한 일
- 저장소 골격, 작업 규칙(CLAUDE.md), 일 단위 작업 목록(docs/backlog.md)
- 순수 C# 코어: 행성·채굴차·부품·레이싱카 모델, MiningSimulator(시간당 산출·오프라인 누적), RaceSimulator(결정론적 레이스), DefaultData(행성 6개, 쿼츠 코스 3개, 시작 부품 5개)
- 테스트 러너 9개 통과. 첫 부품 제작 비용을 40 → 15 광물로 내렸다(첫 부품까지 12.6분 → 4.7분, 5분 관문 기준)

## Tifania가 할 것
1. GitHub 저장소 만들고 이 폴더 push, Claude 앱에 GitHub 연결 (T-01)
2. Unity Hub → Unity 6 LTS → Universal 3D 템플릿 → 위치는 저장소의 `Unity/` (기존 `Packages/` 폴더가 있는 곳). 생성 후 Package Manager > In Project에 "GemRacer Core"가 보이는지 확인 (T-02)
3. `docs/decisions.md` 미정 2개 중 결정된 것 적기 (T-03)
4. 커밋·push

## 확인 포인트
- [ ] Package Manager에 GemRacer Core 0.1.0 표시
- [ ] 콘솔에 컴파일 에러 없음 (코어는 UnityEngine을 안 쓰므로 에러가 나면 asmdef 문제)
- [ ] (선택) 터미널에서 `cd Core.Tests && dotnet run` → 통과 9 / 실패 0

## 막힌 것·결정 필요
- 이 환경에서 NuGet이 막혀 있어 xunit 대신 콘솔 러너를 쓴다. 문제 없음.
- Unity 에디터가 없으니 씬은 에디터 메뉴 스크립트로 생성하는 방식으로 간다 (D01-N부터).

## 아침 피드백 (Tifania가 적는 곳)
-
