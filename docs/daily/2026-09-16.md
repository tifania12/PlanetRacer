# 2026-09-16 (수)

## 오늘 웹에서 확인할 것

1. **`RangeError` 없이 첫 화면(튜토리얼 말풍선·HUD)이 뜨는지** — 스택 크기 수정이 9/14에 처음
   production(`main`)에 나갔다. 여전히 죽으면 예외 메시지의 관리 코드 스택으로 지점을 잡는다.
2. 행성이 흰색 + 파란 하늘로 보이는지, HUD(하단 버튼 다섯 개)도 같이 보이는지.
3. 하단 "업그레이드"/"제작" 버튼이 눌리고 화면이 뜨는지, 세로/가로 재배치와 스크롤이 되는지.

## 00:06 세션

- 시작 시 앞 세션(`a895717`) 커밋이 8분 전이라 겹칠 가능성이 높아 새 작업은 시작하지 않음.
- `docs/feedback.md`에 처리할 `- [ ]` 항목 없음(전부 `[x]`거나 Tifania 판단 대기인 `[?]`).
- 이어서 할 것: 다음 세션이 `docs/backlog.md` 위에서부터 1~2항목 집으면 됨.

## 00:12 이미지 세션

- 이미지 생성 0장. 화면 캡처가 막혀서 시작 전에 멈춤.
- 전제 조건은 넷 다 통과였다 — KST 00:12(20~08시), 대기열 32건, 유휴 21643초(≈6시간), ChatGPT 앱 실행 중.
- 막힌 지점: `Screenshot`/`Snapshot`이 연속 실패. 전체 화면은 "screen grab failed",
  단일 디스플레이(`display=[0]`)는 `액세스가 거부되었습니다`(0x80070005). 화면 두 대, 잠금 화면은 아님(LogonUI 없음).
  화면을 볼 수 없으면 로그인 화면인지·한도 메시지인지·그림이 실제로 나왔는지 확인할 방법이 없어서
  키보드를 눌러보지 않고 끝냈다. PC에는 아무것도 입력하지 않았고 파일도 이 줄 말고는 건드리지 않았다.
- Tifania가 볼 것: 원격 데스크탑 도구의 화면 캡처 권한. 자동 이미지 세션은 이게 풀려야 돌아간다.
- 대기열 32건은 그대로 `docs/design/art-requests.md`에 남아 있다.
- 참고 1: `docs/design/art-requests.md`는 받은 그림에 `tools/strip_bg.py`를 자동으로 돌린다고 적혀 있지만,
  이미지 세션 지침은 마젠타 배경을 그대로 두고 daily에 "배경 제거 필요"라고만 적으라고 한다. 둘이 어긋난다.
- 참고 2: `CLAUDE.md`의 "이미지는 GPT에서 만든다 — 이 세션은 프롬프트만 남긴다(2026-09-15)" 항목은
  이제 옛말이다. 이미지 세션이 PC의 ChatGPT 데스크탑 앱을 직접 조작한다.

### 01:08 세션

- 한 일: U-05(공구 상자 uGUI 이사) 코드까지 완료 — `LootBoxUgui.cs` + `BootstrapLootBoxUgui.cs`
  (`GemRacer/19`) 신규. `MainHudUgui.boxPanel`은 이미 준비돼 있어서 코드 쪽은 손댈 곳이 없었다.
  덤으로 A-10 체크 누락을 발견해 바로잡음 — 상자 3종 포함 우선순위 아이콘 18종이 이미 전부
  대기열에 올라와 있었다(체크만 안 돼 있었음). Core는 안 건드려서 `dotnet run` 생략(코어 변경 없음).
  이번 건은 씬 배선이 안 끝나 웹에 안 보임.
  - 이어서 할 것: `GemRacer/19` 실행 → `MainHudUgui.boxPanel`에 `LootBox` 물리기 → 세 기준점
    Play 확인(특히 세로에서 결과 카드까지 포함해 넘치지 않는지). 그다음 backlog는 U-06(설정 화면).

### 02:05 이미지 세션

- 이미지 생성 0장. 00:12와 똑같이 화면 캡처에서 막혀 아무것도 시작하지 못했다.
- 전제 조건 넷은 이번에도 다 통과 — KST 02:05(20~08시), 대기열 32건, 유휴 28791초(약 8시간), ChatGPT 앱 3개 프로세스 실행 중.
- 막힌 지점(캡처 3회 시도, 전부 실패):
  - `Screenshot` 전체 화면 → "screen grab failed"
  - `Screenshot display=[0]` → `액세스가 거부되었습니다` (0x80070005)
  - 모니터 절전일까 싶어 마우스를 움직여(클릭 없이 호버만) 깨운 뒤 4초 기다렸다가 재시도 → 같은 실패
- 이번에 새로 확인한 것 — **절전이나 잠금 문제가 아니다.**
  - `quser`: baxxr / console / **Active** (2026-09-14 10:42 로그온)
  - `LogonUI` 프로세스 0개, `TerminalServerSession` = False → 잠금 화면 아님
  - MCP 프로세스도 같은 세션(SessionId=1)에서 돌고 있다
  - 모니터 2대 정상 인식: DISPLAY1 2560x1440, DISPLAY2 1920x1080
  - 즉 "같은 데스크탑에 붙어 있는데 캡처 API만 액세스 거부"다. 화면 캡처 권한 쪽을 봐야 한다.
- 그래서 키보드는 한 번도 누르지 않았다. 화면을 못 보면 로그인 화면인지, 사용량 한도 메시지인지,
  그림이 실제로 나왔는지 확인할 방법이 없어서 ChatGPT 앱에 아무것도 입력하지 않았다.
  PC에 들어간 입력은 위의 마우스 호버 한 번뿐이고, 파일은 이 daily 줄 말고 건드린 것이 없다.
- 대기열 32건은 `docs/design/art-requests.md`에 그대로 있다. 다시 요청(`- [!] 다시:`)은 실제 항목 없음(설명문 예시 한 줄뿐).

**Tifania가 볼 것** — 이게 풀리기 전엔 밤마다 이 세션이 0장으로 끝난다.
원격 데스크탑 도구(Windows-MCP)의 화면 캡처 권한을 확인해 주세요. 볼 만한 것:
Windows 설정 > 개인 정보 및 보안 > 화면 캡처/카메라 계열 권한, 그리고 Claude 데스크탑 앱을
관리자 권한으로 띄운 창이 따로 있는지(권한 수준이 다르면 캡처가 거부된다).

남은 두 가지는 00:12에 적은 것과 같고 아직 그대로다.
- `art-requests.md`는 마젠타 배경을 `tools/strip_bg.py`로 자동 제거한다고 적혀 있는데,
  이미지 세션 지침은 배경을 그대로 두고 daily에 "배경 제거 필요"만 적으라고 한다. 어느 쪽인지 정해야 한다.
- `CLAUDE.md`의 "이미지는 GPT에서 만든다 — 세션은 프롬프트만 남긴다(2026-09-15)"는 이제 옛말이다.

추가로 하나 — **유휴 시간 중간 점검이 설계대로 작동하지 않는다.**
`GetLastInputInfo`는 사람이 넣은 입력과 자동화가 넣은 입력을 구분하지 않는다. 그래서 세션이
타이핑을 시작한 뒤에는 유휴 시간이 항상 0에 가깝게 읽혀서 "다섯 장마다 300초 확인"이 무조건
걸리게 된다. 시작 전 확인은 유효하지만, 중간 점검은 다른 방법이 필요하다
(예: 세션이 마지막으로 입력한 시각을 따로 기록해 두고 그 이후 들어온 입력만 사람 것으로 본다).
