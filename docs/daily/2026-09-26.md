# 2026-09-26 (토)

## 00:1x 코딩 세션 — decisions.md 목차 갱신 (code-only 여전히 바닥)

- **시작**: `origin/claude/dev` 마지막 커밋(23:1x, ShopPurchase 경계값 테스트)이 56분 전이라
  안 겹침. `docs/feedback.md` `- [ ]` 없음(전부 `[x]`/`[?]`).
- **backlog를 다시 훑었다** — `grep -n "^- \[ \]" docs/backlog.md`로 전체를 봤는데 여전히
  전부 아트 대기·Unity 세션 몫·Tifania 결정 대기·날짜 미도래뿐이었다. code-only 새 항목 없음
  (09-24 05:1x부터 이번까지 계속 같은 결론).
- **23:1x 세션이 남긴 "이어서 할 것"(Amplifier·Entitlements·SeasonPass의 안 덮인 분기 찾기)을
  직접 확인했다.** `Amplifier.cs`·`Entitlements.cs`·`ShopPurchase.cs`·`RewardAd.cs`·
  `PartCraft.cs`·`LootReward.cs`·`RigPartApply`·`PetSpeciesTable`까지 코드와 `Core.Tests`를
  나란히 대조해 봤는데, **정의 밖 enum 값·경계 시각·빈 컬렉션·switch의 default 분기까지
  이미 다 테스트돼 있었다.** 새로 찾은 구멍이 없다 — 지어내서 테스트를 추가하지 않았다.
- **대신 문서 어긋남 하나를 찾아 고쳤다(CLAUDE.md 7번).** `docs/decisions.md`의
  "지금 판단 대기 중인 것" 목차는 2026-09-22 19시에 만들어져 "다섯" 항목만 가리키는데,
  그 뒤 2026-09-24에 추가된 "HUD 아래 버튼 여덟 개가 안 들어간다" 절(B-02와 같은 내용)이
  목차에 없었다 — 실제로는 여섯 번째 판단 대기 항목이다. 목차에 항목을 추가하고 "다섯"을
  "여섯"으로 고쳤다. 09-25 07시 세션이 확인한 9칸 상황(`btn-planet` 추가로 57→50px)도
  같은 문단에 한 줄로 얹었다.
- 코드·테스트 변경 없음(문서만). `dotnet run` 불필요. 웹에 보이는 변화 없음 —
  이번 건은 웹에 안 보임.
- 이어서 할 것: code-only 항목은 계속 바닥이고, 이번에 확인해 보니 switch/분기 구멍 찾기도
  당분간은 바닥일 가능성이 높다(위 8개 파일을 직접 대조해 확인한 결과). 다음 세션은
  feedback.md·decisions.md 목차만 가볍게 재확인하고, 그래도 시간이 남으면 `PetArt`·
  `RaceBoxReward`·`CourseGenerator` 등 아직 이 방식으로 안 훑은 파일을 봐도 되지만, 이번
  결론(바닥)이 몇 차례 더 반복되면 CLAUDE.md 규칙 넷(설계 문서에서 새 backlog 항목을
  만든다)으로 넘어가는 게 맞다.
## 00:1x 이미지 세션 — 대기 중이 비어 0장 (0절 4번에서 끝)

KST 00:10 시작. **전제 0절 4번에서 끝났다** — `docs/design/art-requests.md`의 "대기 중"이
비어 있다. 뽑을 것이 없으니 유휴 검사·`prepare_capture.ps1`·ChatGPT 앱 조작은 하지 않았다.
**0장.** 09-23 20시부터 **열 세션 연속** 같은 결론이다.

### 전제 확인

- 0절 1번(시각) — 00:10 KST, 20:00~08:00 안이라 통과.
- 0절 4번(대기 목록) — **여기서 끝.** 문서가 시킨 확인 한 줄을 돌린 결과 `MISSING:` **30줄**.
  문서가 오탐으로 못 박아 둔 두 종류(옛 이름 `pet-t1-*`~`pet-t7-*`, 문장 속 잘린 접두사
  `arrive-`·`icon-grade-`·`planet-`·`pet-t`)를 걸러내니 **남는 줄이 하나도 없다.**
  등급 폴더의 실제 PNG는 `1-common` 16 · `2-base` 16 · `3-base` 16 · `4-base` 16 ·
  `5-legend` 20 · `6-myth` 30 · `7-transcend` 10 = **124장**.
- `grep -n "A-20\|A-15" docs/backlog.md` — 둘 다 `- [ ]` 그대로(229행 A-20 채굴차 시트 자르기,
  300행 A-15 스토어용). 대기 중이 비어 있는 것이 맞다.
- 0절 5번(앞 세션 이미지 한도) 해당 없음 — 09-25 daily에 `이미지 한도 — N시간 후 풀림` 줄 없음.
  09-23 20시 이후 어느 세션도 이미지를 한 장도 쓰지 않았다.
- 0절 2·3번(유휴·ChatGPT)은 재지 않았다 — 4번에서 끝났으므로 PC를 건드릴 이유가 없다.
- 설계 문서는 다시 훑지 않았다 — 0절 4번이 "다시 증명하지 말 것"이라고 못 박아 둔 대로다.

### 이어서 할 것

- **A-20(채굴차 시트를 잘라 티어별로 모자란 것이 드러날 때)이나 A-15(출시 준비)가 진행될
  때까지 이미지 세션은 계속 0장으로 끝난다. 고장이 아니다.**
- 이미지 한도는 온전하다 — 뽑을 것이 생기면 12장 상한을 다 쓸 수 있다.

## 01:1x 코딩 세션 — M-13은 손대지 않음(판단 대기), DeterministicRandom 방어 버그 하나 고침

- **시작**: `origin/claude/dev` 마지막 커밋이 43분 전이라 안 겹침. `feedback.md` `- [ ]` 없음.
- **M-13(오프라인 상한 연장 상품 무효과)을 실제로 고치려다 되돌렸다.** `MiningController.
  ComputeOfflineReward`가 `elapsedSeconds`에 `Entitlements.OfflineCapHours`를 전혀 안 걸어서
  상품이 12h를 계산까지 해 놓고 아무 데도 안 쓰는 걸 코드로 직접 확인했다(경과 시간이 얼마든
  `MiningSimulator.Offline`에 그대로 다 들어감 — `RefinedGained`는 무제한). 클램프까지 짜서
  `Core.Tests` 377개 통과까지 확인했는데, 커밋 직전에 `docs/decisions.md`를 다시 보니 **이미
  2026-09-20에 Tifania 판단 대기(A/B/C안)로 못 박아 둔 항목이었다** — 내가 고른 방식(정제
  광물도 상한으로 자름)이 그 A안 그대로라, 판단 없이 기존 무제한 정제 광물 감각을 깎는
  선택을 세션이 임의로 확정하는 셈이었다. **되돌렸다**(`git checkout --`, 커밋 안 함).
  M-13은 여전히 backlog `- [ ]` 그대로 둔다 — 코드는 다 짜 놨으니(A안 기준) Tifania가
  A/B/C 중 하나를 고르면 다음 세션이 몇 분 안에 끝낼 수 있다.
- **대신 실제로 안전한 버그 하나를 찾아 고쳤다.** `RaceSimulator.cs`의
  `DeterministicRandom.NextInt(min, max)`가 `min == max`(빈 범위)면 `% 0`으로
  `DivideByZeroException`을 던진다 — 기존 경계값 테스트("단일값 범위" `NextInt(7,8)`, 폭 1)가
  이 폭 0 케이스를 안 잡고 있었다. 지금 실제 호출부(`CourseGenerator`, `PetSpecies.PickInGrade`)는
  전부 폭이 0보다 커서 아직 안 터지지만, `PickInGrade`가 빈 등급 배열을 넘기면 바로 이 경로다 —
  `MakeOpponents`가 이미 음수 count를 방어하는 것과 같은 이유로 방어 코드를 추가했다
  (`maxExclusive <= minInclusive`면 `minInclusive`를 그대로 돌려줌). `Core.Tests`에 경계 테스트
  4개 추가(0,0 / 3,3 / -2,-2 / 역전된 5,1) — 376 → **377, 실패 0**.
- **backlog가 다시 바닥이었다 — 이번엔 그냥 넘어가지 않고 파고들었다.** M-13 외에
  `docs/decisions.md` "지금 판단 대기 중인 것" 여섯 항목 전부 재확인(전부 Tifania 결정 대기
  또는 저장소 밖 문제, 급하지 않음 확인됨), `docs/design/monetization.md` 2-6~2-8(시즌 패스·
  스킨·광고 제거)이 backlog에 이미 반영돼 있는지 대조(전부 M-07/M-10 등에 있음), `RaceBoxReward`·
  `CourseGenerator`·`RaceSimulator` 세 코어 파일을 전부 다시 읽고 기존 테스트와 대조(위 NextInt
  하나 빼고 더 없음), `docs/design/balance/tempo.md`(증폭기로 2시간 이후가 안 채워짐 — W2 코스
  전까지는 원래 열려 있는 문제, 코드로 더 할 게 없음), `docs/design/p0-gate.md`(2026-09-11
  D00~D03 시절 문서라 완전히 지난 얘기, 손댈 것 없음) 순으로 훑었다. **code-only 새 backlog
  항목은 이번에도 못 찾았다** — 이제 09-24 05시부터 이번(01시)까지 여러 세션 연속 같은
  결론이다.
- **이어서 할 것**: M-13은 Tifania가 A(정제도 상한 적용)/B(정제는 무제한 유지, 원석·보물만
  상한 적용 — 지금 화물칸으로 사실상 이미 막혀 있어 체감 약할 수 있음)/C(상품 재설계) 중
  하나만 골라 주면 코드는 이미 준비돼 있다(`git log`엔 안 남지만 이번 세션이 A안 구현을
  한 번 짜 봤다는 뜻 — 되돌린 diff라 다음 세션이 처음부터 다시 짜야 함, 어렵지 않음).
  code-only 탐색은 몇 차례 더 반복되면 CLAUDE.md 규칙 넷(설계 문서에서 새 backlog 항목을
  만든다) 쪽으로 완전히 넘어갈 것 — 이번 세션은 새 항목을 "만들기"보다 "찾기"에 시간을
  더 썼는데, 다음엔 만드는 쪽(예: 시즌 패스 실제 UI 화면이 아직 backlog에 없는지, 스킨
  카탈로그 데이터 구조가 아직 없는지)도 봐야 할 것 같다.
## 01:1x Unity 배선 세션 — M-05 화물칸 80% 로컬 알림을 씬에 붙였다 (에디터 세션 몫이던 것)

- **시작**: `origin/claude/dev` 마지막 커밋(00:31 이미지 세션)이 42분 전이라 안 겹친다.
  정해진 grep 표식(`씬 배선은 Unity 세션 필요`)은 `docs/backlog.md`·`docs/feedback.md`
  **양쪽 0건**이고, `Assets/Editor/`의 `Bootstrap*Ugui.cs` **16개가 전부 이미 씬에 서 있다**
  (09-25 19시 세션이 `UI Canvas/Overlays` 아래 16개를 직접 세어 확인해 둔 그대로).
  **그래서 "배선 대기"로는 할 일이 없었다.** 그대로 끝내는 대신 CLAUDE.md "미루지 않는다"
  규칙대로 **이 세션만 할 수 있는 것**을 집었다 — M-05 / D18-N 화물칸 알림 축.
  9/15부터 "Package Manager는 클라우드 세션이 건드리면 위험하니 에디터 세션 몫"으로
  열흘 넘게 밀려 있던 항목이다.

- **① 패키지를 깔았다.** `com.unity.mobile.notifications@2.5.0`을 Package Manager로 추가.
  `manifest.json`·`packages-lock.json`이 같이 갱신됐다. 이게 클라우드 세션이 못 하던 부분이다.

- **② 글루 레이어를 새로 썼다** — `Assets/Scripts/Mining/CargoNotificationScheduler.cs`(신규).
  "언제 알릴지"는 코어(`MiningSimulator.HoursUntilCargoThreshold`, 9/15에 이미 테스트 4개까지
  끝나 있던 것)가 계산하고, 이 컴포넌트는 그 값을 플랫폼 알림 API에 넘기기만 한다 —
  RewardAdTracker·DailyLoginReward와 같은 역할 분리다. 동작:
  - 백그라운드로 갈 때(`OnApplicationPause(true)`)·종료할 때 예약, 돌아오면 전부 취소.
    안 지우면 이미 비운 화물칸을 두고 알림이 온다.
  - 코어가 `null`을 주면(정제가 유입을 따라잡아 원석이 그 이상 안 쌓임) **예약하지 않는다** —
    코어 주석이 시킨 그대로다. `0`(이미 80% 넘음)도 예약 안 함.
  - 10분보다 가까우면 예약 안 함. 홈 버튼 눌렀다 바로 돌아오는 사람에게 1분 뒤 알림은 성가시다.
  - 알림 API 호출부는 `#if UNITY_ANDROID`/`UNITY_IOS`로 감쌌다. **에디터·WebGL에서는 계산까지만
    하고 콘솔에 한 줄 남긴다** — WebGL 빌드에 알림 코드가 안 들어가니 웹 빌드는 안전하고,
    Play 모드에서 값이 맞는지 눈으로 볼 수 있다.

- **③ 씬에 붙였다.** `MiningRig` 오브젝트(= `MiningController`가 붙어 있는 그것)에
  `CargoNotificationScheduler` 하나 추가. `GemRacer/7`은 **누르지 않았고**, 씬을 연 채로
  `execute_code`로만 더했다(CLAUDE.md 함정 규칙). 씬 diff는 **컴포넌트 한 개 16줄이 전부**다.

- **확인한 것**
  - `refresh_unity` 후 **컴파일 에러 0** (콘솔에 남은 건 Unity AI `NoSubscription` 하나뿐인데
    이건 이번 작업과 무관하고 세션 시작 전부터 있던 것).
  - **Play 13초 — 예외 0.** 실행 중에 실제 경로(`ScheduleCargoNotification()`)를 한 번 태워 봤다:
    `행성=quartz 원석=564.02 상한=1534.9 80%=1227.9 유입/h=383.72 정제/h=134.30 → 2.66시간`.
    손으로 검산해도 (1227.9−564.02)÷(383.72−134.30)=**2.66시간**으로 맞다.
    콘솔 로그도 그대로 찍혔다 — `[화물칸 알림] 예약함 — 2.66시간(160분) 뒤.`
  - `Core.Tests` — **통과 376 / 실패 0** (코어는 안 고쳤지만 확인은 했다).
  - 씬 저장 + `File/Save Project`까지 했다.
  - **못 한 확인 하나** — 게임 뷰 스크린샷이 이번엔 3D만 찍히고 오버레이 UI가 안 나왔다
    (`manage_camera`가 camera 지정 경로로 붙어서 Screen Space-Overlay 캔버스를 뺀 듯하다).
    이번 변경이 UI를 건드리는 게 아니라 그냥 넘어갔다. 다음 세션이 UI 화면을 찍어야 하면
    이 증상을 먼저 볼 것.

- **Unity는 닫지 않았다** — 이 세션이 켠 게 아니라 09-22부터 떠 있던 것이라 그대로 뒀다.

### 이어서 할 것

- **실제 알림이 뜨는지는 안드로이드 실기/에뮬레이터에서만 볼 수 있다.** 에디터·WebGL은
  계산까지만 하므로 dev 주소에서는 확인할 것이 없다 — **이번 건은 웹에 안 보인다.**
- D18-N이 `- [x]`가 되려면 남은 건 **화물칸 오버플로 결정 하나**(꽉 찬 채로 접속하면 잘린
  원석을 따로 보관할지). 정보구조 결정이라 세션이 임의로 못 정한다 — Tifania 몫이다.
- U-08 ②③⑤(옛 UI Toolkit 파일 삭제)는 이번에도 못 했다. 파일 삭제가 세션 실행 환경에
  막혀 있고(`Irreversible Local Destruction`), 09-24 21시 세션이 정한 대로 **우회하지 않는다.**

### 01:5x 후속 — WebGL 빌드 확인 (패키지를 깔았으니 이건 꼭 봤다)

- 알림 패키지를 새로 깔았으니 CI가 죽지 않는지가 이번 세션의 진짜 관문이었다.
  API로 확인(목록 아이콘 말고, CLAUDE.md 규칙대로): **run 508 / `08e0f47` / completed success.**
  앞 커밋 507(`e621ed3`, D26-N)도 success. **`com.unity.mobile.notifications`를 추가해도
  WebGL 빌드는 멀쩡하다** — `#if UNITY_ANDROID`/`UNITY_IOS` 가드가 의도대로 먹었다.
- dev 주소를 브라우저로 직접 열어 보지는 못했다 — 이 세션에서 PC의 Chrome 확장에 닿지 않았다
  (`Browser extension is not connected`). 이번 변경은 어차피 웹에 안 보이는 것이라 넘어갔지만,
  **패키지가 늘었으니 아침에 dev 주소가 평소처럼 뜨는지만 한 번 봐 주면 좋다**
  (https://dev.planetracer-daz.pages.dev — 게임이 뜨고 HUD 버튼이 보이면 그걸로 충분하다).
- main 승격은 하지 않았다. CLAUDE.md대로 승격은 Tifania 몫이고, 세션은 조건 확인까지만 한다.
