# 2026-09-26 (토)

### 오늘 웹에서 확인할 것

- `https://dev.planetracer-daz.pages.dev` — 업그레이드 화면 곡괭이 줄(맨 위)의 작은 아이콘.
  이제까지는 회색 톱니바퀴 그림이었는데, **채굴차 실루엣**으로 바뀌어 있으면 A-20이 반영된
  것이다(지금 시작 레벨 1이면 곡괭이 팔 버전). CI 빌드가 실패했으면 안 바뀐 채로 남는다.

## 00:1x 코딩 세션 — decisions.md 목차 갱신 (code-only 여전히 바닥)

- **시작**: `origin/claude/dev` 마지막 커밋(23:1x, ShopPurchase 경계값 테스트)이 56분 전이라
  안 겹침. `docs/feedback.md` `- [ ]` 없음(전부 `[x]`/`[?]`).
- **backlog를 다시 훑었다** — `grep -n "^- \[ \]" docs/backlog.md`로 전체를 봤는데 여전히
  전부 아트 대기·Unity 세션 몫·Tifania 결정 대기·날짜 미도래뿐이었다. code-only 새 항목 없음
  (09-24 05:1x부터 이번까지 계속 같은 결론).
- **23:1x 세션이 남긴 "이어서 할 것"(Amplifier·Entitlements·SeasonPass의 안 덮인 분기 찾기)을
  직접 확인했다.** `Amplifier.cs`·`Entitlements.cs`·`ShopPurchase.cs`·`RewardAd.cs`·
  `PartCraft.cs`·`LootReward.cs`·`RigPartApply`·`PetSpeciesTable`까지 코드와 `Core.Tests`를
  나란히 대조해 봤는데, **정의 밖 enum 값·경계 시각·빈 컬렉션·switch의 default 분기까지
  이미 다 테스트돼 있었다.** 새로 찾은 구멍이 없다 — 지어내서 테스트를 추가하지 않았다.
- **대신 문서 어긋남 하나를 찾아 고쳤다(CLAUDE.md 7번).** `docs/decisions.md`의
  "지금 판단 대기 중인 것" 목차는 2026-09-22 19시에 만들어져 "다섯" 항목만 가리키는데,
  그 뒤 2026-09-24에 추가된 "HUD 아래 버튼 여덟 개가 안 들어간다" 절(B-02와 같은 내용)이
  목차에 없었다 — 실제로는 여섯 번째 판단 대기 항목이다. 목차에 항목을 추가하고 "다섯"을
  "여섯"으로 고쳤다. 09-25 07시 세션이 확인한 9칸 상황(`btn-planet` 추가로 57→50px)도
  같은 문단에 한 줄로 얹었다.
- 코드·테스트 변경 없음(문서만). `dotnet run` 불필요. 웹에 보이는 변화 없음 —
  이번 건은 웹에 안 보임.
- 이어서 할 것: code-only 항목은 계속 바닥이고, 이번에 확인해 보니 switch/분기 구멍 찾기도
  당분간은 바닥일 가능성이 높다(위 8개 파일을 직접 대조해 확인한 결과). 다음 세션은
  feedback.md·decisions.md 목차만 가볍게 재확인하고, 그래도 시간이 남으면 `PetArt`·
  `RaceBoxReward`·`CourseGenerator` 등 아직 이 방식으로 안 훑은 파일을 봐도 되지만, 이번
  결론(바닥)이 몇 차례 더 반복되면 CLAUDE.md 규칙 넷(설계 문서에서 새 backlog 항목을
  만든다)으로 넘어가는 게 맞다.
## 00:1x 이미지 세션 — 대기 중이 비어 0장 (0절 4번에서 끝)

KST 00:10 시작. **전제 0절 4번에서 끝났다** — `docs/design/art-requests.md`의 "대기 중"이
비어 있다. 뽑을 것이 없으니 유휴 검사·`prepare_capture.ps1`·ChatGPT 앱 조작은 하지 않았다.
**0장.** 09-23 20시부터 **열 세션 연속** 같은 결론이다.

### 전제 확인

- 0절 1번(시각) — 00:10 KST, 20:00~08:00 안이라 통과.
- 0절 4번(대기 목록) — **여기서 끝.** 문서가 시킨 확인 한 줄을 돌린 결과 `MISSING:` **30줄**.
  문서가 오탐으로 못 박아 둔 두 종류(옛 이름 `pet-t1-*`~`pet-t7-*`, 문장 속 잘린 접두사
  `arrive-`·`icon-grade-`·`planet-`·`pet-t`)를 걸러내니 **남는 줄이 하나도 없다.**
  등급 폴더의 실제 PNG는 `1-common` 16 · `2-base` 16 · `3-base` 16 · `4-base` 16 ·
  `5-legend` 20 · `6-myth` 30 · `7-transcend` 10 = **124장**.
- `grep -n "A-20\|A-15" docs/backlog.md` — 세션 시작 시점(로컬 `7d2d964`)에는 둘 다 `- [ ]`
  였다. **다만 커밋 뒤 `git pull --rebase`에서 origin의 05:2x 코딩 세션 커밋(`dc1b09b`)이
  들어왔고, 그걸로 A-20이 `- [x]`가 됐다** — `rig-tiers-sheet.png`를 곡괭이·드릴·레이저
  세 장으로 자르고 `RigArt.ResourcePath`로 업그레이드 화면에 배선한 것이다.
  **A-20이 끝났는데도 새 아트 요청은 생기지 않았다**: 세 조각 모두 `check_alpha.py`를
  통과해 `Resources/Art/Rigs/`에 자리했고(`rig-tier-pickaxe` · `rig-tier-drill` ·
  `rig-tier-laser`), 티어별로 모자란 그림이 드러나지 않았다. `art-requests.md`의 "대기 중"도
  rebase 뒤 그대로 비어 있고, 확인 한 줄을 다시 돌려도 오탐 세 줄(`arrive-`·`icon-grade-`·
  `planet-`)뿐이다. **그래서 이번 세션의 0장 판정은 rebase 뒤에도 그대로 맞다.**
- 0절 5번(앞 세션 이미지 한도) 해당 없음 — 09-25 daily에 `이미지 한도 — N시간 후 풀림` 줄 없음.
  09-23 20시 이후 어느 세션도 이미지를 한 장도 쓰지 않았다.
- 0절 2·3번(유휴·ChatGPT)은 재지 않았다 — 4번에서 끝났으므로 PC를 건드릴 이유가 없다.
- 설계 문서는 다시 훑지 않았다 — 0절 4번이 "다시 증명하지 말 것"이라고 못 박아 둔 대로다.

### 이어서 할 것

- **A-20(채굴차 시트를 잘라 티어별로 모자란 것이 드러날 때)이나 A-15(출시 준비)가 진행될
  때까지 이미지 세션은 계속 0장으로 끝난다. 고장이 아니다.**
- 이미지 한도는 온전하다 — 뽑을 것이 생기면 12장 상한을 다 쓸 수 있다.

## 01:1x 코딩 세션 — M-13은 손대지 않음(판단 대기), DeterministicRandom 방어 버그 하나 고침

- **시작**: `origin/claude/dev` 마지막 커밋이 43분 전이라 안 겹침. `feedback.md` `- [ ]` 없음.
- **M-13(오프라인 상한 연장 상품 무효과)을 실제로 고치려다 되돌렸다.** `MiningController.
  ComputeOfflineReward`가 `elapsedSeconds`에 `Entitlements.OfflineCapHours`를 전혀 안 걸어서
  상품이 12h를 계산까지 해 놓고 아무 데도 안 쓰는 걸 코드로 직접 확인했다(경과 시간이 얼마든
  `MiningSimulator.Offline`에 그대로 다 들어감 — `RefinedGained`는 무제한). 클램프까지 짜서
  `Core.Tests` 377개 통과까지 확인했는데, 커밋 직전에 `docs/decisions.md`를 다시 보니 **이미
  2026-09-20에 Tifania 판단 대기(A/B/C안)로 못 박아 둔 항목이었다** — 내가 고른 방식(정제
  광물도 상한으로 자름)이 그 A안 그대로라, 판단 없이 기존 무제한 정제 광물 감각을 깎는
  선택을 세션이 임의로 확정하는 셈이었다. **되돌렸다**(`git checkout --`, 커밋 안 함).
  M-13은 여전히 backlog `- [ ]` 그대로 둔다 — 코드는 다 짜 놨으니(A안 기준) Tifania가
  A/B/C 중 하나를 고르면 다음 세션이 몇 분 안에 끝낼 수 있다.
- **대신 실제로 안전한 버그 하나를 찾아 고쳤다.** `RaceSimulator.cs`의
  `DeterministicRandom.NextInt(min, max)`가 `min == max`(빈 범위)면 `% 0`으로
  `DivideByZeroException`을 던진다 — 기존 경계값 테스트("단일값 범위" `NextInt(7,8)`, 폭 1)가
  이 폭 0 케이스를 안 잡고 있었다. 지금 실제 호출부(`CourseGenerator`, `PetSpecies.PickInGrade`)는
  전부 폭이 0보다 커서 아직 안 터지지만, `PickInGrade`가 빈 등급 배열을 넘기면 바로 이 경로다 —
  `MakeOpponents`가 이미 음수 count를 방어하는 것과 같은 이유로 방어 코드를 추가했다
  (`maxExclusive <= minInclusive`면 `minInclusive`를 그대로 돌려줌). `Core.Tests`에 경계 테스트
  4개 추가(0,0 / 3,3 / -2,-2 / 역전된 5,1) — 376 → **377, 실패 0**.
- **backlog가 다시 바닥이었다 — 이번엔 그냥 넘어가지 않고 파고들었다.** M-13 외에
  `docs/decisions.md` "지금 판단 대기 중인 것" 여섯 항목 전부 재확인(전부 Tifania 결정 대기
  또는 저장소 밖 문제, 급하지 않음 확인됨), `docs/design/monetization.md` 2-6~2-8(시즌 패스·
  스킨·광고 제거)이 backlog에 이미 반영돼 있는지 대조(전부 M-07/M-10 등에 있음), `RaceBoxReward`·
  `CourseGenerator`·`RaceSimulator` 세 코어 파일을 전부 다시 읽고 기존 테스트와 대조(위 NextInt
  하나 빼고 더 없음), `docs/design/balance/tempo.md`(증폭기로 2시간 이후가 안 채워짐 — W2 코스
  전까지는 원래 열려 있는 문제, 코드로 더 할 게 없음), `docs/design/p0-gate.md`(2026-09-11
  D00~D03 시절 문서라 완전히 지난 얘기, 손댈 것 없음) 순으로 훑었다. **code-only 새 backlog
  항목은 이번에도 못 찾았다** — 이제 09-24 05시부터 이번(01시)까지 여러 세션 연속 같은
  결론이다.
- **이어서 할 것**: M-13은 Tifania가 A(정제도 상한 적용)/B(정제는 무제한 유지, 원석·보물만
  상한 적용 — 지금 화물칸으로 사실상 이미 막혀 있어 체감 약할 수 있음)/C(상품 재설계) 중
  하나만 골라 주면 코드는 이미 준비돼 있다(`git log`엔 안 남지만 이번 세션이 A안 구현을
  한 번 짜 봤다는 뜻 — 되돌린 diff라 다음 세션이 처음부터 다시 짜야 함, 어렵지 않음).
  code-only 탐색은 몇 차례 더 반복되면 CLAUDE.md 규칙 넷(설계 문서에서 새 backlog 항목을
  만든다) 쪽으로 완전히 넘어갈 것 — 이번 세션은 새 항목을 "만들기"보다 "찾기"에 시간을
  더 썼는데, 다음엔 만드는 쪽(예: 시즌 패스 실제 UI 화면이 아직 backlog에 없는지, 스킨
  카탈로그 데이터 구조가 아직 없는지)도 봐야 할 것 같다.
## 01:1x Unity 배선 세션 — M-05 화물칸 80% 로컬 알림을 씬에 붙였다 (에디터 세션 몫이던 것)

- **시작**: `origin/claude/dev` 마지막 커밋(00:31 이미지 세션)이 42분 전이라 안 겹친다.
  정해진 grep 표식(`씬 배선은 Unity 세션 필요`)은 `docs/backlog.md`·`docs/feedback.md`
  **양쪽 0건**이고, `Assets/Editor/`의 `Bootstrap*Ugui.cs` **16개가 전부 이미 씬에 서 있다**
  (09-25 19시 세션이 `UI Canvas/Overlays` 아래 16개를 직접 세어 확인해 둔 그대로).
  **그래서 "배선 대기"로는 할 일이 없었다.** 그대로 끝내는 대신 CLAUDE.md "미루지 않는다"
  규칙대로 **이 세션만 할 수 있는 것**을 집었다 — M-05 / D18-N 화물칸 알림 축.
  9/15부터 "Package Manager는 클라우드 세션이 건드리면 위험하니 에디터 세션 몫"으로
  열흘 넘게 밀려 있던 항목이다.

- **① 패키지를 깔았다.** `com.unity.mobile.notifications@2.5.0`을 Package Manager로 추가.
  `manifest.json`·`packages-lock.json`이 같이 갱신됐다. 이게 클라우드 세션이 못 하던 부분이다.

- **② 글루 레이어를 새로 썼다** — `Assets/Scripts/Mining/CargoNotificationScheduler.cs`(신규).
  "언제 알릴지"는 코어(`MiningSimulator.HoursUntilCargoThreshold`, 9/15에 이미 테스트 4개까지
  끝나 있던 것)가 계산하고, 이 컴포넌트는 그 값을 플랫폼 알림 API에 넘기기만 한다 —
  RewardAdTracker·DailyLoginReward와 같은 역할 분리다. 동작:
  - 백그라운드로 갈 때(`OnApplicationPause(true)`)·종료할 때 예약, 돌아오면 전부 취소.
    안 지우면 이미 비운 화물칸을 두고 알림이 온다.
  - 코어가 `null`을 주면(정제가 유입을 따라잡아 원석이 그 이상 안 쌓임) **예약하지 않는다** —
    코어 주석이 시킨 그대로다. `0`(이미 80% 넘음)도 예약 안 함.
  - 10분보다 가까우면 예약 안 함. 홈 버튼 눌렀다 바로 돌아오는 사람에게 1분 뒤 알림은 성가시다.
  - 알림 API 호출부는 `#if UNITY_ANDROID`/`UNITY_IOS`로 감쌌다. **에디터·WebGL에서는 계산까지만
    하고 콘솔에 한 줄 남긴다** — WebGL 빌드에 알림 코드가 안 들어가니 웹 빌드는 안전하고,
    Play 모드에서 값이 맞는지 눈으로 볼 수 있다.

- **③ 씬에 붙였다.** `MiningRig` 오브젝트(= `MiningController`가 붙어 있는 그것)에
  `CargoNotificationScheduler` 하나 추가. `GemRacer/7`은 **누르지 않았고**, 씬을 연 채로
  `execute_code`로만 더했다(CLAUDE.md 함정 규칙). 씬 diff는 **컴포넌트 한 개 16줄이 전부**다.

- **확인한 것**
  - `refresh_unity` 후 **컴파일 에러 0** (콘솔에 남은 건 Unity AI `NoSubscription` 하나뿐인데
    이건 이번 작업과 무관하고 세션 시작 전부터 있던 것).
  - **Play 13초 — 예외 0.** 실행 중에 실제 경로(`ScheduleCargoNotification()`)를 한 번 태워 봤다:
    `행성=quartz 원석=564.02 상한=1534.9 80%=1227.9 유입/h=383.72 정제/h=134.30 → 2.66시간`.
    손으로 검산해도 (1227.9−564.02)÷(383.72−134.30)=**2.66시간**으로 맞다.
    콘솔 로그도 그대로 찍혔다 — `[화물칸 알림] 예약함 — 2.66시간(160분) 뒤.`
  - `Core.Tests` — **통과 376 / 실패 0** (코어는 안 고쳤지만 확인은 했다).
  - 씬 저장 + `File/Save Project`까지 했다.
  - **못 한 확인 하나** — 게임 뷰 스크린샷이 이번엔 3D만 찍히고 오버레이 UI가 안 나왔다
    (`manage_camera`가 camera 지정 경로로 붙어서 Screen Space-Overlay 캔버스를 뺀 듯하다).
    이번 변경이 UI를 건드리는 게 아니라 그냥 넘어갔다. 다음 세션이 UI 화면을 찍어야 하면
    이 증상을 먼저 볼 것.

- **Unity는 닫지 않았다** — 이 세션이 켠 게 아니라 09-22부터 떠 있던 것이라 그대로 뒀다.

### 이어서 할 것

- **실제 알림이 뜨는지는 안드로이드 실기/에뮬레이터에서만 볼 수 있다.** 에디터·WebGL은
  계산까지만 하므로 dev 주소에서는 확인할 것이 없다 — **이번 건은 웹에 안 보인다.**
- D18-N이 `- [x]`가 되려면 남은 건 **화물칸 오버플로 결정 하나**(꽉 찬 채로 접속하면 잘린
  원석을 따로 보관할지). 정보구조 결정이라 세션이 임의로 못 정한다 — Tifania 몫이다.
- U-08 ②③⑤(옛 UI Toolkit 파일 삭제)는 이번에도 못 했다. 파일 삭제가 세션 실행 환경에
  막혀 있고(`Irreversible Local Destruction`), 09-24 21시 세션이 정한 대로 **우회하지 않는다.**

### 01:5x 후속 — WebGL 빌드 확인 (패키지를 깔았으니 이건 꼭 봤다)

- 알림 패키지를 새로 깔았으니 CI가 죽지 않는지가 이번 세션의 진짜 관문이었다.
  API로 확인(목록 아이콘 말고, CLAUDE.md 규칙대로): **run 508 / `08e0f47` / completed success.**
  앞 커밋 507(`e621ed3`, D26-N)도 success. **`com.unity.mobile.notifications`를 추가해도
  WebGL 빌드는 멀쩡하다** — `#if UNITY_ANDROID`/`UNITY_IOS` 가드가 의도대로 먹었다.
- dev 주소를 브라우저로 직접 열어 보지는 못했다 — 이 세션에서 PC의 Chrome 확장에 닿지 않았다
  (`Browser extension is not connected`). 이번 변경은 어차피 웹에 안 보이는 것이라 넘어갔지만,
  **패키지가 늘었으니 아침에 dev 주소가 평소처럼 뜨는지만 한 번 봐 주면 좋다**
  (https://dev.planetracer-daz.pages.dev — 게임이 뜨고 HUD 버튼이 보이면 그걸로 충분하다).
- main 승격은 하지 않았다. CLAUDE.md대로 승격은 Tifania 몫이고, 세션은 조건 확인까지만 한다.

## 02:1x 이미지 세션 — 대기 중이 비어 0장 (0절 4번에서 끝)

KST 02:09 시작. **전제 0절 4번에서 끝났다** — `docs/design/art-requests.md`의 "대기 중"이
비어 있다. 뽑을 것이 없으니 유휴 검사·`prepare_capture.ps1`·ChatGPT 앱 조작은 하지 않았다.
**0장.** 09-23 20시부터 **열한 세션 연속** 같은 결론이다.

### 전제 확인

- 0절 1번(시각) — 02:09 KST, 20:00~08:00 안이라 통과.
- 0절 4번(대기 목록) — **여기서 끝.** 문서가 시킨 확인 한 줄을 돌린 결과 `MISSING:` **30줄**로
  00:1x 세션과 한 줄도 다르지 않다. 전부 문서가 오탐으로 못 박아 둔 두 종류다
  (옛 이름 `pet-t1-*`~`pet-t7-*` 27줄, 문장 속 잘린 접두사 `arrive-`·`icon-grade-`·`planet-` 3줄).
  **남는 줄이 하나도 없다.** 등급 폴더의 실제 PNG는 `1-common` 16 · `2-base` 16 · `3-base` 16 ·
  `4-base` 16 · `5-legend` 20 · `6-myth` 30 · `7-transcend` 10 = **124장**(`.meta` 제외).
  한때 막혀 있던 `drill-sovereign`·`ember-heart`도 `7-transcend/`에 자리해 있다.
- `grep -n "A-20\|A-15" docs/backlog.md` — 둘 다 `- [ ]` 그대로(229행 A-20 채굴차 시트 자르기,
  300행 A-15 스토어용). 대기 중이 비어 있는 것이 맞다.
- 0절 5번(앞 세션 이미지 한도) 해당 없음 — 오늘 daily에 `이미지 한도 — N시간 후 풀림` 줄 없음.
- 0절 2·3번(유휴·ChatGPT)은 재지 않았다 — 4번에서 끝났으므로 PC를 건드릴 이유가 없다.
- 설계 문서는 다시 훑지 않았다 — 0절 4번이 "다시 증명하지 말 것"이라고 못 박아 둔 대로다.

### 이어서 할 것

- **A-20이나 A-15가 진행될 때까지 이미지 세션은 계속 0장으로 끝난다. 고장이 아니다.**
- 이미지 한도는 온전하다 — 뽑을 것이 생기면 12장 상한을 다 쓸 수 있다.
- 열한 세션 연속 같은 결론이니, 밤 여섯 번의 이미지 세션 중 몇 개를 A-20이 진행될 때까지
  쉬게 하거나 다른 일로 돌리는 것도 Tifania가 한번 볼 만하다(세션이 임의로 정할 일은 아니다).

### 02:1x 코딩 세션
- 한 일: 없음 — 직전 커밋(이미지 세션, 02:10)이 4분 전이라 앞 세션과 겹쳐 이번 차례는 건너뛴다. feedback.md에도 처리할 "- [ ]" 항목 없음.
- 이어서 할 것: 다음 시간 세션이 backlog 위에서부터 진행.

### 03:1x 코딩 세션
- 한 일: 없음 — 직전 커밋(03:0x Unity 배선 세션)이 10분 전이라 앞 세션과 겹쳐 이번 차례는 건너뛴다. feedback.md에도 처리할 "- [ ]" 항목 없음.
- 이어서 할 것: 다음 시간 세션이 backlog 위에서부터 진행.

### 03:0x Unity 배선 세션 — 배선 대기 중인 것 없음

- grep 표식(`씬 배선은 Unity 세션 필요`)은 `docs/backlog.md`·`docs/feedback.md` **양쪽 0건**.
  daily에 걸린 줄은 전부 앞 세션들의 자기 기록이고, `docs/design/ugui-migration.md` 히트는
  표식 자체를 설명하는 문장이다. backlog에 `- [?]` 항목도 없다(1336행 히트는 완료된 D16-N
  줄이 접힌 조각).
- 오늘 01:1x 배선 세션이 이미 이 세션 몫이던 것(M-05 화물칸 알림 패키지 + 씬 부착)을
  끝냈고, `Bootstrap*Ugui.cs` 16개도 전부 씬에 서 있다. 그래서 **Unity를 켜지 않았다.**
- 남은 것은 세션이 못 하는 종류다 — 화물칸 오버플로 정책은 Tifania 결정, U-08 ②③⑤ 파일
  삭제는 실행 환경 제한(`Irreversible Local Destruction`), 알림 실제 표시는 안드로이드 실기.
- **이번 건은 웹에 안 보임**(씬 변경 없음).

## 04:0x 이미지 세션 — 대기 중이 비어 0장 (0절 4번에서 끝)

KST 04:08 시작. **전제 0절 4번에서 끝났다** — `docs/design/art-requests.md`의 "대기 중"이
비어 있다. **0장.** 09-23 20시부터 **열두 세션 연속** 같은 결론이다.
ChatGPT 앱은 건드리지 않았다(`prepare_capture.ps1`·클립보드·클릭 전부 없음).

### 전제 확인

- 0절 1번(시각) — 04:08 KST, 20:00~08:00 안이라 통과.
- 0절 2번(유휴) — `tools/idle_check.ps1`: `IDLE=103249`(28.7시간), 300초 훨씬 넘어 통과.
  같은 줄이 `CHATGPT=RUNNING`이라 0절 3번도 통과. 둘 다 읽기만 하는 검사다.
- 0절 4번(대기 목록) — **여기서 끝.** 문서가 시킨 확인 한 줄의 결과가 `MISSING:` **30줄**인데,
  문서가 오탐으로 못 박아 둔 두 종류로 **30줄이 전부 설명된다** — 옛 이름 26줄
  (`pet-t1-*`·`pet-t2-*`·`pet-t3-*`·`pet-t4-*` 각 4 + `pet-t7-*` 10)과 문장 속 잘린 접두사 4줄
  (`arrive-`·`icon-grade-`·`pet-t`·`planet-`). **남는 줄이 하나도 없다.**
- `grep -n "A-20\|A-15" docs/backlog.md` — 둘 다 `- [ ]` 그대로(229행 A-20 채굴차 시트 자르기,
  300행 A-15 스토어용). 대기 중이 비어 있는 것이 맞다.
- 0절 5번(앞 세션 이미지 한도) 해당 없음 — 오늘·어제 daily에 `이미지 한도 — N시간 후 풀림` 줄 없음.
  09-23 20시 이후 어느 세션도 이미지를 한 장도 쓰지 않았다.
- 설계 문서는 다시 훑지 않았다 — 0절 4번이 "다시 증명하지 말 것"이라고 못 박아 둔 대로다.

### 이어서 할 것

- **A-20이 05:2x 세션에서 끝났지만 새 아트 요청은 나오지 않았다.** 그러니 이미지 세션의
  다음 방아쇠는 이제 **A-15(출시 준비 — 앱 아이콘·배너)** 하나뿐이다. 그때까지 이미지
  세션이 0장으로 끝나는 것은 고장이 아니다. `image-session.md` 0절 4번이 "A-20이나 A-15"라고
  적어 둔 두 방아쇠 중 하나가 이렇게 소진됐으니, 그 문장을 다음에 손볼 사람은 참고할 것
  (이번 세션이 문서를 고치지는 않았다 — 절차 문서는 절차대로 돌린 세션이 고치는 게 맞지만,
  A-20이 아트 부족을 안 드러낸 것은 한 번의 관찰이라 못 박기엔 이르다).
- 이미지 한도는 온전하다 — 뽑을 것이 생기면 12장 상한을 다 쓸 수 있다.
- **00:1x 세션이 올린 제안을 한 번 더 얹는다(Tifania 몫).** 열두 세션 연속 0장이니,
  밤 여섯 번의 이미지 세션 중 일부를 A-20이 끝날 때까지 쉬게 하거나 다른 일로 돌리는 것.
  세션이 스스로 정할 일이 아니라 그대로 남겨 둔다.
- **이번 건은 웹에 안 보임**(아트·코드 변경 없음, 문서만).

### 04:1x 코딩 세션 — 앞 세션과 겹쳐 건너뜀

- `origin/claude/dev` 마지막 커밋(04:08 이미지 세션, "대기 중이 비어 0장")이 2분 전이라
  0절 규칙대로 새 작업을 시작하지 않았다. `docs/feedback.md`에 `- [ ]` 줄도 없었다.
- 이어서 할 것: 없음. 다음 코딩 세션이 backlog에서 이어서 집는다.

### 05:2x 코딩 세션 — A-20 채굴차 티어 외형 끝냄

- **시작**: 마지막 커밋(04:08 이미지 세션)이 1시간 전이라 안 겹침. `docs/feedback.md`
  `- [ ]` 없음. backlog 맨 위 미완료 항목이 A-17(뽑기 재화 Tifania 결정 대기)이라 그다음인
  A-18·A-19·A-20을 봤는데, 셋 다 "화면이 없어서 못 붙인다"로 묶여 있었다. 다만 A-20은
  자세히 보니 다르다 — `rig-tiers-sheet.png`는 이미 들어와 있고(A-12), 붙일 자리도 사실
  **이미 있다**(업그레이드 화면 `tool-icon`, 지금은 고정 톱니바퀴). "새 화면 필요"가 아니라
  "그림 세 장으로 자르기 + 기존 자리에 레벨별로 바꿔 끼우기"라 code-only로 끝까지 갈 수 있는
  항목이었다. 열두 세션 넘게 "code-only 바닥났다"고 적어 온 것과 달리 이번엔 실제로 있었다.
- **자르기**: dotnet이 이 컨테이너에 없어 `apt-get install -y dotnet-sdk-8.0`부터 했다
  (CLAUDE.md에 적힌 대로, apt 저장소는 열려 있다). Pillow로 알파 채널 bbox를 계산해 세
  조각(왼쪽부터 곡괭이·드릴·레이저)을 16px 여백을 두고 잘랐다 — `rig-tier-pickaxe.png`
  (512x627) · `rig-tier-drill.png`(512x603) · `rig-tier-laser.png`(512x620), 모두
  `tools/check_alpha.py` 통과(모서리 알파 0, 투명 영역 45~51%). 세 장 다 원본 시트의 같은
  열 범위 안에서만 잘라 옆 칸 침범 없음을 좌표로 확인했다. `.meta`는 손으로 안 만들었다 —
  `ArtImportSettings.cs`가 `.meta` 없는 새 파일을 자동으로 Sprite(Rigs 폴더 maxSize 1024)로
  임포트하니, 다음에 Unity가 이 프로젝트를 열 때 저절로 맞게 들어온다.
- **코어**: `RigArt.cs`(신규, `PetArt.cs`와 같은 자리·같은 이유 — Unity 없이 규칙을 검증하려고)
  에 `ResourcePath(int toolLevel)` 추가. Models.cs의 "곡괭이→드릴→레이저, 10단계씩 티어"
  주석과 DefaultData.cs 보물 등급 경계(RequiredToolLevel 1·11·21)가 가리키는 구간
  (1~10/11~20/21~30) 그대로 썼다 — 새 경계를 만들지 않았다. 새 파일이라 `.meta`도 새로
  만들었다(GUID `e4e9f40c...`, 저장소 전체에서 중복 없음 확인). `Core.Tests`에 경계값
  4개 추가(1·10·11·20·21·30 및 범위 밖 0·음수·31·999) — **377 → 381, 실패 0.**
- **배선**: `UpgradeUgui.cs`의 `tool-icon`이 Awake에서 한 번 고정 아이콘(`icon-gear-tool`)을
  넣던 것을 없애고, `Refresh()`(매 프레임, 다른 줄과 같은 이유로 레벨업 순간을 놓치지 않으려고)
  에서 `RigArt.ResourcePath(rig.ToolLevel)`로 그때그때 맞는 티어 그림을 넣게 바꿨다.
  `UiKit.SetSpriteAtPath`가 캐시하니 디스크는 한 번만 읽는다. `BootstrapUpgradeUgui.cs`
  주석도 "tool-icon만 Awake가 아니라 Refresh에서 넣는다"로 맞춰 고쳤다. Unity 참조 코드라
  컴파일 확인은 못 했다 — **CI의 WebGL 빌드가 실제 컴파일러라 이번 푸시로 컴파일 여부가
  드러난다.** 다음 Unity 세션이 할 일: `refresh_unity`로 다시 한번 확인 + 업그레이드 화면을
  플레이 모드로 열어 곡괭이 줄 아이콘이 실제로 채굴차 그림으로 보이는지, 레벨업 시 티어가
  바뀌는지 확인. 24px 아이콘 박스에 세로로 긴 그림(preserveAspect라 가로 폭이 더 줄어든다)이
  들어가 너무 작아 안 보이면 `IconSize`를 키우거나 이 줄만 따로 키우는 것도 그 세션이 볼 것.
- `docs/backlog.md`(A-20 체크) · `docs/design/art-wiring.md`(3절, 완료 기록)도 같이 갱신.
- 이어서 할 것: 다음 Unity 세션이 위 컴파일·화면 확인. 그 전까지 A-18(행성 선택)·A-19(컷신)는
  둘 다 실제로 "화면이 없다"가 맞아서 여전히 code-only로는 못 간다 — 다음 코딩 세션은 그
  둘 대신 backlog 더 아래(P2 이후)나 Core.Tests 보강 쪽을 볼 것.
## 06:1x 이미지 세션 — 대기 중이 비어 0장 (0절 4번에서 끝)

KST 06:09 시작. **전제 0절 4번에서 끝났다** — `docs/design/art-requests.md`의 "대기 중"이
비어 있다. **0장.** 09-23 20시부터 **열세 세션 연속** 같은 결론이다.
ChatGPT 앱은 건드리지 않았다(`prepare_capture.ps1`·클립보드·클릭·캡처 전부 없음).

### 전제 확인

- 0절 1번(시각) — 06:09 KST, 20:00~08:00 안이라 통과.
- 0절 4번(대기 목록) — **여기서 끝.** 문서가 시킨 확인 한 줄의 결과가 `MISSING:` **30줄**로
  04:0x 세션과 한 줄도 다르지 않다. 문서가 오탐으로 못 박아 둔 두 종류로 30줄이 전부
  설명된다 — 옛 이름 26줄(`pet-t1-*`·`pet-t2-*`·`pet-t3-*`·`pet-t4-*` 각 4 + `pet-t7-*` 10)과
  문장 속 잘린 접두사 4줄(`arrive-`·`icon-grade-`·`pet-t`·`planet-`).
  **남는 줄이 하나도 없다.** 등급 폴더의 실제 PNG는 `1-common` 16 · `2-base` 16 ·
  `3-base` 16 · `4-base` 16 · `5-legend` 20 · `6-myth` 30 · `7-transcend` 10 = **124장**.
- `grep -n "A-20\|A-15" docs/backlog.md` — 둘 다 `- [ ]` 그대로(229행 A-20 채굴차 시트 자르기,
  300행 A-15 스토어용). 대기 중이 비어 있는 것이 맞다.
- 0절 5번(앞 세션 이미지 한도) 해당 없음 — 오늘·어제 daily에 `이미지 한도 — N시간 후 풀림`
  줄 없음. 09-23 20시 이후 어느 세션도 이미지를 한 장도 쓰지 않았다.
- 0절 2·3번(유휴·ChatGPT)은 재지 않았다 — 4번에서 끝났으므로 PC를 건드릴 이유가 없다.
- 설계 문서는 다시 훑지 않았다 — 0절 4번이 "다시 증명하지 말 것"이라고 못 박아 둔 대로다.

### 이어서 할 것

- **A-20(채굴차 시트 자르기)이나 A-15(출시 준비)가 진행될 때까지 이미지 세션은 계속 0장으로
  끝난다. 고장이 아니다.**
- 이미지 한도는 온전하다 — 뽑을 것이 생기면 12장 상한을 다 쓸 수 있다.
- **00:1x·04:0x 세션이 올린 제안을 한 번 더 얹는다(Tifania 몫).** 열세 세션 연속 0장이니,
  밤 여섯 번의 이미지 세션 중 일부를 A-20이 끝날 때까지 쉬게 하거나 다른 일로 돌리는 것.
  세션이 스스로 정할 일이 아니라 그대로 남겨 둔다. 이 밤(09-25 20시~09-26 06시)의 이미지
  세션은 이번까지 여섯 번 전부 0장이었다.
- **이번 건은 웹에 안 보임**(아트·코드 변경 없음, 문서만).

## 06:1x 코딩 세션 — 앞 세션과 겹쳐 건너뜀

- 한 일: 마지막 커밋(06:10, 이미지 세션)이 2분 전이라 06:30 정리 세션과도 겹치기 쉬운
  시간대라 새 작업 시작 안 함. `docs/feedback.md`에 `- [ ]` 없음(처리할 것 없음).
- 이어서 할 것: 다음 코딩 세션은 06:1x A-20 세션이 남긴 대로 backlog P2 이후나
  Core.Tests 보강 쪽을 볼 것.

## 07:0x Unity 배선 세션 — 배선 대기 중인 것 없음

- `origin/claude/dev` 최신 `f092d58` 까지 당겨 확인. 작업 트리 깨끗.
- 정해진 grep 표식(`씬 배선은 Unity 세션 필요`)은 `docs/backlog.md`·`docs/feedback.md` **양쪽 0건**.
  daily 쪽 hit는 전부 앞 세션들이 "0건이었다"고 적어둔 회고 문장이고, `docs/design/ugui-migration.md`
  hit는 표식 규칙 정의 줄이다. 실제 대기 항목이 아니다.
- `feedback.md`에 남아 있는 `- [?]` 두 줄(Personal 라이선스 스플래시 / HUD 액션 줄 라벨 잘림)은
  배선 대기가 아니라 판단이 막힌 항목이라 이 세션 범위가 아니다.
- **Unity 에디터는 켜지 않았다.** 할 일이 없으므로 여는 값이 없다.
- 이번 차례도 문서 한 줄만 남기고 끝낸다(씬·코드 변경 없음).

### 07:1x 코딩 세션 — 앞 세션과 겹쳐 건너뜀

- 한 일: 마지막 커밋(07:04, Unity 배선 세션)이 11분 전이라 겹칠 가능성이 높아 새 작업 시작
  안 함. `docs/feedback.md`에 `- [ ]` 없음(처리할 것 없음, `- [?]` 두 줄만 남아 있고 둘 다
  Tifania 판단 대기).
- 이어서 할 것: 다음 세션은 앞 세션이 남긴 대로 backlog P2 이후나 Core.Tests 보강 쪽을 볼 것.

## 08:1x 코딩 세션 — M-14 신설(시즌 패스 core가 11일째 아무 데도 안 불림) + XP 배선까지 끝냄

- **시작**: 마지막 커밋(07:15)이 58분 전이라 안 겹침. `feedback.md` `- [ ]` 없음. 오늘(9/26)이
  KST 기준 토요일이자 추석 연휴라 CLAUDE.md대로 주말 일과(매시간, 1~2항목)를 따랐다.
- **backlog를 또 훑기 전에 다른 방향을 봤다** — 지난 여러 세션(9/24~9/26 새벽)이 "code-only
  바닥"을 반복 확인해 온 것과 CLAUDE.md 규칙 넷("세 번 연속이면 설계 문서에서 새 항목을 만든다")에
  따라, backlog 재확인 대신 `docs/design/monetization.md` 2-6(시즌 패스)을 실제 코드와 대조했다.
  `grep -rn "SeasonPassProgress\|SeasonPassState\|SeasonPassTier" Assets/Scripts Assets/Editor`가
  **0건** — M-10(9/15)이 만든 `SeasonPass.cs`(레벨 계산·보상 수령, 순수 함수 10개 테스트 통과)를
  11일째 아무도 안 부르고 있었다. XP를 주는 곳도, 유료 트랙을 사는 SKU도, 보여주는 화면도
  전부 없어서 **셋 다 빈 상태**였다.
- **backlog에 M-14로 새로 올렸다**(위 세 조각과 "한 세션에 몰지 말고 나눠서" 순서까지 적음).
- **첫 조각(XP 배선)은 그대로 남겨 두지 않고 이어서 끝냈다** — "미루지 않는다" 규칙대로.
  core `SeasonPassRaceXp.cs`(신규, `RaceBoxReward.ForTier`와 같은 모양) — 레이스 등급→XP
  매핑(로컬15/서킷25/챌린지40/그랑프리60, 플레이스홀더). `MiningController.TryEnterRace`
  우승 분기에 `SeasonPassProgress.AddXp` → `_save.ApplySeasonPassState` 세 줄 추가(M-10이
  이미 만들어 둔 세이브 왕복 헬퍼 그대로 씀). `Core.Tests`에 3개 추가(등급별 값·정의 밖 등급
  방어·"로컬만 반복해도 28일 안에 마지막 레벨 닿는다" 대략치 확인) — 컨테이너에 dotnet이
  없어 `apt-get install -y dotnet-sdk-8.0`부터 설치(CLAUDE.md에 적힌 대로), **381 → 384,
  실패 0.**
- Unity 참조 파일(`MiningController.cs`)을 고쳤지만 컴파일 확인은 못 함(에디터 없음) — CI의
  WebGL 빌드가 실제 컴파일러다. 다음 Unity 세션이 `refresh_unity`로 확인.
- 이번 건은 웹에 안 보임(레이스 승리해도 화면에 XP가 안 뜸 — 화면은 아직 다음 조각).
- 이어서 할 것: M-14 남은 두 조각 — ① `ShopSkuId`에 시즌 패스 유료 트랙용 SKU 추가 +
  `ShopPurchase.Apply`에 `OwnsPaidTrack = true` 분기, ② 화면(다른 `Bootstrap*Ugui` 패턴으로
  티어·보상 표시 + 수령 버튼). 순서대로 SKU부터. backlog M-14 항목에 전부 적어 뒀다.

## 09:0x 코딩 세션 — M-14 두 번째 조각(SKU 추가) 끝냄

- **시작**: 마지막 커밋(08:1x, XP 배선)이 44분 전이라 안 겹침. `feedback.md` `- [ ]` 없음(`- [?]`
  두 줄만, 둘 다 Tifania 판단 대기). 08:1x 세션이 남긴 "이어서 할 것"을 그대로 집었다.
- **`ShopSkuId`에 `SeasonPassPaidTrack` 추가** — `ShopCatalog.cs`의 enum 맨 뒤에(기존 아홉 개
  뒤). 중간에 끼우면 `ShopUgui.Prefixes`가 인덱스로 맞춰 둔 나머지 줄이 한 칸씩 밀린다는 걸
  먼저 확인하고 나서 위치를 정했다. `DefaultData.ShopItems()` + `docs/design/balance/shop.csv`에
  같은 값(12,000원, monetization.md 2-6 그대로) 추가 — 기존 "CSV=DefaultData 일치" 테스트가
  그대로 이 줄도 검사한다.
- **구매 반영 경로를 따로 뺐다.** `ShopPurchase.Apply`는 `PurchaseState`만 다루는 함수인데
  `OwnsPaidTrack`은 `SeasonPassState`(완전히 다른 세이브 구조체) 필드라 그 switch에 그냥
  끼워 넣을 수가 없었다 — 억지로 넣으려 했으면 `Apply`의 반환 타입부터 바꿔야 했다. 대신
  `SeasonPass.cs`에 `SeasonPassProgress.PurchasePaidTrack(state)`(멱등, 중복 구매 무해)를
  새로 만들고, `MiningController.DebugPurchase`가 skuId로 `ShopPurchase.Apply`와 이 함수 중
  하나로 분기하게 했다 — 분기 없이 그대로 뒀으면 이 멀쩡한 SKU가 `ShopPurchase.Apply`의
  default case에서 "정의 밖 SkuId" 예외로 죽었을 것이다(실제로 이 조합을 회귀 테스트로도
  남겨 뒀다).
- `Core.Tests` 2개 추가(PurchasePaidTrack이 OwnsPaidTrack만 켜고 XP·마스크는 안 건드리는지,
  ShopPurchase.Apply가 이 SKU를 여전히 거부하는지) — 컨테이너에 dotnet이 없어
  `apt-get install -y dotnet-sdk-8.0`부터 설치(CLAUDE.md대로). **384 → 386, 실패 0.**
- `ShopUgui.cs`는 손 안 댔다 — 화면(③)은 다음 조각이라 지금은 10번째 SKU가 있어도 기존
  두 for문이 `i < Prefixes.Length`(9)로 잘라 도니 조용히 무시된다(에러 아님, 주석으로 남김).
  `MiningController.cs`는 Unity 참조 파일이라 컴파일 확인은 CI의 WebGL 빌드가 실제 컴파일러다.
- 이번 건은 웹에 안 보임(상점 화면에 아직 이 줄이 없다).
- 이어서 할 것: M-14 남은 건 화면 하나뿐 — 다른 `Bootstrap*Ugui` 패턴으로 티어·보상 표시 +
  수령 버튼(에디터 있는 세션이나, 코드까지만이라도 다음 코딩 세션이 준비). backlog M-14에
  경로 전부 적어 뒀다.

## 10:0x 코딩 세션 — M-14 세 번째 조각(화면) 코드까지 끝냄, M-14 전부 완료

- **시작**: 마지막 커밋(09:0x, SKU 추가)이 55분 전이라 안 겹침. `feedback.md` `- [ ]` 없음
  (`- [?]` 두 줄만, 둘 다 Tifania 판단 대기). 09:0x 세션이 남긴 "이어서 할 것"(화면)을 그대로 집었다.
- **보상을 실제로 적용하는 글루** — `MiningController.TryClaimSeasonPassReward(track, level,
  out reward)` 추가. `SeasonPassProgress.CanClaim/Claim`이 레벨 도달·유료 트랙 보유·중복 수령을
  방어적으로 다시 확인해 주니, 여기는 그 판정만 믿고 `reward.Kind`별로 세이브 값을 바꾸는
  글루만 맡는다(TryOpenBox가 LootBoxOpener 결과를 RigPartApply로 적용하는 것과 같은 자리).
- **코어에 두 개를 새로 추가했다.** ① `RewardAdBoost.ExtendCargoCapBoost(current, now, hours)`
  — 기존 `ExtendCargoCapDoubleHour`는 보상형 광고 전용으로 1시간 고정이라, 시즌 패스
  `CargoCapBoostHours`(레벨마다 2·3시간 등 다른 값)엔 못 썼다. 같은 "켜진 중이면 지금이 아니라
  원래 만료 시각부터 이어 붙인다" 정책을 시간만 인자로 받게 일반화했고, 기존 함수는 그대로
  뒀다(회귀 없음). ② `SaveData.OwnedCosmeticIds` + `AddCosmetic` — 스킨 보상용, 실제 꾸미기
  카탈로그가 없어 문자열 id만 도감에 채운다(PetGachaSave.AddOwnedSpecies와 같은 중복 방지 관용).
  `Core.Tests`에 6개 추가(화물칸 보상 0·음수 방어, 스킨 중복·null·빈 문자열 방어 포함) —
  컨테이너에 dotnet이 없어 `apt-get install -y dotnet-sdk-8.0`부터 설치(CLAUDE.md대로).
  **386 → 388, 실패 0.**
- **화면**: `Assets/Scripts/UI/SeasonPassUgui.cs`(신규) — ShopUgui와 같은 결로 이름으로 찾고
  Awake에서 `DefaultData.SeasonPassTiers()`로 레벨·XP·보상 텍스트를 채운 뒤, Update에서는
  버튼 상태(받기/받음/잠김/레벨 부족)와 진행도 라벨만 매 프레임 갱신한다. 유료 트랙 구매
  버튼도 이 화면에 직접 달았다 — `ShopUgui`가 아직 10번째 SKU 줄이 없어서(M-14 ② 커밋 주석)
  이 화면이 유일한 구매 경로다. + `Assets/Editor/BootstrapSeasonPassUgui.cs`(신규) —
  `GemRacer/33`(화면 세우기, BootstrapShopUgui와 같은 ScrollRect+GridLayoutGroup, 줄마다
  무료·유료 두 칸)·`GemRacer/34`(HUD 액션 줄에 "btn-seasonpass" 추가, AddShopButtonToActionRow와
  같은 자리). `MainHudUgui.seasonPassPanel` 필드 + `Wire("btn-seasonpass", ...)` 호출도 다른
  아홉 개와 같은 자리에 추가.
- **씬에는 아직 아무것도 없다** — 위 두 MenuItem이 실제로 `SeasonPass` GameObject와 HUD 버튼을
  만드는 건 Unity 세션 몫이다. 새 스크립트 두 개의 `.meta`는 이 세션이 직접 GUID를 만들어
  붙였다(A-20 RigArt.cs와 같은 이유, 저장소 전체 GUID와 중복 없음 확인). `MiningController.cs`·
  `MainHudUgui.cs`는 Unity 참조 파일이라 컴파일 확인은 CI의 WebGL 빌드 몫.
- **주의를 하나 남겼다** — action-row가 이미 열 칸이라(feedback.md의 "라벨 잘림" 판단 대기
  항목) `GemRacer/34`를 누르면 열한 칸이 된다. 새 화면 접근 경로 자체를 안 만들 수는 없어서
  버튼은 그대로 뒀지만, feedback.md 해당 항목에 이 사실을 적어 뒀다 — 판단이 나기 전까지는
  라벨이 지금보다 더 잘릴 수 있다.
- `docs/backlog.md` M-14 항목 갱신(`- [ ]` → `- [?]`, "씬 배선은 Unity 세션 필요" 성격 —
  코드 세 조각은 전부 끝났다).
- 이번 건은 웹에 안 보임(씬에 아직 없다).
- 이어서 할 것: 다음 Unity 세션이 `GemRacer/33` → `34` → `MainHudUgui.seasonPassPanel`에
  `SeasonPass` 연결 → 플레이 모드로 레벨·보상 텍스트와 받기 버튼이 실제로 되는지 확인.
  그 전까지 다음 코딩 세션은 backlog 다른 항목이나 Core.Tests 보강 쪽을 볼 것 — M-14는
  이제 code-only로 더 할 게 없다.

## 11:0x 코딩 세션 — P-06 곡괭이 레벨 물려주기, TravelTo에 배선

- **시작**: 마지막 커밋(10:0x, M-14 화면)이 49분 전이라 안 겹침. `feedback.md` `- [ ]` 없음
  (`- [?]` 두 줄만, 둘 다 Tifania 판단 대기). backlog를 다시 훑기 전에 09-24~09-26 새벽 여러
  세션이 "code-only 바닥"을 반복 확인해 온 파일들(Amplifier·Entitlements·ShopPurchase·RewardAd·
  PartCraft·LootReward·RaceBoxReward·CourseGenerator·RaceSimulator·PetArt·SeasonPass 등)을
  또 훑는 대신, P-06(행성별 곡괭이 물려주기) 항목을 다시 읽었다 — "Unity 세션 몫"으로 남아
  있던 이유가 **"P-09(행성 이동 화면)가 있어야 '행성이 바뀌는 시점'이 존재한다"** 였는데, P-09가
  2026-09-25에 이미 코드+씬 배선까지 끝나 있었다. 막혀 있던 전제가 이미 풀려 있는 걸 발견했다.
- **`MiningController.TravelTo`에 세 줄 배선.** 떠나는 행성 값을 `PlanetToolLevel.Set`으로
  창고에 적고, 도착 행성을 전에 가 본 적 있으면 `Level`로 그 값을 돌려주고 처음이면
  `CarryOverStartLevel`(40%)로 시작값을 준다 — `_run = new MiningRunState(rig, _planet)`보다
  먼저 둬서 새 레벨이 첫 틱부터 반영된다. **세이브 마이그레이션은 필요 없었다** — 첫 이동에서
  `Set`이 그 순간 떠나는 행성의 `Rig.ToolLevel`을 자연스럽게 창고에 적어 넣는다(옛 세이브가
  창고 없이 시작해도 문제없음). P-07(정제 광물 행성별 창고)은 그대로 안 건드렸다 — "이동할 때
  한 번? 정제되는 족족?"이 P-06과 달리 여전히 진짜 미정이라 임의로 정하지 않았다.
- 코어(`PlanetToolLevel.cs`)는 안 고쳤다(9/24에 이미 완성) — `Core.Tests` 새 항목 없음,
  `dotnet run` **388 통과 / 실패 0** 회귀만 확인(컨테이너에 dotnet 없어 `apt-get install -y
  dotnet-sdk-8.0`부터 설치, CLAUDE.md대로). `MiningController.cs`는 Unity 참조 파일이라
  컴파일 확인은 못 함 — **다음 Unity 세션이 `refresh_unity` + P-09 `PlanetTravel` 패널로
  실제 왕복하며 곡괭이 레벨이 떠날 때 저장되고 돌아올 때 복원되는지, 새 행성은 40%로
  줄어서 시작하는지 확인.**
- `docs/backlog.md` P-06 항목 갱신("여전히 남은 것" → "배선 끝냄", 남은 건 P-07과 컴파일 확인).
- 이번 건은 웹에 안 보임(행성 이동 자체가 아직 씬에서 확인 안 된 상태고, 곡괭이 레벨도 숫자
  변화라 눈으로 티가 안 남).
- 이어서 할 것: 다음 Unity 세션이 위 컴파일·왕복 확인. 그 전까지 다음 코딩 세션은 P-07(정제
  광물 이관 시점)을 억지로 정하지 말고, backlog 다른 항목이나 Core.Tests 보강 쪽을 볼 것.
## 12:0x 코딩 세션 — P-16 인장 유료 구매 SKU 배선

- **시작**: 마지막 커밋(11:0x, P-06)이 54분 전이라 안 겹침. `feedback.md` `- [ ]` 없음(`- [?]`
  두 줄만, 둘 다 Tifania 판단 대기). backlog·decisions.md를 다시 훑다가 P-16(초월의 인장)
  "인장 획득 경로 넷" 중 **유료 구매(1장 500원/10장 4,500원)만 실물결제 단가가 이미 정해져
  있어 결정 없이 바로 SKU로 옮길 수 있는 걸 발견** — 나머지 세 경로(티타늄 상자 드롭률/행성
  클리어/시즌 패스)는 각자의 시스템 자체가 없어 여전히 못 잇는다.
- `ShopSkuId.TranscendentSeal1`/`TranscendentSeal10`(신규, enum 맨 뒤 — `ShopUgui.Prefixes`
  인덱스가 안 밀리게) + `DefaultData.ShopItems()`·`docs/design/balance/shop.csv` 양쪽에 값
  추가. `ShopPurchase.Apply`에는 일부러 안 넣었다 — `SeasonPassPaidTrack`(M-14)과 같은 이유로
  `PurchaseState`가 아니라 `SaveData.PetGachaSave.AddSeal`을 늘리는 구매라서.
  `MiningController.DebugPurchase`에서 두 SKU를 걸러 `AddSeal(1또는10)`로 보내는 세 줄만 추가.
- `Core.Tests` 1개 추가(두 SKU 모두 `ShopPurchase.Apply`로 보내면 예외임을 고정) — 컨테이너에
  dotnet이 없어 `apt-get install -y dotnet-sdk-8.0`부터 설치(CLAUDE.md대로). **388 → 389,
  실패 0.**
- `ShopUgui.cs`는 손 안 댔다 — 기존 아홉 줄짜리 `Prefixes`엔 이미 M-14 SKU도 안 보이던
  상태라 조용히 무시된다(에러 아님). 인장 화면은 `ShopUgui`보다 `PetGachaPullUgui`(A-17,
  이미 있음) 쪽이 더 자연스러워 보여 다음 세션 몫으로 코드 주석에 제안 남김.
  `MiningController.cs`는 Unity 참조 파일이라 컴파일 확인은 CI의 WebGL 빌드 몫.
- `docs/backlog.md` P-16 항목 갱신(유료 구매 경로 완료, 남은 세 경로 상태 정리).
- 이번 건은 웹에 안 보임(화면에서 아직 이 SKU를 안 부른다).
- 이어서 할 것: 다음 Unity 세션이 컴파일 확인. 인장 구매 화면을 붙일 세션은 `PetGachaPullUgui`
  쪽에 자리 두 칸(구매 버튼)을 추가하는 걸 검토. 그 전까지 다음 코딩 세션은 P-16 남은 세 경로
  중 시즌 패스 쪽(`SeasonPassRewardKind`에 인장 항목 추가)을 볼 만하다 — 단 "레벨 몇 개가
  한 주인지"가 안 정해져 있어 그 매핑부터 정할지 말지 판단이 필요하다.

## 13:0x 코딩 세션 — P-06 물려주기 40% 검증 시뮬레이션 (code-only 바닥 재확인 후)

- **시작**: 마지막 커밋(12:0x, P-16 유료 구매)이 53분 전이라 안 겹침. `feedback.md` `- [ ]`
  없음(`- [?]` 두 줄, 둘 다 Tifania 판단 대기). backlog를 다시 훑었지만(A-17/A-18/A-19 아트 대기,
  U-08/M-13/B-02/P-08 Unity·Tifania 결정 대기, P-16 남은 세 경로도 전부 시스템 자체가 없거나
  결정 대기) code-only 새 항목은 여전히 없었다 — 대신 오늘 11시 세션이 막 배선한 P-06(곡괭이
  물려주기 40%)이 "그 40%가 실제로 적절한 값인지 한 번도 검증 안 됐다"는 걸 발견해 CLAUDE.md
  세션 프롬프트 3번(밸런스 봇 시뮬레이션)으로 다뤘다.
- `Core.Tests/PlanetTravelSim.cs`(신규) + `Program.cs`에 `sim-planet-travel` 인자 분기 추가 —
  `BalanceSim`/`BalanceSimParts`와 같은 패턴(테스트 아니고 리포트라 `dotnet run`엔 안 낌).
  이전 행성 이탈 레벨(30=완주/15=절반) → `PlanetToolLevel.CarryOverStartLevel`로 물려받는 시작
  레벨 → 도착 행성 5곳에서 `MiningSimulator.MineralsPerHour`를 레벨1 대비로 비교.
- **결과가 실제로 판단거리였다**: 완주(레벨30) 후 이동하면 물려받는 레벨12가 레벨1 대비
  ×7~8배 산출로 시작한다 — 새 행성 초반 체감이 도착 직후 거의 사라질 수 있는 크기.
  절반(레벨15)만 채우고 이동하면 ×2.5~2.7배로 훨씬 온건하다. 즉 문제는 "40%"라는 비율 자체가
  아니라 "몇 레벨에 이동하느냐"(P-08 클리어 조건, 아직 Tifania 결정 대기)가 이 배율을 좌우한다는
  것. 값은 안 고쳤다(P-08 없이는 판단 근거 부족) — `docs/design/balance/planet-travel-carryover-sim.md`에
  기록, `docs/backlog.md` P-06·P-08 양쪽에 짧게 링크해 뒀다(P-08을 정할 때 참고하도록).
- `Core.Tests` 새 테스트는 없음(리포트 파일이라 회귀 테스트 대상 아님) — 기존 389개
  `dotnet run` **통과 389 / 실패 0** 회귀만 재확인(컨테이너에 dotnet 없어 `apt-get install -y
  dotnet-sdk-8.0`부터 설치, 매번 반복).
  Unity 참조 없는 순수 C#, 새 파일 하나(테스트 러너 전용, `.meta` 불필요 — Core.Tests는 Unity
  프로젝트 밖).
- 이번 건은 웹에 안 보임(리포트 문서, 화면 변화 없음).
- 이어서 할 것: P-08(행성 클리어 조건)을 정할 세션이 위 시뮬레이션 결과를 같이 볼 것. 그 전까지
  다음 코딩 세션도 backlog는 여전히 code-only가 바닥이니(이번까지 반복 확인) 규칙 넷대로
  설계 문서를 다시 훑어 새 항목을 찾거나, Core.Tests 경계값 보강 쪽을 볼 것.

## 14:0x 코딩 세션 — P-16 인장 구매 버튼을 PetGachaPullUgui에 배선

- **시작**: `origin/claude/dev` 마지막 커밋(13:0x, P-06 물려주기 시뮬레이션)이 54분 전이라 안
  겹침. `docs/feedback.md` `- [ ]` 없음(`- [?]` 두 줄, 둘 다 Tifania 판단 대기라 못 건드림).
  backlog 위에서부터 다시 훑었지만 대부분 아트 대기·Unity 세션 몫·Tifania 결정 대기였다 —
  대신 12시 세션이 P-16(인장 유료 구매 SKU)에 코드 주석으로 남긴 "다음 세션이 `PetGachaPullUgui`
  쪽에 자리 두 칸(구매 버튼)을 검토"를 그대로 집었다.
- `Assets/Editor/BootstrapPetGachaPullUgui.cs`에 "인장 구매" 카드(`btn-buy-seal-1`/
  `btn-buy-seal-10`)를 특수 뽑기 카드 바로 아래에 추가(멱등 — 메뉴 맨 앞에서 기존 `PetGachaPull`을
  지우고 새로 세우니 재실행해도 같은 결과). `Assets/Scripts/UI/PetGachaPullUgui.cs`는
  `Awake`에서 `DefaultData.ShopItems()`로 두 SKU의 이름·가격을 읽어 버튼 라벨을 채우고
  `MiningController.DebugPurchase(skuId)`로 잇는다 — `SeasonPassUgui`의 `paidtrack-button`과
  같은 패턴(`ShopPurchase.Apply`가 아니라 `DebugPurchase`가 직접 `PetGachaSave.AddSeal`을 늘리는
  SKU라서). 목록에 SkuId가 없으면 버튼을 꺼서 조용히 무시하게 방어(`ShopUgui`의 "안 물린 버튼은
  끈다" 태도와 같음).
- Core는 안 건드림 — `Core.Tests`는 그대로 389, 실패 0(재확인 생략, 변경 파일이 전부 Unity
  참조라 Core에 영향 없음). `.meta` 새 파일 없음(기존 파일 두 개만 수정).
  Unity 참조 파일이라 컴파일 확인은 다음 Unity 세션 몫 — **`GemRacer/26. 펫 뽑기 실행 화면
  세우기 (uGUI)` 재실행 후 Play 모드에서 구매 버튼 두 개를 눌러 "초월의 인장" 개수가 실제로
  1개/10개씩 오르는지 확인.**
- `docs/backlog.md` P-16 항목 갱신(유료 구매 SKU를 화면까지 실제로 이었다는 내용 추가, 남은
  세 경로는 그대로).
- 이번 건은 웹에 안 보임(이 화면 자체가 아직 HUD에서 여는 버튼이 없다 — A-17 기존 상태 그대로).
- 이어서 할 것: 다음 Unity 세션이 위 컴파일·구매 확인. **정정**: 이 줄이 "A-17의 남은 조각
  (펫 뽑기 화면을 여는 버튼이 없다)"이라고 적었었는데 확인해 보니 틀렸다 — `MainHudUgui.cs`에
  `btn-pet-gacha`/`btn-pet-dex`가 이미 있고 `GemRacer/28`로 2026-09-21 21시 Unity 세션이 씬
  배선·확인까지 끝냈다(backlog A-17 참고). 다음 세션은 backlog 다른 항목·Core.Tests 보강 쪽을 볼 것.

## 15:0x 코딩 세션 — SaveData 라운드트립 테스트 보강 (code-only 계속 바닥이라 fallback)

- **시작**: 마지막 커밋(14:0x, 인장 구매 버튼)이 55분 전이라 안 겹침. `feedback.md` `- [ ]`
  없음(`- [?]` 두 줄, 둘 다 Tifania 판단 대기). backlog를 다시 훑었지만(A-17/A-18/A-19 아트
  대기·M-13/U-08/P-08/B-02 Unity·Tifania 결정 대기, P-06/P-07도 남은 조각이 전부 UI 흐름
  결정 대기) 오늘 하루 열두 번 넘게 반복된 것과 같은 결론 — code-only 새 항목 없음.
  위 14:0x 세션 메모에서 "A-17 여는 버튼이 없다"는 게 실제로는 틀린 줄(이미 배선·확인
  끝남)이라는 것도 이번에 발견해 위에서 바로 정정했다. 예술 대기열(`art-requests.md`)도
  09-23 20시부터 비어 있는 게 맞고 채울 것도 없음을 재확인(이미지 세션 쪽 몫이 아니라 이건
  실제로 할 게 없는 상태).
- CLAUDE.md 규칙 넷대로 설계 문서(`idle-research.md`)도 다시 봤지만 이미 decisions.md·backlog에
  전부 반영돼 있어 새로 뽑을 게 없었다 — 그래서 fallback 목록의 "Core.Tests 보강, 커버리지가
  얇은 수식부터"로 갔다.
- **찾은 얇은 지점**: `Program.cs`의 D03-M 라운드트립 테스트(직렬화→역직렬화가 값을 보존하는지,
  1834번째 통합 테스트 포함)가 전부 2026-09-14 시점 필드(Rig·RawMinerals·OwnedPartIds 등)만
  본다 — 오늘 오후까지 P-06(`ToolLevelPlanetIds/Values`)·P-07(`PlanetMineralIds/Amounts`)·
  M-14(`SeasonPassXp` 등 비트마스크 두 개)·P-16(`TranscendentSealCount`)·A-17
  (`PetGacha.OwnedSpeciesIds` — **List&lt;bool&gt; 124칸**, `EquippedSpeciesId`)이 전부
  새 필드로 추가됐는데, 이 형태가 실제로 JsonUtility(를 흉내내는 System.Text.Json)를 그대로
  통과하는지는 한 번도 확인된 적이 없었다. List&lt;bool&gt;과 비트마스크(음수·높은 비트)는
  JsonUtility에서 종종 문제가 나는 자리라 특히 비워 두면 위험한 구멍이었다.
- 테스트 두 개 추가. ① 새 필드 전부(위 병렬 리스트·마스크·PetGacha 하위 필드)를 채운 뒤
  라운드트립해도 값이 그대로인지(높은 비트 `1L&lt;&lt;40`, `-1L` 같은 음수 비트 표현, 리스트
  첫/끝 칸까지 포함). ② SaveData.cs 곳곳 주석이 반복해 온 "새 필드라 마이그레이션이 필요
  없다 — JSON에 없으면 초기화 값이 그대로 남는다"는 주장을 실제 옛 세이브 JSON 문자열
  (새 필드가 아예 없는 최소 JSON)로 역직렬화해 검증 — `PetGacha.OwnedSpeciesIds`가 실제로
  `PetSpeciesTable.All.Count`(124)만큼 기본값(false)으로 채워지는지, `EquippedSpeciesId`가
  -1로 채워지는지까지 확인했다. 지금까지는 말로만 있던 전제였다.
  컨테이너에 dotnet이 없어 `apt-get install -y dotnet-sdk-8.0`부터 설치(CLAUDE.md대로).
  **389 → 391, 실패 0.**
- 기존 파일만 고침(`Core.Tests/Program.cs`), 새 파일 없음 — `.meta` 해당 없음(Core.Tests는
  Unity 프로젝트 밖). Unity 참조 없는 순수 C#이라 `dotnet run`으로 컴파일까지 확인됨.
- 이번 건은 웹에 안 보임(테스트 파일, 화면 변화 없음).
- 이어서 할 것: code-only가 계속 바닥이면 다음 세션도 이 fallback(Core.Tests 보강)을 이어가도
  되지만, 아직 안 본 얇은 지점으로 `RewardAdBoost`/`SeasonPassProgress.Claim`의 만료 시각
  경계값(정확히 만료 순간, 자정 경계)을 다음으로 보는 걸 추천한다. 그 전에 backlog나
  feedback.md에 새 항목이 생겼는지부터 먼저 확인할 것.

## 16:0x 코딩 세션 — SeasonPassProgress 경계값 테스트 보강 (code-only 계속 바닥)

- **시작**: 마지막 커밋(15:0x, SaveData 라운드트립 보강)이 55분 전이라 안 겹침. `feedback.md`
  `- [ ]` 없음(`- [?]` 두 줄, 둘 다 Tifania 판단 대기라 그대로 둠). backlog를 다시 훑었지만
  A-17/A-18/A-19(아트 대기)·M-13/U-08/P-08/B-02(Unity·Tifania 결정 대기)·P-06/P-07(UI 흐름
  결정 대기)뿐 — 오늘 열두 번 넘게 반복된 결론과 같았다. 15:0x 세션이 남긴 "이어서 할 것"
  (`RewardAdBoost`/`SeasonPassProgress.Claim`의 만료 시각·자정 경계)을 따라가 봤는데, 실제로
  찾아보니 그 경계값들은 이미 다 있었다 — `RewardAdBoost.CargoCapMultiplier`·
  `Entitlements.Effective`의 만료 시각 정확히 일치 케이스, `RewardAdTracker.DayIndex`의 자정
  경계(음수 로컬 시각 포함)까지 전부 기존 테스트가 잡고 있었다(`SeasonPassProgress.Claim` 자체는
  시간 인자가 아예 없다 — 레벨은 XP로만 오른다). 그 제안은 이번에 확인해 보니 틀린 실마리였다.
- 대신 실제로 비어 있던 경계 두 곳을 찾아 채웠다. ① `SeasonPassProgress.CanClaim`/`Claim`이
  **마지막 유효 티어(레벨 = tiers.Length, 지금 10)** 에서 정상 동작하는지 — 기존 테스트는
  "tiers.Length+1은 범위 밖"만 확인했지, 그 바로 안쪽 경계("tiers.Length 그 자체는 정상")는
  한 번도 확인한 적이 없었다(예: 가드가 `level > tiers.Length`에서 `level >= tiers.Length`로
  잘못 바뀌면 마지막 티어를 영영 못 받게 되는데 기존 테스트로는 못 잡는 자리). ② `IsClaimed`가
  `MaxTiers`(63) 그 자체에서는 예외 없이 동작하는지 — 기존 테스트는 "MaxTiers+1은 예외"만
  확인했다. `Core.Tests/Program.cs`에 테스트 2개 추가. 컨테이너에 dotnet 없어
  `apt-get install -y dotnet-sdk-8.0`부터 설치. **391 → 393, 실패 0.**
- 기존 파일만 고침(`Core.Tests/Program.cs`), 새 파일 없음 — `.meta` 해당 없음. Unity 참조
  없는 순수 C#이라 `dotnet run`으로 컴파일까지 확인됨.
- CLAUDE.md 규칙 넷("code-only 바닥이 세 번 연속이면 테스트 보강 대신 설계 문서에서 새
  backlog 항목을 뽑는다")도 같이 따라 monetization.md를 다시 훑었다 — 대부분 이미 backlog에
  반영돼 있었고, 유일하게 빠진 건 2-7 스킨 직접 구매(₩3,300~8,800, 지금은 시즌 패스 유료
  트랙 보상으로만 `Cosmetic`이 생기고 상점 직접 구매 경로가 없다). "출시 후 추가 매출"로
  분류된 항목이라 급하지 않고 "몇 종을 얼마에 팔지"는 설계 판단이 필요해 직접 시작하진
  않고 `docs/backlog.md` 맨 아래 "다음에 할 만한 것"에 후보로만 적어 뒀다.
- 이번 건은 웹에 안 보임(테스트 파일 + backlog 문서, 화면 변화 없음).
- 이어서 할 것: **code-only 항목이 오늘 하루 열세 번 넘게 계속 바닥이다.** 다음 세션도
  당장 code-only가 없으면 Core.Tests 얇은 지점을 더 찾거나(이번에 본 SeasonPassProgress 외에
  `ShopPurchase`/`PetFusionController` 쪽 경계도 아직 안 훑어봤다), 아니면 위에 남긴 "다음에
  할 만한 것" 후보를 Tifania가 골라 줄 때까지 기다린다. backlog 맨 위부터 다시 훑는 것보다
  이 daily 파일 이어서 할 것부터 보는 게 빠를 것.

## 17:0x 코딩 세션 — ShopPurchase·PetFusionController·RewardAdBoost 훑었지만 새 구멍 없음

- **시작**: 마지막 커밋(16:0x, SeasonPassProgress 경계값)이 55분 전이라 안 겹침. `feedback.md`
  `- [ ]` 없음(`- [?]` 두 줄만, 둘 다 Tifania 판단 대기). backlog `- [ ]` 전체를 다시 grep했지만
  A-17/A-18/A-19(아트 대기)·U-08/M-13/P-08/B-02(Unity·Tifania 결정 대기)·W-09(에디터 확인 필요)·
  D19~D24(날짜 미도래)뿐 — 오늘 열네 번 넘게 반복된 것과 같은 결론. 16:0x 세션이 "이어서 할 것"에
  남긴 대로 `ShopPurchase.cs`·`PetFusionController.cs`·`RewardAdBoost.cs`(ExtendCargoCapBoost,
  10:0x 세션이 새로 만든 일반화 함수)를 코드와 `Core.Tests` 기존 테스트를 나란히 대조해 직접 훑었다.
- **결과: 새로 찾은 구멍이 없다.** `ShopPurchase.Apply`는 화물칸 확장 세 단계 전부 개별로,
  Math.Max 역행 방지, 기간제 이어붙임의 활성/만료 경계(`==` 포함), 정의 밖 SkuId, M-14·P-16
  SKU가 일부러 여기 없음을 고정하는 회귀 테스트까지 이미 다 있었다. `PetFusionController`도
  조각 부족·정확히 0개·최고 등급(Transcendent)·조각 이미 있는 위 등급에 더해지는 경우·등급
  사다리 맨 위까지 경계가 이미 채워져 있었다. `RewardAdBoost.ExtendCargoCapBoost`도 0/음수 시간
  무시, 이미 활성 중 연장 케이스가 10:0x 세션 자신이 이미 테스트 2개를 남겨 뒀다 — 지어내서
  테스트를 추가하지 않았다.
- **dotnet run 재확인** — 컨테이너에 dotnet이 없어 `apt-get install -y dotnet-sdk-8.0`부터 설치
  (CLAUDE.md대로). **393 통과 / 실패 0**, 변경 없음이니 그대로 회귀 확인만 된 것.
- GitHub Actions로 최신 빌드 확인(목록 아이콘 말고 API로, CLAUDE.md 규칙대로): run 530 /
  `37eb3c5`(16:0x 세션 커밋) / completed success. dev 배포는 정상 진행 중이다.
- 코드·문서 변경 없음(daily 기록만) — 커밋 없음.
- 이어서 할 것: **code-only 항목이 오늘 하루 열다섯 번 넘게 계속 바닥이다.** 다음 세션은
  16:0x 세션이 찾은 monetization.md 2-7(스킨 직접 구매) 후보를 Tifania가 골랐는지 feedback.md·
  decisions.md에서 먼저 확인하고, 없으면 이번 세션이 확인해 이제 완전히 훑인 파일 목록
  (Amplifier·Entitlements·ShopPurchase·RewardAd·PartCraft·LootReward·RigPartApply·
  PetSpeciesTable·RaceBoxReward·CourseGenerator·RaceSimulator·SeasonPassProgress·
  PetFusionController) 대신 아직 안 본 `PetGachaController`·`RigPartApply`(뽑기 쪽 나머지)나
  `DefaultData.cs` 자체의 카탈로그 정합성(가격·이름 중복 등)을 볼 것.

## 18:0x 코딩 세션 — DefaultData.cs 카탈로그 정합성 테스트 보강 (code-only 계속 바닥)

- **시작**: 마지막 커밋(17:0x, ShopPurchase 등 훑음)이 57분 전이라 안 겹침. `feedback.md`
  `- [ ]` 없음(`- [?]` 두 줄만, 둘 다 Tifania 판단 대기). backlog도 다시 훑었지만 아트 대기·
  Unity 세션 몫·Tifania 결정 대기뿐 — 오늘 열여섯 번 넘게 반복된 것과 같은 결론.
- 17:0x 세션이 "이어서 할 것"에 남긴 대로 `DefaultData.cs`(손으로 쓴 카탈로그) 자체의 정합성을
  봤다. 지금까지 CSV 대조 테스트(`밸런스 CSV: ...`)는 "CSV가 DefaultData와 같은가"만 봤지,
  DefaultData 자체가 내부적으로 앞뒤가 맞는지(Id 오타·중복, 코드가 조용히 assume만 하고 강제는
  안 하는 정렬 순서)는 누구도 확인한 적이 없었다.
- **찾은 구멍 두 개가 특히 실질적이었다** — 둘 다 "지금은 우연히 맞지만 다음에 고치다 깨져도
  아무 데서도 안 터지는" 자리다.
  ① `MiningController`/`RaceEntryPanel`/`RaceEntryUgui` 세 곳이 전부
  `QuartzLocalRaceRewards().Find(r => r.CourseId == course.Id)`로 보상을 찾는데, `.Find`는 못
  찾으면 예외 없이 `null`만 돌려준다 — `CourseId` 오타가 나면 레이스를 이겨도 보상이 "조용히"
  안 나가고, 그것도 실제로 플레이해서 우연히 알아채야만 드러난다.
  ② `SeasonPassProgress.LevelForXp`의 코드 주석이 "tiers는 Level 오름차순이라고 가정한다
  (DefaultData.SeasonPassTiers가 그렇게 만든다)"고 명시적으로 적어 뒀는데, 정작 그 전제 자체를
  확인하는 테스트가 없었다.
- `Core.Tests/Program.cs`에 "카탈로그 정합성" 테스트 7개 추가: 행성 Id 중복·Order 연속성,
  부품 Id(C/B/A/S 전체) 중복, 코스 Id 중복, 보물 정의 Id 중복 + 등급 오름차순에 따른 요구
  도구 레벨·환산치 단조 증가, 상점 SKU 중복 + 가격 양수, 로컬 레이스 보상 CourseId가 실제
  코스를 가리키는지(위 ①), 시즌 패스 Level 연속성 + RequiredXp 단조 증가(위 ②).
  전부 지금 데이터로는 통과 — 실제 버그를 고친 게 아니라 "지금은 맞다"를 앞으로도 지켜지도록
  잠근 것이다. 컨테이너에 dotnet이 없어 `apt-get install -y dotnet-sdk-8.0`부터 설치(CLAUDE.md대로).
  **393 → 400, 실패 0.**
- 기존 파일만 고침(`Core.Tests/Program.cs`), 새 파일 없음 — `.meta` 해당 없음. Unity 참조
  없는 순수 C#이라 `dotnet run`으로 컴파일까지 확인됨.
- 이번 건은 웹에 안 보임(테스트 파일, 화면 변화 없음).
- 이어서 할 것: 다음 세션도 code-only가 바로 없으면 `PetGachaController`의 `RigPartReward`류
  카탈로그(있다면)나, 아직 안 훑은 `MiningController`/`RaceEntryPanel`/`RaceEntryUgui`의 다른
  `.Find`/딕셔너리 조회 자리(같은 종류의 "조용히 null" 위험)를 먼저 볼 것. 그다음은 위 16:0x
  세션이 남긴 monetization.md 2-7(스킨 직접 구매) 후보가 Tifania에게 골렸는지 feedback.md·
  decisions.md 확인.

## 19:0x 코딩 세션 — 코스·부품 PlanetId 카탈로그 정합성 테스트 보강 (code-only 계속 바닥)

- **시작**: 마지막 커밋(18:0x, DefaultData 카탈로그 정합성)이 56분 전이라 안 겹침. `feedback.md`
  `- [ ]` 없음(`- [?]` 두 줄만, 둘 다 Tifania 판단 대기). backlog `- [ ]` 30개를 다시 훑었지만
  전부 아트 대기(A-17/18/19)·Unity·Tifania 결정 대기(U-08/M-05/M-13/W-09/P-06/P-07/P-08/P-11/
  P-16/P-17/B-02 등)·날짜 미도래(D18~D24)뿐 — 오늘 열아홉 번째로 같은 결론.
- 18:0x 세션이 "이어서 할 것"에 남긴 대로 `MiningController`/`RaceEntryPanel`/`RaceEntryUgui`의
  다른 `.Find` 자리를 봤다(`results.Find(r => r.Id == "player")`) — "player"는 항상 entrants
  맨 앞에 직접 넣고 시작해서 못 찾을 수가 없는 구조였다(실제 위험 없음, 새 테스트 불필요).
  `PetGachaController`/`PetSpeciesTable`도 다시 봤지만 이미 다 채워져 있었다.
- 대신 18:0x 세션이 추가한 "카탈로그 정합성" 테스트들을 보다가, `Course`/`Part` 양쪽에 있는
  `PlanetId` 필드가 실제 `DefaultData.Planets()`의 Id를 가리키는지는 아무도 확인한 적이 없다는
  걸 봤다(CourseId→코스 존재 확인은 18:0x가 이미 잠갔지만, PlanetId→행성 존재는 빠져 있었다).
  지금은 쿼츠 하나뿐이라 항상 통과하지만, 나중에 루비 등 다음 행성 코스·부품을 추가하다가
  `PlanetId = "quartz"`를 실수로 복붙해 놓고 안 고치면(예: 루비 부품에 PlanetId만 빠뜨림)
  지금 테스트로는 못 잡는 자리였다. `Core.Tests/Program.cs`의 "부품 Id" · "쿼츠 코스 Id" 두
  테스트에 PlanetId 검증을 추가. **새 테스트 케이스를 늘린 게 아니라 기존 두 테스트 안에
  단언을 추가한 것**이라 통과 개수는 그대로 **400 / 실패 0**(전부 통과, 테스트 수 불변).
  컨테이너에 dotnet 없어 `apt-get install -y dotnet-sdk-8.0`부터 설치.
- 기존 파일만 고침(`Core.Tests/Program.cs`), 새 파일 없음 — `.meta` 해당 없음.
- 이번 건은 웹에 안 보임(테스트 파일, 화면 변화 없음).
- 이어서 할 것: **code-only 항목이 오늘 하루 열아홉 번 연속 바닥이다.** 다음 세션은 먼저
  `feedback.md`/`decisions.md`에서 16:0x 세션이 남긴 monetization.md 2-7(스킨 직접 구매)
  후보가 골렸는지 확인. 안 골렸으면 `RaceSimulator.Course`/`Part` 외 다른 필드(예:
  `Part.Slot`별 카탈로그가 5종 전부 갖춰졌는지, 등급별 스탯이 단조 증가하는지)에서 아직 안 잠근
  경계가 있는지 볼 것 — 다만 이 자리들도 거의 다 훑였으니, 그다음은 `docs/decisions.md`
  "지금 판단 대기 중인 것" 목차가 실제로 최신인지(2026-09-26 이후 새로 생긴 판단 대기 항목이
  누락되지 않았는지) 대조하는 쪽이 더 남아 있을 만하다.

## 19:0x Unity 배선 세션 — M-14 시즌 패스 화면을 씬에 붙였다 (+ 눈으로만 보이는 버그 둘 수정)

- **할 일이 있었다.** 정해진 grep 표식(`씬 배선은 Unity 세션 필요`)은 `docs/backlog.md`·
  `docs/feedback.md` 양쪽에 이번에도 0건이지만(걸린 줄은 전부 지난 세션들이 "0건이었다"고
  적어 둔 daily 기록이다), 오늘 낮 08~10시 코딩 세션이 M-14(시즌 패스) 세 조각의 코드를
  끝내면서 **씬 배선을 통째로 남겨 뒀다.** 마지막 Unity 배선 세션이 07:0x("배선 대기 중인 것
  없음")라 그 뒤로 쌓인 것이다. 표식이 아니라 backlog의 `- [?]`와 daily의 "이어서 할 것"을
  보고 집었다.
- **PC의 폴더가 세션에 연결돼 있지 않아 `device_bash`는 쓰지 못했다.** 대신 Windows-MCP의
  PowerShell과 Desktop Commander로 저장소를 직접 다뤘다(같은 PC, 같은 파일이다). 다음
  세션도 같을 수 있으니 적어 둔다 — `device_bash`가 "No folders are connected"로 실패해도
  **PC에 못 닿는 것이 아니다.** Unity MCP도 그대로 붙는다.
- **Unity는 이미 켜져 있었다**(9/22부터 MainGame 씬을 연 채). 그래서 이 세션은 **닫지 않았다**
  — Tifania가 쓰던 중일 수 있다.
- **같은 19:0x에 코딩 세션도 돌았다**(`4b02162` 코스·부품 PlanetId 테스트 보강). 그쪽은
  `Core.Tests`만 건드렸고 이쪽은 씬·에디터 스크립트라 겹치는 파일이 없었다 — 이 daily 파일에서만
  충돌이 나서 두 기록을 나란히 두는 것으로 풀었다.
- **컴파일 먼저 확인했다.** `refresh_unity` 후 `scriptCompilationFailed = false`, CS 에러 0.
  M-14 ①②(`MiningController.TryEnterRace`·`DebugPurchase`), P-06(`TravelTo`), P-16(인장 SKU),
  그리고 새 파일 `SeasonPassUgui.cs`·`BootstrapSeasonPassUgui.cs`까지 **여러 코딩 세션이
  "다음 세션 몫"으로 남겨 둔 컴파일 확인이 여기서 한꺼번에 끝났다.**
- **배선**: `GemRacer/33`(화면 세우기) → `GemRacer/34`(HUD에 "시즌패스" 버튼) → `execute_code`로
  `MainHudUgui.seasonPassPanel`에 `SeasonPass` 대입 → 씬 저장. `Overlays` 아래 화면이
  **16개 → 17개**. 열한 개 `UiPanel` 칸이 전부 채워져 있는 것도 같이 확인했다(하나도 NULL 없음).
- **플레이 모드에서 열어 눈으로 봤다.** 예외 0. "시즌 패스 / 레벨 0 · 0/100 XP", 티어 열 줄의
  무료·유료 보상 텍스트, "레벨 부족"·"잠김" 버튼 상태, 닫기 버튼까지 정상이고 한글도 안 깨졌다.
- **그리고 눈으로 봐야만 보이는 버그 두 개를 찾아서 고쳤다.** 둘 다 부트스트랩 코드를 고쳤고
  (CLAUDE.md 3번 — 씬 YAML을 손으로 안 고친다), 고친 뒤 `GemRacer/33`을 다시 눌러 멱등까지
  확인했다.
  - ① **유료 트랙 구매 줄이 44픽셀이어야 하는데 356픽셀로 벌어졌다** — 구매 버튼이 세로로
    길쭉한 금색 기둥이 돼 있었다. 원인은 `paidtrack-row`의 `LayoutElement`가 `flexibleHeight`를
    안 건드려 미설정(-1)으로 뒀는데, 같은 오브젝트에 붙는 `HorizontalLayoutGroup`이
    `childForceExpandHeight = true`라 **자기 자신의 flexibleHeight를 1로 보고**하기 때문이다.
    그래서 바깥 `VerticalLayoutGroup`이 남는 세로 공간을 `scroll-view`와 이 줄에 **반씩**
    나눠 줬다(44+312, 120+312). `buyLayout.flexibleHeight = 0f` 한 줄로 고정했다 — 고친 뒤
    줄은 44픽셀, `scroll-view`가 744픽셀로 남는 공간을 전부 가져간다.
    **LayoutElement + (자식을 늘리는) LayoutGroup을 한 오브젝트에 같이 붙이는 다음 화면도
    같은 함정에 빠진다** — flexibleHeight를 명시적으로 0으로 박아 두는 편이 안전하다.
  - ② `MakeHeaderText`·`MakeButton` 헬퍼가 이미 `LayoutElement`를 붙여 주는데
    `paidtrack-state`·`paidtrack-button`에서 `AddComponent`를 또 불러 **한 오브젝트에
    LayoutElement가 둘씩** 달려 있었다. 유니티는 우선순위가 같으면 큰 값을 택하므로 명시한
    `preferredHeight = 32f`가 헬퍼의 40에 조용히 먹히고 있었다. `GetComponent`로 기존 것을
    고쳐 쓰게 바꿨다.
- **HUD 라벨 잘림은 실측해서 `feedback.md`에 적었다**(판단은 여전히 Tifania 몫이라 건드리지
  않았다). 칸 너비 **9칸 50px → 10칸 44.4px**, 잘리는 버튼이 **둘에서 셋으로** 늘었다 —
  "업그레이드"(86.4px 필요), **"레이스"(51.9px 필요, 이번에 새로 잘림)**, "시즌패스"(69.1px 필요).
  앞 기록이 걱정하던 "짧은 라벨까지 위험해진다"가 실제로 일어난 것이다. 버튼이 하나만 더
  붙으면 40.4px가 되어 두 글자 라벨도 20pt로는 못 들어간다.
- **스크린샷 찍을 때 한 가지**: `manage_camera screenshot`이 Main Camera 경로로 잡히면
  Screen Space - Overlay 캔버스가 **안 찍힌다**(UI 없이 배경만 나온다). 플레이 중에 캔버스를
  잠깐 `ScreenSpaceCamera`로 바꿔서 찍고, 플레이를 멈춰 되돌렸다(플레이 중 변경은 저장 안 됨 —
  정지 후 `ScreenSpaceOverlay`로 돌아온 것, 씬이 dirty가 아닌 것까지 확인했다).
- **오늘 웹에서 확인할 것** — https://dev.planetracer-daz.pages.dev
  HUD 아래 액션 줄 맨 끝의 **"시즌패스"** 버튼을 누르면 시즌 패스 화면이 열린다. 레벨 열 줄이
  스크롤되고, 맨 위 "유료 트랙 구매" 줄이 **가로로 납작한 한 줄**인지 봐 주시면 된다(세로로
  길쭉하면 이번 수정이 안 올라간 것이다). XP는 레이스에서 우승해야 오르므로 처음엔 레벨 0이
  정상이고, 그래서 받기 버튼은 전부 "레벨 부족"이다.
- **라벨 잘림은 결정 대기**: 액션 줄 라벨 세 개가 잘린다. `feedback.md`의 (1) 라벨 줄이기 /
  (2) 두 줄로 접기 / (3) 가로 스크롤 중에 골라 주시면 다음 세션이 바로 적용한다.
- 이어서 할 것: 다음 코딩 세션은 M-14가 닫혔으니 backlog 다른 항목으로. 다음 Unity 세션은
  배선 대기가 다시 없으면, 위 ①과 같은 `LayoutElement` + `LayoutGroup` 조합을 쓰는 다른
  `Bootstrap*Ugui`(특히 `BootstrapShopUgui`의 줄 구조)를 같은 눈으로 한 번 훑어볼 만하다.
