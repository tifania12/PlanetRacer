# 2026-09-20 (일)

## 오늘 웹에서 확인할 것 (00:1x 세션 갱신)

1. **dev·main 배포가 여전히 이틀째 안 올라간다(T-13, 9/19 23시 세션 발견).** `Assets/Resources`
   그림이 98MB라 Cloudflare 25MiB 제한에 막힌다. `docs/decisions.md` T-13의 A/B/C 중 하나만
   정해 주면 다음 세션이 바로 푼다 — 정해지기 전엔 아무 세션도 아트 파이프라인을 안 건드린다.
2. 이번 세션(P-05) 작업은 코어 수식만 고쳐서 웹에서 볼 변화는 없다.

## 00:1x 코딩 세션 — 이번 건도 웹에 안 보임(코어 전용)

- **시작할 때 확인**: `origin/claude/dev` 마지막 커밋이 46분 전이라 앞 세션과 안 겹침.
  `docs/feedback.md`엔 처리할 `- [ ]` 없음(전부 `[x]`/`[?]`). T-13은 여전히 Tifania 결정 대기라
  손 안 댔다.
- **P-05(채굴 장비 절반)를 집었다.** `docs/backlog.md`·`docs/design/amplifier.md`를 보니 P-05의
  첫 조각("SaveData에 칸별 증폭률 자리를 만들고, 상한은 안 자른다")은 `Amplifier.cs` 주석이
  이미 "상한이 정해지면 Sum을 받는 쪽(P-05)에서 자른다"고 못 박아 둔 대로라 Tifania 결정 없이도
  진행할 수 있었다 — 반면 "칸 배정(무작위/플레이어 선택)"과 "누적 상한 값"은 여전히 열린
  질문이라 이번 세션은 손대지 않았다.
- **`SaveData.Amplifiers`(`RigAmplifierSave` 신규)** — RigSlot 다섯 칸(Tool/Cargo/Engine/
  Detector/Refinery)에 float로 증폭률 합을 쌓는다. `SaveData.AddAmplifier(slot, bonus)`가
  더하는 입구. 0 이하 값은 무시(증폭기는 깎는 경우가 없다는 amplifier.md 규칙 반영).
- **`MiningSimulator`에 amp-aware 오버로드 8개** — `RigSpeed`/`YieldPerVein`/`MineralsPerHour`/
  `CargoHours`/`CargoCapacityMinerals`/`ClampToCargoCapacity`/`GemsPerHour`/`RefinePerHour`/
  `Offline` 전부 기존 시그니처는 그대로 두고 `RigAmplifierSave amp` 인자 하나를 받는 오버로드로
  추가했다(RaceFuel.Recover·MiningSimulator.Offline forceFullRefine과 같은 "나란히 추가" 패턴 —
  기존 호출부는 컴파일도 동작도 안 바뀐다). 칸→지표 매핑은 새로 정한 게 아니라
  `UpgradeCost.Cost` 주석이 이미 적어 둔 것(Tool→YieldPerVein, Cargo→CargoHours,
  Engine→RigSpeed, Refinery→RefinePerHour, Detector→GemsPerHour)을 그대로 따랐다.
  Tool 증폭은 광맥 매장량(VeinYield)을 못 넘게 그대로 클램프, Detector는 레벨 0이면 증폭해도
  여전히 0, Refinery는 비율이 아니라 최종 정제량에 곱해서 채굴 속도를 넘어서면 화물칸이 아예
  안 찰 수 있다(의도된 동작 — `Offline`의 `rate<=refineRate` 분기가 이미 다루던 경우).
- **`Core.Tests` 8개 추가, 276 → 284, 실패 0.** amp=null 회귀 없음, 칸별 독립 누적, Tool 클램프,
  Engine 증폭이 MineralsPerHour까지 정확히 전파되는지(손계산과 대조), Cargo+Tool+Engine 합성,
  Refinery가 화물칸을 비우는 경계 케이스, Detector 레벨 0 무효까지 확인. `dotnet run sim`도
  돌려 페이싱이 그대로인지 봤다(증폭기가 아직 봇 시뮬레이션엔 안 붙어서 당연히 그대로 — 회귀
  아님, amplifier.md 6단계가 다음 몫).
- **안 한 것 셋.** ① 레이싱카 다섯 칸(엔진·타이어·서스펜션·차체·부스터, `PartSlot`)은 그대로
  남았다 — `RaceSimulator.cs`를 먼저 읽어야 하는 별도 작업이라 시간 안에 못 했다.
  ② 상자에서 나온 증폭기를 실제로 어느 칸에 넣을지 고르는 자리(무작위/플레이어 선택)는 여전히
  없다 — `AddAmplifier(slot, bonus)`까지만 만들어 뒀고, 화면이 필요해 Unity 세션 몫.
  ③ 누적 상한은 amplifier.md가 여전히 안 정해서 이번에도 안 잘랐다.
  `docs/backlog.md`의 P-05 항목과 "다음에 할 만한 것"에 셋 다 다음 몫으로 적어 뒀다.
- **Core만 고쳐서 컴파일 위험 낮음** — Unity 참조 없는 순수 C#, 새 파일 없어 `.meta`도 불필요.
  그래도 Unity 세션이 한 번 `refresh_unity`로 컴파일 확인은 해 줄 것(새 오버로드가 실제
  어셈블리에 올라오는지).
- 이어서 할 것: (1) P-05 나머지 절반 — 레이싱카 다섯 칸 amp 반영(`RaceSimulator.cs`부터 읽을 것).
  (2) T-13은 여전히 Tifania 결정 대기. (3) T-13이 풀리면 main 승격 조건이 다시 성립하는지
  확인(테스트 실패 0은 계속 유지 중, 남은 건 "직전 WebGL 빌드 성공"과 fast-forward).
