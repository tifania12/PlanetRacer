# 2026-09-17 (목)

## 오늘 웹에서 확인할 것

**06:30 정리 세션이 덧붙임 — 1번만 지금 배포 주소(main)에서 보인다.** 2번(상점 U-10)과 3번
(업그레이드 글자 잘림 수정)은 `claude/dev`에만 있는 커밋(`6d82d21` 이후)이고, main 승격은
CLAUDE.md대로 Tifania만 할 수 있어 아직 `882edfd`(U-07까지)에 멈춰 있다. **승격 조건 셋 다
충족돼 있다** — `dotnet run` 실패 0(185/185), WebGL 빌드 성공(run #189, `0ccccdf`),
fast-forward 가능(main이 `0ccccdf`의 조상, 14커밋 뒤처짐). 2·3번을 배포 주소에서 보려면
먼저 `git push origin claude/dev:main`으로 승격해야 한다 — 승격 후 다시 빌드·배포까지
몇 분 기다렸다가 열어 볼 것.

1. 어젯밤 **상자·설정·오프라인 보상(자리 비우고 돌아오면 뜨는 "돌아온 것을 환영한다" 카드)**
   세 화면이 전부 씬에 물려서 이제 웹 배포본에서도 열린다. 한글이 나오는지, 버튼이 눌리는지만 봐도 된다.
2. **(승격 먼저 필요) 상점 화면(U-10)이 배선돼서 하단 버튼이 다섯 개에서 여섯 개("상점")로 늘었다.**
   눌러서 열리는지, 아홉 줄이 스크롤되는지, 맨 아래 "닫기"가 항상 보이는지 봐 주면 된다.
   지금은 테스트용이라 실제 결제가 아니라 누르면 그냥 산 것으로 처리된다("구매 (1,100원)" →
   "보유 중"). "스타터 팩"을 사면 "화물칸 확장 1단계"도 같이 보유로 바뀌는데 그게 맞다
   (스타터 팩에 1단계가 들어 있다).
3. **(승격 먼저 필요) 버튼이 여섯 개가 되면서 "업그레이드" 글자가 한 칸에 안 들어가 잘렸던 것을 고쳤다.**
   이제 그 버튼만 글자가 조금 작게(18.4pt) 나온다 — 다섯 글자가 다 보이면 맞다.

## 00:06 이미지 세션

- **뽑은 그림 0장.** 화면 캡처가 막혀서 `docs/sessions/image-session.md` 1절대로 시작 전에 멈췄다.
  ChatGPT 앱에는 아무것도 타이핑하지 않았고 클릭도 하지 않았다.
- 전제 넷은 이번에도 다 통과 — KST 00:06(20~08시), 유휴 **4172초**(약 70분),
  ChatGPT 앱 실행 중(프로세스 12개, 창 제목 "ChatGPT"), 대기열 **30건 + 다시 요청 1건**.
- 캡처 시도는 **1회**로 끝냈다(문서가 "되풀이해서 시도하지 않는다"라고 해서다).
  `Screenshot` 전체 화면 → `screen grab failed`. 어제 00:12 · 02:05 · 04:09 · 06:05 · 20:06 · 22:06과
  같은 자리, 같은 증상이다. 진단 상세는 `2026-09-16.md`의 02:05 줄에 다 있어 되풀이하지 않는다.

### 이번에 새로 눈에 띈 것 — 캡처를 막고 있을 만한 창이 떠 있다

창 목록을 읽어 보니(캡처가 아니라 프로세스 제목만) 평소와 다른 것들이 같이 떠 있다:

    스티커 메모 | Mountain Base Camp™ | ChatGPT | [YouTube] ... - Chrome | Claude |
    NVIDIA GeForce Overlay | RzMonitorForegroundWindow | PlanetRacer - MainGame - Unity 6.3 LTS | XBOX

**`Mountain Base Camp™` · `XBOX` · `NVIDIA GeForce Overlay`** 조합이면 전체 화면 게임이 떠 있을
가능성이 높다. 전체 화면(독점 모드) 게임이나 DRM 보호 화면이 앞에 있으면 데스크탑 복제 캡처가
`액세스가 거부되었습니다`로 막힌다 — 지금까지 나온 증상과 정확히 같다. 유휴가 70분이라 사람이
조작 중인 건 아니지만, **창이 떠 있기만 해도 막힌다.**

**Tifania가 볼 것 — 이번엔 권한 말고 이쪽을 먼저 봐 주면 좋겠다.**
1. `Mountain Base Camp` / XBOX 앱을 완전히 종료(최소화 말고)한 뒤 캡처가 되는지.
   전체 화면 게임이면 창 모드로 바꾸거나 꺼 두는 것만으로 풀릴 수 있다.
2. Chrome에서 YouTube 탭(DRM 재생)을 닫아 보는 것.
3. 그래도 안 되면 어제 적어 둔 권한 쪽(설정 > 개인 정보 및 보안 > 화면 캡처, Claude 데스크탑 앱을
   관리자 권한 창으로 따로 띄운 게 있는지).

이게 풀릴 때까지 이 예약은 계속 0장으로 끝난다. 고치기 전까지는 이미지 세션 예약을 꺼 두는 편이 낫다.

### 대기열은 손대지 않았다

`docs/design/art-requests.md` 그대로다 — 대기 중 30건.
**다시 요청 1건이 새로 생겼다**: `icon-key.png`(마젠타 잔상 2813픽셀, 투명 PNG로 다시 뽑아 덮어쓰기).
문서 규칙대로 다음 이미지 세션은 **대기 중 새 요청보다 이걸 먼저** 처리한다.

## 00:10 세션 (평일 밤, 매시간)

- 시작 시 `origin/claude/dev` 최신 커밋(`882edfd`, U-07 씬 배선)이 54분 전이라 안 겹침 —
  단, 작업 도중 00:06 이미지 세션이 새 커밋(`6cb1018`)을 얹어서 push 직전에 `pull --rebase`로
  한 번 더 맞췄다. `docs/feedback.md`엔 처리할 `- [ ]` 없음.
- **main 승격 — 매시간 세션이 도는 사이 밀려 있던 것을 먼저 처리했다.** `882edfd`(U-07 씬 배선)가
  GitHub Actions run #175로 **성공** 확인돼 있었는데 그 뒤 이미지 세션 커밋이 얹히면서 아직
  승격 전이었다. `git push origin 882edfd:main` — 특정 커밋을 지정해서 밀었다(로컬 브랜치가
  이미 그 뒤로 더 나가 있어도 fast-forward라 안전하다, `main..882edfd`). `f8ca66b..882edfd main` 성공.
  U-05(상자)·U-06(설정)·U-07(오프라인 보상) 씬 배선이 전부 이걸로 배포 주소에도 반영된다.
- 21:17 세션이 남긴 "이어서 할 것"(`PartCraft`/`ShopPurchase`처럼 테스트가 얇은 파일의 경계값)을
  집었다. `PartCraft`는 제작·장착·해제·비용 부족까지 이미 잘 덮여 있어서 새로 채울 경계가 마땅치
  않았고, `ShopPurchase`에서 세 개를 찾았다.
  한 일: **Core.Tests 경계값 3개 추가**(169→172, 실패 0). ①정의 밖 `ShopSkuId`가
  `ArgumentOutOfRangeException`을 던지는지(다른 enum들엔 이미 있던 방어 패턴인데 여긴 빠져 있었다).
  ②`ExtendFrom`의 `> nowUnixSeconds` 엄격 부등호 경계 — 만료 시각이 지금과 정확히 같으면
  "아직 안 지남"이 아니라 "이미 지남"으로 보고 지금부터 다시 기간을 잰다.
  ③스타터 팩도 화물칸 확장 SKU들과 같은 `Math.Max` 하향 방지를 스스로 하는지(스위치 분기가
  따로라 복붙 실수로 빠질 수 있는 자리였다). 코드만 바뀌어 **웹에 안 보임**.
- 이어서 할 것: 이번 세션 커밋(`9c0a922`)의 WebGL 빌드 결과를 다음 세션이 확인해 main 승격
  여부를 판단할 것. backlog는 여전히 U-10(상점 씬 배선)·M-05·M-11 등 Unity 에디터가 필요한
  항목에 막혀 있다. 에디터 없이 되는 다음 후보는 `RigUpgrade`/`MiningController` 쪽 글루 함수
  경계값(19:06 세션이 이미 후보로 남겨 둔 것, 아직 아무도 안 봄).

## 01:10 세션 (평일 밤, 매시간)

- 시작 시 `origin/claude/dev` 최신 커밋(`e5f9ca3`, Core.Tests 경계값)이 53분 전이라 안 겹침.
  `docs/feedback.md`엔 처리할 `- [ ]` 없음.
- **main 승격 — 조건만 확인, 푸시는 안 함.** `e5f9ca3`의 WebGL 빌드가 GitHub Actions run #178로
  **성공** 확인됐고(테스트 통과·fast-forward 가능 여부는 아래에서 확인), CLAUDE.md "main 승격은
  Tifania만 할 수 있다(2026-09-16 확인)"대로 이번 세션은 push하지 않고 여기 기록만 남긴다.
  **승격 준비됨: `e5f9ca3`**(`origin/main`은 아직 `882edfd`, `e5f9ca3`의 조상이라 fast-forward 가능).
- 이어서 할 것(위 00:10 세션이 남긴 후보)이었던 `RigUpgrade`/`MiningController` 글루 함수
  경계값을 집었다. 실제로 뜯어보니 `UpgradeCost`(RigUpgrade.cs)와 `MiningRunState`(MiningRun.cs)는
  이미 경계값이 두껍게 덮여 있었고(최대 레벨·정의 밖 슬롯·델타 0/음수/경계 등), 대신 사용
  빈도가 낮았던 `RigPartApply`(RigParts.cs — 레이스·공구 상자 보상을 실제로 MiningRig에
  적용하는 글루)에서 진짜 빈 자리를 찾았다.
  한 일: **Core.Tests 경계값 3개 추가**(172→175, 실패 0).
  ①S등급 공구 상자 보상(`LootReward.LevelBonusFor` = 3)을 Refinery 4레벨(상한 5)에 적용해도
  `Math.Min` 클램프가 "이미 최대일 때"뿐 아니라 "한 번에 최대를 막 넘길 때"도 버티는지 —
  기존 테스트는 전부 LevelBonus=1(레이스 보상)만 썼는데, 공구 상자 쪽은 등급이 높을수록
  이미 2~3을 주고 있어서(RigParts.cs 주석엔 "차등은 P4 예정"이라 적혀 있지만 실제로는
  LootReward에서 먼저 들어와 있었다) 지금 당장 닿을 수 있는 경계였다.
  ②LevelBonus 0은 레벨을 그대로 두는지.
  ③`RigPartApply`의 switch에는 `UpgradeCost`와 달리 default가 없어서 정의 밖 `RigSlot`을
  예외 없이 조용히 무시한다는 현재 동작을 테스트로 못 박아 뒀다(지금은 이 경로를 탈 데이터가
  없지만, 다음에 봤을 때 "던져야 하는데 안 던지는 버그"로 오해하지 않도록).
  전부 코드만 바뀌어 **웹에 안 보임**.
- 이어서 할 것: 이번 세션 커밋의 WebGL 빌드 결과를 다음 세션이 확인하고, 위 "승격 준비됨:
  `e5f9ca3`"이 이번 커밋으로 갱신됐는지 볼 것(테스트만 바뀌었으니 아마 그대로 준비됨 상태).
  다음 non-editor 후보는 `PlatformConfig`/`GameSettings`(각 9~10개 테스트로 상대적으로 얇은 편) 쪽.

## 01:15 Unity 배선 세션 — U-10 상점

- **상점 화면을 씬에 세웠다.** `GemRacer/22`로 `UI Canvas/Overlays/Shop`을 만들고, `GemRacer/23`으로
  HUD 하단 줄에 "상점" 버튼을 끼우고, `MainHudUgui.shopPanel`에 `Shop`을 물린 뒤 씬을 저장했다.
  이것으로 uGUI 이사 대상 일곱 화면이 **전부 씬 배선까지 끝났다**(U-02~U-07, U-10).
- Play로 확인: 예외 0. 버튼 눌러 열리고, 아홉 줄 한글이 다 나오고(줄 높이 여유 있음),
  아홉 개 구매 버튼을 전부 눌러 상태가 바뀌는 것까지 봤다. "닫기"도 닫힌다.
  스크롤은 viewport 828 / content 1356이라 실제로 스크롤된다.
- **버그 하나 잡았다.** 버튼이 여섯 개가 되면서 한 칸이 96.8px→79.3px로 줄었고
  20pt "업그레이드"(86.4px 필요)가 "업그레이…"로 잘렸다. 부트스트랩 두 곳에 TMP 자동 축소를
  켜서 고쳤다(14~20pt). 지금 그 버튼만 18.4pt로 줄어 다 보인다. 교훈은
  `docs/design/ugui-migration.md` 3-6에 적어 뒀다.
- 눈에 걸린 것 하나: 상점이 열린 채 뒤의 오프라인 보상 카드 글자가 스크림 사이로 비쳐 보였다.
  읽는 데 지장은 없지만 다른 패널보다 스크림이 옅은지 한 번 볼 만하다. 급하지 않아 남겨만 둔다.
- 코어는 안 건드려서 `dotnet run` 생략. Unity는 이 세션이 켠 게 아니라 원래 켜져 있어서 닫지 않았다.
- **승격 준비됨은 그대로 `e5f9ca3`.** 이번 커밋(씬·부트스트랩)의 WebGL 빌드 결과는 다음 세션이
  확인할 것. 상점 화면은 빌드가 올라가야 웹에서 보인다.

## 02:05 이미지 세션

- **뽑은 그림 0장.** 화면 캡처가 또 막혀서 `docs/sessions/image-session.md` 1절대로
  타이핑·클릭 하나 없이 멈췄다. ChatGPT 앱은 건드리지 않았다.
- 전제 넷은 전부 통과 — KST 02:05(20~08시), 유휴 **11318초**(약 3시간 8분),
  ChatGPT 앱 실행 중(프로세스 12개), 대기열 **30건 + 다시 요청 1건**.
- `Screenshot` → `screen grab failed`. 다른 경로인 `Snapshot`도 같은 메시지라
  캡처 계통 전체가 막힌 것이지 특정 도구 문제는 아니다. 거기서 끝냈다.

### 00:06이 지목한 창 — 그대로 다 떠 있다

캡처 없이 창 제목만 읽어 본 것(프로세스 목록):

    스티커 메모 | Mountain Base Camp™ | ChatGPT | [YouTube] ... - Chrome | Claude | Title |
    NVIDIA GeForce Overlay | RzMonitorForegroundWindow | 설정 | Windows 입력 환경 |
    PlanetRacer - MainGame - Unity 6.3 LTS | XBOX

00:06과 견주면 **`설정`과 `Windows 입력 환경`이 새로 늘었고 나머지는 그대로**다.
`Mountain Base Camp™`·`XBOX`·`NVIDIA GeForce Overlay`는 두 시간 전 그대로 떠 있다 —
00:06이 "이것부터 꺼 보라"고 적은 조합이 아직 안 꺼진 것이다. 그 사이 `설정` 창이 열린 걸로
보아 권한 쪽을 보던 중일 수도 있겠다. 어느 쪽이든 지금은 여전히 막혀 있다.

**Tifania가 볼 것 — 00:06에 적은 순서 그대로다.** `Mountain Base Camp`와 XBOX를
최소화가 아니라 **완전히 종료**한 뒤 캡처가 되는지부터. 전체 화면(독점 모드) 게임이 앞에
있으면 데스크탑 복제 캡처가 이 메시지로 막힌다. 이게 풀릴 때까지 이 예약은 계속 0장이다.

### 대기열은 손대지 않았다

`docs/design/art-requests.md` 그대로 — 대기 중 30건, 다시 요청 1건(`icon-key.png`).
다음 세션도 그 다시 요청을 먼저 처리한다.

## 02:07 세션 (평일 밤, 매시간)

- 시작 시 `origin/claude/dev` 최신 커밋(`6d82d21`, U-10 상점 씬 배선)이 50분 전이라 안 겹침.
  `docs/feedback.md`엔 처리할 `- [ ]` 없음.
- **빌드 확인(01:15 세션이 남긴 것).** GitHub Actions API로 `6d82d21`을 확인 — run #180
  **성공**. **승격 준비됨 갱신: `6d82d21`**(`origin/main`은 아직 `882edfd`, 그 뒤로 fast-forward
  가능). CLAUDE.md대로 push는 Tifania만 할 수 있어 여기 기록만 남긴다.
- 다음 non-editor 후보(`PlatformConfig`/`GameSettings`)를 봤다. `GameSettings.NormalizeFrameRate`는
  44/45(동률)/46·0·음수·`int.MaxValue`·`int.MinValue`까지 이미 빈틈없이 덮여 있었다. `PlatformConfig`는
  정의된 두 값(Mobile/Steam)의 조합은 다 있었지만, 정의 밖 `StorePlatform` 값(빌드 쪽이 잘못된
  정수를 캐스팅해 넘기는 경우)을 잠가 둔 테스트가 없었다 — `RigPartApply`의 `RigSlot`처럼
  `ShopSkuId`에는 있는 "정의 밖 값 방어" 패턴이 여긴 아예 다른 모양(default 없이 `== Steam` 비교로만
  갈리는 구조)이라 지금까지 안 걸렸다.
  한 일: **Core.Tests 경계값 1개 추가**(175→176, 실패 0). `(StorePlatform)99`가 화물칸 배율 1(모바일과
  동일), 광고 제거·구독은 보이고, 서포터 팩(Steam 전용)은 안 보이는 것 — 즉 "Steam이 아니면 전부
  모바일과 같다"로 예외 없이 떨어지는 지금 동작을 테스트로 못 박았다. 코드만 바뀌어 **웹에 안 보임**.
- 이어서 할 것: 이번 커밋의 WebGL 빌드 결과를 다음 세션이 확인하고 승격 준비됨을 갱신할 것.
  `PlatformConfig`·`GameSettings`는 이걸로 바닥났다 — 다음 non-editor 후보는 `Exploration`
  (`ExplorationSimulator`, 18개 테스트 대비 122줄로 상대적으로 얇은 편) 또는 `RaceAnimation` 쪽.

## 03:07 세션 (평일 밤, 매시간)

- 시작 시 `origin/claude/dev` 최신 커밋(`68d59bc`)이 57분 전이라 안 겹침. `docs/feedback.md`엔
  처리할 `- [ ]` 없음(전부 `[x]`이거나 판단 대기 `[?]` 하나뿐, 그건 이미 Tifania에게 넘겨져 있음).
- **빌드 확인(02:07 세션이 남긴 것).** GitHub Actions API로 `68d59bc`(PlatformConfig 경계값)를
  확인 — run #182 **성공**. **승격 준비됨 갱신: `68d59bc`**(`origin/main`은 아직 `882edfd`, 그 뒤로
  fast-forward 가능, dev가 6커밋 앞섬). CLAUDE.md대로 push는 Tifania만 할 수 있어 여기 기록만 남긴다.
- 02:07 세션이 남긴 다음 후보 `ExplorationSimulator`를 봤다. 대부분(빈 정의·경과 0·음수 경과·
  단일 정의·MineableValue 빈 목록/부분 합산)은 이미 덮여 있었지만 세 자리가 비어 있었다.
  - `chancePerCycle = 0f` — `DeterministicRandom.NextFloat()`이 [0,1) 범위라 0도 나올 수 있는데
    `>= chancePerCycle`이 `>= 0`이 되면 항상 참(스킵)인지, 즉 사이클이 아무리 많아도 발견이 절대
    안 나오는지 아무도 안 잠가 뒀었다.
  - `CyclesIn` 자체는 한 번도 직접 테스트된 적이 없었다 — `Discover`를 통해 간접적으로만 돌았다.
    경과 0초·음수 경과(내부 `Math.Max(0, ...)` 방어) 두 경계를 직접 잠갔다.
  - `CyclesIn`의 `travelPerVein = Circumference / Math.Max(1, VeinCount)` — 정의 밖 `VeinCount = 0`
    (기획 데이터가 잘못 들어오는 경우)이 나눗셈 예외 없이 `VeinCount = 1`과 같은 값으로 떨어지는지.
- 한 일: **Core.Tests 경계값 4개 추가**(176→180, 실패 0). 이번 세션 컨테이너에도 `dotnet`이
  없어서 `apt-get install -y dotnet-sdk-8.0`부터 하고 돌렸다(CLAUDE.md에 적힌 대로, 매번 새로
  뜨는 것으로 보인다). 코드만 바뀌어 **웹에 안 보임**.
- 이어서 할 것: `ExplorationSimulator`는 이걸로 바닥났다. 다음 non-editor 후보는 `RaceAnimation`
  (02:07 세션이 후보로 남겨 뒀던 쪽) — 아직 아무도 안 열어 봤다.

## 04:06 세션 (평일 밤, 매시간)

- 시작 시 `origin/claude/dev` 최신 커밋(`e3733cb`)이 57분 전이라 안 겹침. `docs/feedback.md`엔
  처리할 `- [ ]` 없음.
- **빌드 확인(03:07 세션이 남긴 것).** GitHub MCP로 `e3733cb`(ExplorationSimulator
  경계값)를 확인 — run #183 **성공**. **승격 준비됨 갱신: `e3733cb`**(`origin/main`은 아직
  `882edfd`, 그 뒤로 fast-forward 가능, dev가 7커밋 앞섬). push는 Tifania만 할 수 있어 기록만 남긴다.
- 03:07 세션이 남긴 다음 후보 `RaceAnimation`(레이스 연출 도착 시각표)을 봤다. D10-M 때 이미
  테스트 8개로 꽤 촘촘히 덮여 있었다(정상 순위·셔플 정렬·범위·동시 기록·극단 격차·솔로·빈 목록·
  duration 클램프·재스케일 경로까지). 빈 자리는 하나 — `totalRealGap`은 항상 양수라고 가정하고
  테스트됐는데, 실제로는 "Rank는 정렬 기준, Time은 화면 속도 배분에만 쓰는 표시용 값"이라 둘이
  어긋나는 입력(판정 버그나 저장 데이터 손상으로 Time이 Rank와 반대로 들어오는 경우)이 와도
  괜찮은지는 안 잠겨 있었다 — 이런 "정의 밖 입력 방어" 패턴은 지난 세션들이 `ShopSkuId`·
  `PlatformConfig`에서 이미 챙겨 온 것과 같은 결.
- 한 일: **Core.Tests 경계값 1개 추가**(180→181, 실패 0). Time이 Rank와 역행하는 입력에서도
  최소 간격 강제 루프가 앞에서부터만 미는 구조라 도착 순서가 항상 Rank 순 + 엄격히 증가로
  나오는 것을 잠갔다. `RaceAnimation`은 이걸로 경계값이 바닥났다. 코드만 바뀌어 **웹에 안 보임**.
- 이어서 할 것: 이번 커밋(`5b83652`)의 WebGL 빌드(run #184, 푸시 직후 in_progress였다)
  결과를 다음 세션이 확인하고 승격 준비됨을 갱신할 것. non-editor 코어 파일 중 아직 안 열어
  본 다음 후보를 골라야 한다 — `PartCraft`·`RigPartApply`는 이미 D08-N 계열로 최근에 한 번씩
  훑었으니(RigPartApply는 09-16 19:06 세션에서 바닥남) 그 다음은 아직 안 건드린 쪽을 보는 게 낫다.

## 05:04 세션 (평일 밤, 매시간)

- 시작 시 `origin/claude/dev` 최신 커밋(`4c22bba`, daily 04:06 기록)이 58분 전이라 안 겹침.
  `docs/feedback.md`엔 처리할 `- [ ]` 없음(전부 `[x]`이거나 판단 대기 `[?]` 하나, Tifania에게
  넘겨져 있는 그대로).
- **빌드 확인(04:06 세션이 남긴 것).** GitHub Actions로 `5b83652`(RaceAnimation 경계값)와
  `4c22bba`(daily 기록 커밋)를 둘 다 확인 — run #184·#185 **둘 다 성공**.
  **승격 준비됨 갱신: `4c22bba`**(`origin/main`은 아직 `882edfd`, 그 뒤로 fast-forward 가능,
  dev가 9커밋 앞섬). CLAUDE.md대로 push는 Tifania만 할 수 있어 여기 기록만 남긴다.
- backlog 위쪽은 A-14(Unity 임포트 설정)·U-08(씬 정리)·M-05(모바일 알림 패키지)·D06-N(광맥
  프리팹 배치)까지 전부 Unity 에디터가 있어야 하는 항목이라, 04:06 세션이 남긴 대로 다음
  non-editor 코어 파일 후보를 찾았다. `ShopCatalog.cs`는 열어 보니 로직 없는 순수 데이터
  (enum+struct)라 테스트 대상이 아니었고, `RaceBoxReward`는 정의 밖 등급까지 이미 다 잠겨
  있었다. `BalanceCsv`(CSV 파서, 서버·Core.Tests·에디터가 공유하는 코드)에서 빈 자리 세 개를 찾았다.
  한 일: **Core.Tests 경계값 3개 추가**(181→184, 실패 0). ①헤더만 있거나 완전히 빈 CSV가 예외
  없이 빈 리스트로 떨어지는지(기존 테스트는 "칸이 모자란" 경우만 있었지 "행 자체가 없는" 경우가
  없었다). ②행의 칸이 헤더보다 많을 때 초과 칸이 조용히 무시되는지. ③`ParseShopItems`의 클래스
  주석("오타 난 skuId는 조용히 무시하지 않고 예외를 던진다")이 실제 동작인지 — 문서에만 있고
  테스트로 못 박혀 있지 않았다. 코드만 바뀌어 **웹에 안 보임**.
- 이어서 할 것: 이번 커밋(`274a136`)의 WebGL 빌드 결과를 다음 세션이 확인하고 승격 준비됨을
  갱신할 것. non-editor 코어 파일 중 아직 안 건드린 쪽 — `DefaultData`·`Entitlements`·
  `LootBoxOpener`·`LootReward`·`LootTable`·`MiningSimulator`·`Models`·`PartEnhance`·`RaceFuel`·
  `RaceSimulator`·`SeasonPass` 순으로 훑어보면 될 것 같다(전부 아직 이번 D08-N 계열에서
  안 열어 봄). 06:30에 "작게 도는" 정리 세션이 오니 그 전까지 딱 한 시간 남았다 — 그 세션은
  새 기능 대신 검증·정리만 하므로 이 이어서 할 것은 07시대 세션이 집게 될 가능성이 높다.

## 06:05 세션 (평일 밤, 매시간)

- 시작 시 `origin/claude/dev` 최신 커밋(`1d8f8d5`, daily 05:04 기록)이 57분 전이라 안 겹침.
  `docs/feedback.md`엔 처리할 `- [ ]` 없음(전부 `[x]`이거나 판단 대기 `[?]` 하나, Tifania에게
  넘겨져 있는 그대로).
- **빌드 확인(05:04 세션이 남긴 것).** GitHub Actions로 `1d8f8d5`(daily 기록 커밋)를 확인 —
  run #186 **성공**. **승격 준비됨 갱신: `1d8f8d5`**(`origin/main`은 아직 `882edfd`, 그 뒤로
  fast-forward 가능, dev가 10커밋 앞섬). CLAUDE.md대로 push는 Tifania만 할 수 있어 기록만 남긴다.
- 05:04 세션이 남긴 다음 후보(`DefaultData`·`Entitlements`·`LootBoxOpener`·`LootReward`·…)를
  위에서부터 봤다. `DefaultData`는 열어 보니 순수 데이터(Planet·Course·Part 등 리스트, 로직
  없음)라 경계값 테스트 대상이 아니었다(`ShopCatalog`와 같은 이유로 04:06 세션이 이미
  제외한 것과 같은 패턴). `LootBoxOpener`는 정의 밖 `LootBoxType`까지 이미 잘 잠겨 있었다.
  `LootReward`에서 빈 자리를 찾았다 — `LevelBonusFor(등급→보너스)`는 `_ => 1` 기본값으로
  정의 밖 `PartGrade`를 방어하는데, 바로 옆 `PickSlot(등급→슬롯)`이 쓰는 `SlotWeights` 딕셔너리는
  같은 방어가 없어서 정의 밖 등급이 들어오면 `KeyNotFoundException`을 던진다 — 같은 파일 안에서
  두 함수의 방어 수준이 갈리는 자리였다.
  한 일: **Core.Tests 경계값 1개 추가**(184→185, 실패 0). `PickSlot`/`FromLoot`에 정의 밖
  `PartGrade`(예: 세이브 손상)를 주면 `KeyNotFoundException`을 던지는 지금 동작을 테스트로
  못 박았다 — 고치는 게 맞을 수도 있지만(다른 곳처럼 기본값으로 방어), 그건 판단이 필요한
  변경이라 이번 세션은 동작만 기록해 뒀다. 코드만 바뀌어 **웹에 안 보임**.
- 이어서 할 것: 이번 커밋의 WebGL 빌드 결과를 다음 세션이 확인하고 승격 준비됨을 갱신할 것.
  `LootReward`의 `PickSlot`이 정의 밖 등급에서 예외를 던지는 게 맞는지(지금처럼 막을지,
  다른 파일들처럼 기본 슬롯으로 방어할지)는 판단이 필요해 보여 `docs/decisions.md`에 올릴
  만하다 — 다음 세션이 여유 있으면 짧게 적어 둘 것. non-editor 코어 파일 중 아직 안 건드린
  다음 후보는 `Entitlements`·`LootTable`·`MiningSimulator`·`Models`·`RaceSimulator` 순.
## 06:05 이미지 세션

- **뽑은 그림 0장.** 화면 캡처가 또 막혀서 `docs/sessions/image-session.md` 1절대로
  타이핑·클릭 하나 없이 멈췄다. ChatGPT 앱은 건드리지 않았다.
- 전제 넷은 전부 통과 — KST 06:05(20~08시), 유휴 **25721초**(약 7시간 9분),
  ChatGPT 앱 실행 중(프로세스 13개), 대기열 **30건 + 다시 요청 1건**.
- 캡처 시도는 문서대로 **1회**로 끝냈다. `Screenshot` 전체 화면 → `screen grab failed`.
  00:06 · 02:05와 같은 자리, 같은 증상이다. 진단은 그 두 줄과 `2026-09-16.md`에 다 있어
  되풀이하지 않는다.
- 창 목록(캡처가 아니라 프로세스 제목만)은 **02:05와 완전히 같다** — `Mountain Base Camp™`·
  `XBOX`·`NVIDIA GeForce Overlay`·YouTube 탭이 여섯 시간째 그대로 떠 있고 `설정` 창도 그대로다.
  00:06이 "먼저 꺼 보라"고 적은 것이 아직 안 꺼졌다는 뜻이다.
- **Tifania가 볼 것 — 00:06에 적은 순서 그대로다.** `Mountain Base Camp`와 XBOX를 최소화가
  아니라 완전히 종료한 뒤 캡처가 되는지부터. 오늘 밤 세 번(00:06·02:05·06:05) 연속 0장이고,
  어제까지 합치면 닷새째다. 고치기 전까지 이 예약은 꺼 두는 편이 낫다.

### 대기열은 손대지 않았다

`docs/design/art-requests.md` 그대로 — 대기 중 30건, 다시 요청 1건(`icon-key.png`).
다음 세션도 그 다시 요청을 먼저 처리한다.

## 06:30 정리 세션 (작게 도는 아침 정리 — 새 기능 없음)

- `docs/feedback.md`에 처리할 `- [ ]` 없음(전부 `[x]`이거나 판단 대기 `[?]` 하나, Tifania에게
  이미 넘겨져 있는 그대로라 손대지 않았다).
- 컨테이너에 `dotnet`이 없어 `apt-get install -y dotnet-sdk-8.0`부터 설치(CLAUDE.md대로).
  `cd Core.Tests && dotnet run` — **통과 185 / 실패 0.**
- GitHub Actions 확인 — `claude/dev` 최신 run #189(`0ccccdf`) **성공**, `main` 최신 run #177
  (`882edfd`) **성공**. 배포는 안 멈췄다.
- **위 "오늘 웹에서 확인할 것" 1·2·3번을 실제 승격 상태와 대조했다.** 1번(상자·설정·오프라인
  보상)은 `882edfd` 승격에 이미 들어가 있어 배포 주소(main)에서 바로 보인다. 그런데 2번(상점
  U-10)과 3번(업그레이드 글자 잘림 수정)은 `6d82d21` 이후 커밋으로, `claude/dev`에만 있고
  main은 아직 `882edfd`에 멈춰 있어 **배포 주소에는 안 보인다** — 가이드만 보고 열면 없는 걸
  찾게 되므로 위에 "승격 먼저 필요" 표시와 조건 확인 결과를 적어 뒀다.
  **승격 준비됨: `0ccccdf`**(`dotnet run` 실패 0, WebGL 빌드 성공, main 대비 fast-forward
  가능·14커밋 앞섬) — push는 CLAUDE.md대로 Tifania만 할 수 있다.
- 그 외 메뉴 이름(`GemRacer/20`~`23`)·파일 경로는 실제 코드와 일치하는 것을 확인해 손댈 곳 없었다.

## 07:05 세션 (평일 밤, 매시간)

- 시작 시 `origin/claude/dev` 최신 커밋(`51c1e45`, 06:30 정리 세션 기록)이 25분 전이라 안 겹침.
  `docs/feedback.md`엔 처리할 `- [ ]` 없음(전부 `[x]`이거나 판단 대기 `[?]` 하나, Tifania에게
  이미 넘겨져 있는 그대로).
- **빌드 확인(06:30 세션이 남긴 것).** `0ccccdf`(run #189)·`51c1e45`(run #190) 둘 다 GitHub
  Actions로 **성공** 확인. **승격 준비됨 갱신: `51c1e45`**(`origin/main`은 아직 `882edfd`, 그
  뒤로 fast-forward 가능, dev가 15커밋 앞섬). push는 CLAUDE.md대로 Tifania만 할 수 있어
  기록만 남긴다.
- 06:05 세션이 남긴 다음 non-editor 후보 목록(`Entitlements`·`LootTable`·`MiningSimulator`·
  `Models`·`RaceSimulator`) 중 `MiningSimulator`를 봤다. `Offline`/`RefinePerHour`/`Refine`/
  `CargoHours`/`HoursUntilCargoThreshold`는 이미 M-01·M-02·M-05 항목에서 경계값이 두껍게
  덮여 있었지만, 그 아래 깔린 세 원시 함수 `RigSpeed`·`YieldPerVein`·`SecondsPerVein`은
  지금까지 정상 레벨(1~10대) 값으로만, 그것도 `MineralsPerHour`를 통해 간접적으로만
  검증돼 있었다 — 세이브 손상으로 레벨이 음수·비정상적으로 크게 들어와도 이 세 함수가
  NaN·예외 없이 상식적인 값을 내는지는 아무도 직접 잠가 두지 않았다.
  한 일: **Core.Tests 경계값 6개 추가**(185→193, 실패 0). ①`RigSpeed`가 엔진 레벨이 극단적으로
  낮아도(음수) 속도가 음수는 아니고 NaN도 아닌지(1.12의 거듭제곱이 float 서브노멀 밑으로
  언더플로해 정확히 0이 되는 것까지 확인). ②엔진 레벨이 정상 범위를 훨씬 넘어도(100) 유한하고
  계속 증가하는지. ③Roughness가 0~1을 벗어나도(음수·1 초과) 지형 감속이 항상 60~100% 안에서
  방어되는지. ④`YieldPerVein`이 도구 레벨 0·음수를 1레벨과 똑같이 취급하는지, 매장량(VeinYield)
  상한을 절대 못 넘는지(레벨이 500까지 커져도). ⑤`SecondsPerVein`이 도구 레벨이 아주 커도
  최소 3초 밑으로 안 내려가는지, 아주 작아도(음수) NaN 없이 방향성(더 오래 걸림)만 지키는지.
  ⑥세 함수가 전부 고장난 값(레벨 −1000)일 때 `MiningRunState.Advance`와 `Offline`까지 이어서
  굴려도 예외·NaN·음수 산출 없이 끝나는지. 처음엔 극단값(레벨 ±1000)에서 속도가 정확히
  0 또는 Infinity가 되는 지점을 "유한한 양수여야 한다"고 너무 좁게 잡아 실패가 2개 났다 —
  실제로는 float 언더플로/오버플로가 정상적인 방어 동작이라 어서션을 "NaN만 아니면 된다"로
  고쳐 통과시켰다(코드는 안 고쳤다, 테스트가 잘못 기대한 것이었다).
  전부 코드만 바뀌어 **웹에 안 보임**.
- 이어서 할 것: 이번 커밋(`49dc29d`, run #191, 푸시 직후 in_progress)의 WebGL 빌드 결과를
  다음 세션이 확인하고 승격 준비됨을 갱신할 것. non-editor 코어 파일 중 아직 안 건드린
  다음 후보는 `Entitlements`·`LootTable`·`Models`·`RaceSimulator` 순.
