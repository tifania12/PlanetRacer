# 2026-09-14 (월)

## 06:30 세션이 고친 것 — 위 가이드가 지금 웹에서 안 맞는다

**"상자"/"설정" 항목은 오늘 폰으로 열어 봐도 안 보인다.** `Assets/Scenes/MainGame.unity`가 아직
한 번도 만들어진 적이 없다 — `BootstrapMainGame.cs`(`GemRacer/7`)는 Unity 에디터가 있어야 실행되는
부트스트랩 코드일 뿐인데, D09-N부터 지금까지 클라우드 세션만 돌아서 이 메뉴를 실제로 누른 적이
없다. `PlanetRacer/ProjectSettings/EditorBuildSettings.asset`을 직접 확인해도 등록된 씬은
`RaceCameraSpike.unity`·`TestPlanet.unity` 둘뿐이고, 배포 시작 씬(0번)은 `RaceCameraSpike`다.
즉 지금 배포 주소를 열면 D08~D14-N에서 쌓은 HUD·제작·레이스·상자·튜토리얼·설정 화면 전부
안 보이고 예전 레이스 카메라 실험 씬만 보인다. **결정 필요**: PC에서 Unity를 열 수 있을 때
`GemRacer > 7. 메인 게임 씬 만들기`를 한 번 눌러 `MainGame.unity`를 생성·커밋해야 한다 — 클라우드
세션은 에디터가 없어 이 한 단계를 대신할 수 없다.

## 오늘 웹에서 확인할 것 (위 사정으로 사실상 보류)

1. 컴파일 에러부터 확인 — 밤새 여러 세션이 에디터 없이 커밋을 쌓았다(개봉 화면, 강화, 튜토리얼,
   사운드 배선, 설정 화면 등). *(다만 지금 배포된 화면은 이 코드를 안 쓰므로, 컴파일 확인은
   Unity를 열어야 진짜로 된다 — CI가 성공했다는 건 문법이 맞다는 뜻일 뿐, 씬에 실제로 얹어 보진
   못했다.)*
2. ~~"상자" 버튼 → 녹슨 상자 열기 → 결과 카드~~ — `MainGame.unity`가 생기기 전까지는 확인 불가.
3. ~~다섯 번째 "설정" 버튼~~ — 위와 같은 이유로 확인 불가.

## 한 일

23:00 세션. `docs/feedback.md`는 비어 있었다. backlog에서 "코어 루프 개정 반영" 절의 L-01~L-03을
위에서부터 셋 다 끝까지 갔다 — D04(실시간 채굴 UI)보다 먼저 확인하라고 backlog에 적혀 있던 항목들이다.

- **L-01 탐험+발견+선택 채굴 구조 — 코어 모델**
  - `Packages/.../Runtime/Exploration.cs`: `ExplorationSimulator.Discover(rig, planet, 경과초, 보물목록, seed)`
    가 경과 시간(온라인 틱이든 오프라인 누적이든 같은 함수) 동안의 보물 발견 목록을 만든다.
    seed 하나로 재현 가능 — 서버가 같은 코드로 검증할 수 있다.
  - 광맥은 그대로 `MiningSimulator`가 시간당 자동 산출을 계산한다(기획 문서대로 "광맥은 흔해서 자동").
    여기서는 드문 보물만 다룬다.
  - 발견마다 `CanMineNow`(지금 도구로 캘 수 있는지)를 들고 있다. 못 캐는 등급도 목록엔 남아서
    "도구를 올려야 캘 수 있다"는 다음 목표가 된다 — core-loop.md의 설계를 그대로 코드로 옮긴 것.
  - **D04-N에서 남은 일**: 이 위에 MonoBehaviour를 얹어 실시간 틱마다 `Discover`를 부르고
    UI에 발견 목록을 그리는 것. 코어 계산은 이미 끝났다.
- **L-02 보물 데이터 모델**
  - `Models.cs`에 `TreasureGrade`(C~S), `TreasureDef`(등급, 요구 도구 레벨, 광물 환산치)
  - `DefaultData.QuartzTreasureDefs()`: 4종, 요구 도구 레벨 1/11/21/26 (곡괭이/드릴/레이저 티어 경계에 맞춤).
    수치는 플레이스홀더, P4 봇 시뮬레이션에서 재조정
- **L-03 채굴차 부품 슬롯 · 레이스 보상 재작성**
  - 슬롯 5개로 결정: Tool/Cargo/Engine/Detector/Refinery — 기존 `MiningRig`의 레벨 필드와 그대로 1:1.
    새 오브젝트 체계를 안 만들어도 됐다
  - `RigParts.cs`: `RigPartReward`(슬롯 + 레벨 보너스) + `RigPartApply.Apply`(순수 함수, 슬롯 레벨만 올림)
  - `DefaultData.QuartzLocalRaceRewards()`: 쿼츠 로컬 레이스 3개가 각각 Tool/Cargo/Engine을 하나씩 올려 줌.
    광물이 아니라 부품이 나와야 코어 루프가 상호 강화 나선이 된다(core-loop.md)
  - `docs/design/core-loop.md`의 "아직 안 정한 것"에서 정해진 항목을 "정해진 것"으로 옮기고,
    `docs/decisions.md`에 두 줄 추가
- 테스트 6개 추가(보물 요구 레벨 단조 증가, 탐험 재현성·시간 비례, CanMineNow 게이팅, 레이스 보상 적용·슬롯 분산).
  `Core.Tests`: **통과 20 / 실패 0** (`cd Core.Tests && dotnet run`)
  - 이 클라우드 환경도 `dotnet`이 없어서 `apt-get install dotnet-sdk-8.0`으로 새로 설치했다(9/13 밤과 동일한 상황, 매번 새 환경이라 그런 듯)

## 웹에서 확인할 것

- 없음. 이번 건은 전부 코어 C# 로직(모델·시뮬레이터·데이터 테이블)이라 화면이 없다.

## 이번 건 중 웹에 안 보이는 것

- L-01/L-02/L-03 전부. 순수 데이터·계산 코드라 눈에 보이는 화면이 없다. 굳이 배포 주소를 열어 볼 필요 없음
  (기존 배포 화면은 이전과 그대로 — 레이스 카메라 실험 씬)

## 막힌 것·결정이 필요한 것

- **보물 발견 확률(`chancePerCycle = 0.05` 고정값)과 요구 도구 레벨 수치**는 전부 플레이스홀더다.
  실제 체감(너무 자주/드물게 나온다)은 D04-N에서 UI가 붙고 나서, 최종 조정은 backlog L-05(봇 시뮬레이션)에서.
- **레이스 보상 등급별 차등** — 지금은 로컬 레이스든 그랑프리든 전부 `LevelBonus = 1` 고정이다.
  "로컬=C, 그랑프리=S" 식의 대응은 아직 안 정했음(core-loop.md에 남겨둠). P2 레이스 4등급이 생길 때 결정 필요.
- 이번 세션도 Unity 에디터가 없어 컴파일 확인은 `Core.Tests`까지만 했다. 새로 만든 `Exploration.cs`,
  `RigParts.cs`, `Models.cs`/`DefaultData.cs` 추가분은 `Core.Tests`가 실제로 컴파일·실행해서 검증된 코드다
  (Assets/Scripts 쪽은 이번에 건드리지 않았음).

## 아침 피드백 (Tifania가 적는 곳)
-

### 00:02 세션 (주말 매시간)

- 한 일: 앞 세션(D10-N, 51분 전 커밋) 안 겹침 확인 → `docs/feedback.md` 비어 있어서 backlog 다음
  순서 D11-N(공구 상자)을 집었다. 등급(C~S)만 뽑는 확률표 코어 `Core/LootTable.cs` 신규 —
  녹슨/강철/티타늄 세 표 + 천장(강철 30개 A 확정, 티타늄 10개 S 확정, GDD 그대로). 등급이 실제로
  무엇을 주는지(채굴차 부품 vs 레이싱카 부품 청사진)는 GDD와 core-loop.md가 서로 다른 말을 하고
  있어서 판단이 필요하다고 보고 개봉 연출·인벤토리 반영은 안 했다 — `docs/decisions.md` "T-07"에
  선택지 3개(A/B/C안) 정리, 이번 세션은 C안(결정 보류, 코어만 완성)으로 두고 넘어감.
  `Core.Tests`에 6개 추가(가중치 합 1.0, seed 재현성, 10만 회 분포가 표와 1%p 안쪽, 천장이 정확히
  pityCount번째에만 작동, 천장 없는 상자는 안 확정, 빈 표/가중치 합 0 예외) — **통과 66 / 실패 0**.
  이 클라우드 환경도 `dotnet`이 없어서 다시 설치했다(매 세션 새 환경).
- 웹에서 확인할 것: 없음. 순수 코어 C# 로직(확률 계산)이라 화면이 없다.
- 이어서 할 것: T-07 결정이 나면(또는 애매하면 A안대로 "전부 채굴차 부품" 기본 진행) D11-N 나머지
  — 개봉 연출(`Assets/UI/LootBox.uxml` 등, 다른 오버레이 패턴 재사용)과 상자 인벤토리(SaveData에
  상자 종류별 보유 개수 + openedSincePity 카운터 필드)를 마저 만든다. 결정이 계속 안 나면 다음
  세션이 B안 세부 설계(레이싱카 부품 B/A/S 등급 데이터부터 필요)까지 검토해도 된다.

### 01:07 세션 (주말 매시간)

- 한 일: 앞 세션(00:02, 50분 전 커밋) 안 겹침 확인 → `docs/feedback.md` 비어 있음 → 직전 세션이
  남긴 "이어서 할 것"대로 T-07이 이번에도 결정 안 났으니 그 세션이 미리 남긴 안전장치("애매하면
  A안 기본 진행")를 그대로 써서 D11-N을 이어갔다.
  - `Core/LootReward.cs` 신규 — 공구 상자 등급(C~S)을 채굴차 부품 보상(`RigPartReward`)으로
    바꾼다. 등급이 높을수록 희귀 슬롯(Detector/Refinery) 확률이 커지고(C는 아예 안 나옴, S는
    흔한 슬롯 Tool/Cargo/Engine이 아예 안 나옴), LevelBonus도 커진다(C/B=1, A=2, S=3).
  - `Core/LootBoxOpener.cs` 신규 — 확률표 뽑기(`LootTable.Open`) + 천장 카운터 갱신
    + 부품 매핑(`LootReward.FromLoot`)을 한 번에 묶는 조립 함수. 다음에 개봉 화면을 만들 때
    이 함수 하나만 부르면 되게 설계.
  - `SaveData.cs`에 상자 인벤토리 필드 추가 — `RustyBoxCount`/`SteelBoxCount`/`TitaniumBoxCount`,
    `SteelOpenedSincePity`/`TitaniumOpenedSincePity`(녹슨은 천장이 없어 카운터 불필요).
  - `docs/decisions.md` T-07에 "A안으로 확정 진행" 후속 기록 추가, `docs/GDD.md`의 "공구 상자"
    문장에서 "S 청사진 확정" 표현을 지웠다(A안과 어긋나던 부분 — CLAUDE.md 7번 규칙).
  - `docs/backlog.md` D11-N 항목을 갱신 — 남은 일은 "상자를 실제로 얻는 경로(레이스 보상에 상자
    자체가 아직 없음)"와 "개봉 화면(UI)" 둘로 좁혀졌다.
  - `Core.Tests`에 7개 추가(LevelBonus 단조 증가, 슬롯 선택 재현성, C/S 등급 슬롯 배타성,
    RigPartApply 실제 적용, LootBoxOpener 천장+카운터 리셋 통합, 녹슨 상자 카운터 무제한 증가).
    이 클라우드 환경도 `dotnet`이 없어서 다시 설치했다(매 세션 새 환경). **통과 73 / 실패 0**.
- 웹에서 확인할 것: 없음. 순수 코어 C# 로직 + SaveData 필드 추가라 화면이 없다.
- 이어서 할 것: D11-N 남은 두 가지 중 하나를 다음 세션이 고르면 된다 — ①`DefaultData`의 레이스
  보상 테이블에 공구 상자 자체를 보상으로 추가(지금은 `RigPartReward`만 나옴, GDD엔 "서킷=강철,
  챌린지=티타늄" 식 매핑이 있으니 참고), ②`Assets/UI/LootBox.uxml`+`LootBoxPanel.cs` 개봉 화면
  (다른 오버레이 패턴 재사용, `LootBoxOpener.Open` 하나만 부르면 됨). 순서는 상관없지만 ①이 먼저
  있어야 ②를 실제로 테스트할 상자가 생긴다.

### 02:02 세션 (주말 매시간)

- 한 일: 앞 세션(01:07, 54분 전 커밋) 안 겹침 확인 → `docs/feedback.md` 비어 있음 → 직전 세션이
  남긴 "이어서 할 것" ①(레이스 보상 테이블에 상자 추가)을 집었다. `docs/backlog.md`를 다시
  보니 "서킷·챌린지 코스 자체"는 이미 W2("레이스 4등급 해금 구조")가 예정해 둔 별도 작업이라,
  그 코스를 새로 만들지는 않고 매핑 뼈대만 준비하는 쪽으로 좁혔다 — 그러다 GDD와 L-03(로컬
  레이스 보상)이 또 어긋나 있는 걸 발견해 `docs/decisions.md` T-08로 정리하고 결정했다.
  - `Core/RaceSimulator.cs`의 `Course`에 `Tier` 필드 추가(`RaceTier`: Local/Circuit/Challenge/
    GrandPrix, GDD "레이스" 항목 그대로).
  - `Core/RaceBoxReward.cs` 신규 — 등급→공구 상자 매핑(로컬=녹슨/서킷=강철/챌린지=티타늄,
    그랑프리는 상자가 아니라 "워프"라 제외). 서킷·챌린지 코스 자체는 안 만들었다(W2 몫).
  - T-08 결정: 로컬 레이스는 L-03의 확정 슬롯 보상 + GDD의 녹슨 상자 **둘 다** 준다 — 온보딩
    설계 의도(확정 슬롯)는 안 건드리고, 그동안 코드 어디에도 없던 GDD 상자 보상만 채웠다.
  - `MiningController.TryEnterRace`가 우승 시 `RaceBoxReward.ForTier(course.Tier)`로 상자를
    골라 `_save`에 바로 더한다(`RustyBoxCount` 등). `MiningController.RustyBoxCount/SteelBoxCount/
    TitaniumBoxCount` 읽기 전용 프로퍼티 추가 — `_save`를 그대로 읽으니 별도 캐시 필드 없이도
    Save()가 다루는 값과 항상 같다. `RaceEntryPanel`의 우승 문구도 상자를 같이 보여주게 고쳤다.
    nullable enum을 switch로 매치하는 문법은 이 Unity 버전에서 확실하지 않아서(에디터 없음)
    `==` 비교로만 썼다.
  - `LootBoxOpener.cs`에 `NameKo(LootBoxType)` 추가 — 화면 표시용 한국어 이름, 개봉 화면(다음
    세션)과 레이스 결과 화면이 같이 쓴다.
  - `Core.Tests`에 2개 추가(등급→상자 매핑이 GDD와 일치, 쿼츠 코스 3개가 전부 Local이라 셋 다
    녹슨 상자). 이 클라우드 환경도 `dotnet`이 없어서 다시 설치했다(매 세션 새 환경).
    **통과 75 / 실패 0**.
- 웹에서 확인할 것: `RaceEntryPanel`의 우승 결과 문구가 바뀌었다("우승 보상: ... + 녹슨 상자
  1개"). 다만 이번 세션은 Unity 에디터가 없어 컴파일을 못 봤다 — **아침 확인 필요**: 다음 Unity
  세션이 컴파일 에러 없는지부터 보고, 있으면 로컬 레이스 우승 화면에서 문구가 실제로 뜨는지 확인.
- 이어서 할 것: D11-N 남은 건 개봉 화면(`Assets/UI/LootBox.uxml`+`LootBoxPanel.cs`) 하나뿐 —
  `MiningController.RustyBoxCount`로 실제 보유 개수를 읽고 `LootBoxOpener.Open`으로 열면 된다.
  이번 세션에서 처음으로 실제 상자가 생기니(로컬 레이스 우승 시 녹슨 상자 1개) 다음 세션은
  바로 화면 작업에 들어가도 된다. 서킷·챌린지 코스는 손대지 말 것(W2 몫).

### 03:02 세션 (주말 매시간)

- 한 일: 앞 세션(02:02, 60분 전 커밋) 안 겹침 확인 → `docs/feedback.md` 비어 있음 → 직전 세션이
  남긴 "이어서 할 것"(개봉 화면)을 그대로 집어 D11-N을 마무리했다. `MiningController.TryOpenBox
  (type, out result)` 신규 — 보유 0개면 false, 등급·슬롯 뽑기 seed는 `TryEnterRace`와 같은
  이유로 `UnityEngine.Random`(코어는 seed를 인자로만 받는다, CLAUDE.md 1번), `LootBoxOpener.Open`
  결과를 `RigPartApply.Apply`로 즉시 채굴차 부품에 반영하고 상자 개수 차감 + 강철/티타늄 천장
  카운터 갱신까지 한 번에 저장한다. `Assets/UI/LootBox.uxml`+`.uss`(Crafting.uxml과 같은 반응형
  패턴 — 상자 세 종류 한 줄씩 + 결과 카드 하나) + `Assets/Scripts/UI/LootBoxPanel.cs`(Crafting
  Panel과 같은 역할 분담, 코어가 이미 다 조립해 둬서 여기는 화면 갱신·클릭 전달만).
  `Root.uxml`의 action-row에 "상자" 버튼 추가(기존 세 칸이 flex-basis 25%라 네 칸도 그대로
  맞는다), `MainHud.cs`에 `boxDocument` 토글 배선(업그레이드/제작/레이스와 완전히 같은 패턴),
  `BootstrapMainGame.cs`(`GemRacer/7`)에 오버레이(sortingOrder 13, 레이스보다 위) 추가 + 아침
  확인 안내 문구 갱신. 코어(Packages)는 하나도 안 고쳐서(Assets 글루 코드만) `Core.Tests`는
  손 안 댐 — 직전 세션 수치(통과 75 / 실패 0) 그대로일 것.
  `docs/backlog.md` D11-N을 `[x]`로 닫았다 — 서킷·챌린지 코스(강철·티타늄 상자를 실전에서
  얻는 길)는 여전히 W2 몫으로 남아 있어서, "상자" 버튼의 강철/티타늄 줄은 지금은 보유 0개로
  계속 비활성인 게 정상이다(코드 확인용으로만 존재, 회귀 아님).
- 웹에서 확인할 것: "상자" 버튼을 누르면 개봉 화면이 열리는지, 로컬 레이스로 얻은 녹슨 상자의
  "열기"를 누르면 결과 카드에 등급·부품 보상이 뜨고 실제로 채굴차 슬롯이 오르는지(업그레이드
  화면에서 레벨 확인). 이번 세션도 Unity 에디터가 없어 컴파일을 못 봤다 — **아침 확인 필요**:
  컴파일 에러부터 보고, 특히 `LootBoxPanel.cs`의 `UIDocument`/`Button.clicked` 클로저 캡처와
  `TryOpenBox`의 `out LootBoxOpenResult` 문법.
- 이어서 할 것: D11-N 끝. 다음은 backlog 순서대로 D12-N(부품 강화 +10)이나, W2(레이스 4등급
  해금 구조 — 서킷·챌린지 코스가 생기면 이번 세션이 만든 강철/티타늄 개봉 버튼이 바로 살아난다).

### 04:03 세션 (주말 매시간)

- 한 일: 앞 세션(03:02, 57분 전 커밋) 안 겹침 확인 → `docs/feedback.md` 비어 있음 → 직전 세션이
  남긴 "이어서 할 것" 그대로 D12-N(부품 강화)을 집었다. 코어에 `PartEnhance.cs` 신규 —
  `Cost(part)`(등급 제작 비용의 절반에서 시작해 단계마다 1.9배로 가파르게, +10이면
  `PositiveInfinity`), `AtMax`, `Apply`(실패 롤 없음 — `RigUpgrade`와 같은 패턴, GDD "+10
  실패 없음, 비용 가파름" 그대로). `Part.Enhance`/`Effective()`(+6%/단계)는 D08-N 때 이미
  있었다.
  - **부품 정체성 버그를 하나 발견해 같이 고쳤다**: `MiningController.AvailableParts`가
    `DefaultData.QuartzStarterParts()`를 부를 때마다 완전히 새 `Part` 인스턴스를 만들고
    있었다 — 강화 단계를 Enhance 필드에만 두면 다음 프레임(Update마다 다시 그리는
    CraftingPanel)에서 그냥 사라진다는 뜻. `MiningController`에 `_partEnhanceLevels`(부품
    id→강화 단계 딕셔너리)를 두고 `AvailableParts`가 새 인스턴스를 만들 때마다 저장된
    값을 다시 입히게 고쳤다. 세이브는 `SaveData.OwnedPartEnhanceLevels`(`OwnedPartIds`와
    같은 인덱스의 병렬 리스트 — Dictionary를 JsonUtility가 못 다뤄서 `EquippedPartIds`와
    같은 방식). `LoadParts`는 이 딕셔너리를 `OwnedPartIds`보다 먼저 채운다 — 그 아래서
    장착 슬롯을 복원할 때 `FindPart`가 `AvailableParts`(강화 반영판)를 부르기 때문에
    순서가 어긋나면 강화 전 상태로 복원된다.
  - `MiningController.TryEnhancePart(part)` 신규 — 비용 차감→`PartEnhance.Apply`→
    `_partEnhanceLevels` 갱신→저장. 지금 장착 중인 슬롯이 같은 부품이면 그 인스턴스(Awake
    시점에 만들어진 별개 참조, Car.Slots에 저장된 것)도 같이 맞춰서 레이스 스탯 계산에
    바로 반영되게 했다 — 안 그러면 강화 버튼은 눌리는데 실제 레이스에는 안 먹는 상태가 된다.
  - `Assets/UI/Crafting.uxml`/`.uss`: 기존 다섯 줄 각각에 강화 라벨(+N)과 강화 버튼을
    추가(새 오버레이·새 씬 없음, 같은 반응형 패턴 재사용). `CraftingPanel.cs`가 미보유면
    버튼 비활성, +10이면 "MAX", 그 외엔 "강화 (비용)"을 보여주고 클릭 시 `TryEnhancePart`.
  - `Core.Tests`에 5개 추가 — 비용 단조 증가(가파름 확인), +10에서 Cost=무한대·AtMax=참,
    Apply가 실패 없이 10단계까지 오르고 그 이상은 클램프, +0/+10 스탯 배율 경계값(1.0배/1.6배,
    Models.cs 공식과 일치), 미정의 등급(B) 예외. 이 클라우드 환경도 `dotnet`이 없어서
    `apt-get install dotnet-sdk-8.0`으로 새로 설치(매 세션 반복되는 패턴). **통과 80 / 실패 0**.
- 웹에서 확인할 것: 위 "오늘 웹에서 확인할 것" 3번 항목 그대로 — 제작 화면 강화 버튼이 실제로
  뜨는지, 누르면 +N이 오르고 원석이 깎이는지, 장착 중인 부품을 강화했을 때 레이스 결과(스탯)에
  반영되는지까지. 이번 세션도 Unity 에디터가 없어 컴파일을 못 봤다 — **아침 확인 필요**:
  컴파일 에러부터 보고, 특히 `CraftingPanel.cs`의 새 `_enhanceLabels`/`_enhanceButtons` 배열
  이름이 `Crafting.uxml`과 정확히 맞는지.
- 이어서 할 것: backlog 순서대로 다음은 D13-N(튜토리얼 첫 5분)이나 W2(레이스 4등급 해금
  구조 — D11-N이 만들어 둔 강철/티타늄 개봉 버튼이 이걸로 살아난다). 코어 쪽엔 아직 급한
  막힌 것 없음.

### 05:02 세션 (주말 매시간)

- 한 일: 앞 세션(04:03, 54분 전 커밋) 안 겹침 확인 → `docs/feedback.md` 비어 있음 → 직전 세션이
  남긴 "이어서 할 것" 중 backlog 순서대로 D13-N/D13-M(튜토리얼 첫 5분)을 집었다(W2는 코스 데이터가
  더 필요해 범위가 커서 다음 큰 세션 몫으로 남겼다). 자세한 내용은 `docs/backlog.md` D13-N/D13-M
  항목 참고 — `SaveData.TutorialStep`(코어, 필드 하나) + `MiningController.TutorialStep`/
  `AdvanceTutorial()` + `Assets/UI/Tutorial.uxml`/`.uss` + `TutorialController.cs`(화면을 막지
  않는 배너, `pickingMode` 조정으로 배너 밖 클릭을 HUD로 통과시킴) + `BootstrapMainGame.cs` 배선.
  `Core.Tests`는 필드 하나만 늘어서 그대로 — 직접 `dotnet run`으로 재확인, **통과 80 / 실패 0**
  (이 클라우드 환경도 `dotnet`이 없어서 다시 설치, 매 세션 반복되는 패턴).
- 웹에서 확인할 것: 위 "오늘 웹에서 확인할 것" 3번 항목 그대로. 이번 세션도 Unity 에디터가 없어
  컴파일을 못 봤다 — **아침 확인 필요**: 컴파일 에러부터, 특히 `TutorialController.cs`의
  `VisualElement.pickingMode`/`PickingMode.Ignore`·`Position` 사용이 이 Unity 버전에서 맞는지.
- 이어서 할 것: backlog 다음 순서는 D14-N(사운드 자리 배선 + 설정 화면)이나 W2(레이스 4등급 해금
  구조 — D11-N이 만들어 둔 강철/티타늄 개봉 버튼이 이걸로 살아난다). 코어 쪽엔 급한 막힌 것 없음.
  튜토리얼 문구(D13-M)는 일단 다듬어 뒀지만 실제 플레이 감상은 Tifania가 웹에서 보고 판단할 몫 —
  이상하면 `docs/feedback.md`에 적어 주면 다음 세션이 `TutorialController.Messages`만 고치면 된다.

### 06:05 세션 (주말 매시간)

- 한 일: 앞 세션(05:02, 63분 전 커밋) 안 겹침 확인 → `docs/feedback.md` 비어 있음 → 직전 세션이
  남긴 "이어서 할 것" 그대로 D14-N/D14-M(사운드 자리 + 설정 화면)을 집었다(W2는 코스 데이터가
  더 커서 다음 큰 세션 몫으로 다시 넘김). 자세한 내용은 `docs/backlog.md` D14-N/D14-M 항목 참고 —
  `Core/GameSettings.cs`(순수 함수 `NormalizeFrameRate`, 30/60만 허용) + `SaveData.SoundEnabled`/
  `TargetFrameRate` + `Assets/Scripts/Audio/AudioHub.cs`(엔진·채굴·UI 탭·상자 AudioSource 네
  개, 클립 없으면 전부 무음) + `Assets/UI/Settings.uxml`/`.uss` + `SettingsPanel.cs` +
  `MiningController.SetSoundEnabled`/`SetTargetFrameRate` + `Root.uxml`의 다섯 번째 "설정" 버튼 +
  `BootstrapMainGame.cs`(`GemRacer/7`) 배선. `MainHud`/`LootBoxPanel`의 기존 버튼들도 `audioHub`가
  물려 있으면 탭/개봉 효과음을 내도록 살짝 고쳤다(클립이 없어 지금은 들리지 않음). 코어 함수
  하나뿐이라 `Core.Tests`는 3개만 추가(허용값 그대로, 45 경계, 0·음수·아주 큰 값 방어) —
  **통과 83 / 실패 0**. 이 클라우드 환경도 `dotnet`이 없어서 `apt-get install dotnet-sdk-8.0`으로
  다시 설치했다(매 세션 반복되는 패턴, apt 미러가 한 번 404를 냈다가 `apt-get update` 후 됐다).
- 웹에서 확인할 것: 위 "오늘 웹에서 확인할 것" 3번 항목 그대로. 이번 세션도 Unity 에디터가 없어
  컴파일을 못 봤다 — **아침 확인 필요**: 컴파일 에러부터, 특히 `SettingsPanel.cs`의
  `Button.EnableInClassList` 호출과 `BootstrapMainGame.cs`가 `AudioSource`를 한 GameObject에
  네 개 붙이는 부분(`AddComponent<AudioSource>()` 네 번 — 여러 개 붙는 게 이 Unity 버전에서
  문제없는지). action-row가 이제 버튼 다섯 개라 세로 화면에서 넷+하나로 줄바꿈되는 모양이
  어색하면 `Root.uss`의 `.action-button` `flex-basis`를 20%로 낮출 것(코드에도 주석으로 남겨 둠).
- 이어서 할 것: backlog 다음 순서는 D15-N(안드로이드 빌드 준비)이나 W2(레이스 4등급 해금 구조).
  사운드는 아직 클립이 하나도 없다 — 실제로 소리를 넣는 건 W3(에셋 팩 적용) 몫, 지금은 자리만
  잡아 둔 상태라는 걸 다음 세션도 헷갈리지 않게 남겨 둔다.

### 07:02 세션 (주말 매시간)

- 한 일: 앞 세션(06:30, 21분 전 커밋) 안 겹침 확인 → `docs/feedback.md` 비어 있음 → backlog
  다음 순서 D15-N(안드로이드 빌드 준비)을 집었다. `docs/design/android-build-checklist.md`
  신규 — `ProjectSettings.asset`을 직접 열어 지금 값(최소 API 25, 목표 API Auto, IL2CPP,
  ARM64는 이미 적정값이라 안 건드림; 화면 방향은 4방향 자동이라 고쳐야 함; 패키지명은 URP
  템플릿 placeholder 그대로)을 표로 대조했다. `Assets/Editor/BuildSettingsMobilePC.cs` 신규 —
  `GemRacer/9. 안드로이드 세로 고정 적용`(방향 필드만), `GemRacer/10. PC 세로 창 설정 적용`
  (Standalone 기본 창 540×960 + 리사이즈 허용 + Windowed). 둘 다 `ProjectSettings.asset`의
  실제 YAML 키(defaultScreenOrientation, allowedAutorotateTo*, defaultScreenWidth/Height,
  resizableWindow, fullscreenMode)를 먼저 확인하고 그 이름 그대로 PlayerSettings API를 썼다 —
  에디터가 없어 컴파일 확인은 못 했지만 API 이름 자체는 근거가 있다. 키스토어는 비밀번호가
  같이 도는 절차라 코드화 대신 문서 절차로만 남기고 `.gitignore`에 `*.keystore`/`*.jks` 추가.
  패키지명은 클라우드 세션이 정할 값이 아니라고 보고 `docs/decisions.md` T-09에 선택지(A안
  `com.bax.gemracer` 제안)만 올리고 스크립트는 안 건드렸다. 코어(Packages)는 안 건드려서
  `Core.Tests`는 그대로(직전 통과 83 / 실패 0, 이번 세션은 재실행 안 함 — 위 규칙대로 코어
  변경 없을 때는 생략).
- 웹에서 확인할 것: 없음. Android/Standalone 플레이어 설정이라 지금 배포된 WebGL 화면에는
  아무 영향이 없다.
- 이어서 할 것: D15-M(PC에서 Unity 열어 `GemRacer/9`·`GemRacer/10` 메뉴가 실제로 컴파일·
  실행되는지, 체크리스트 표의 "확인만" 줄들이 Player Settings 창과 맞는지 대조) — 다음 PC
  세션 몫. 그 다음 backlog 순서는 D16-N(안정화 1, 피드백 밀린 것 처리)이나 W2.

### 08:09 세션 — 이 세션부터는 "주말 매시간"이 아니라 평일 아침이다 (일정 확인 필요)

**일정 확인 필요**: 이번 세션에 온 프롬프트는 "토요일·일요일 매시간" 주말 세션 지시문이었는데,
실제 컨테이너 시계는 `Mon Sep 14 08:09 KST 2026`(평일 아침, CLAUDE.md 일과표의 06:30 "작게 도는"
칸도 이미 지난 시각)이었다. 앞 세션(07:02, 62분 전 커밋)도 스스로 "주말 매시간 세션"이라고
적었지만 그 시각도 이미 월요일 새벽(KST)이었다 — 즉 최소 07:02 세션부터 이 예약이 주말 경계를
넘어 평일에도 매시간 계속 돌고 있다는 뜻. CLAUDE.md 평일 일과(23:00/03:00/06:30)와 다르게
지금 이 스케줄이 평일 낮에도 매시간 계속 돌면 Tifania가 회사 일에 쓰려던 사용량을 깎아 먹는다
— 이 클라우드 세션에서는 예약 자체(며칠·몇 시에 도는지)를 조회·수정할 도구가 없어서 코드로
고치지 못했다. Tifania가 예약 설정(주말 전용으로 제한돼 있는지)을 직접 확인해 줄 것.
- 한 일: 앞 세션(07:02, 62분 전 커밋) 안 겹침 확인 → `docs/feedback.md` 비어 있음 → backlog
  순서상 D15-M은 PC 전용이라 건너뛰고 D16-N(피드백 밀린 것 처리 — 처리할 게 없어 바로 통과)·
  D16-M(테스트 전수 통과 확인)을 거쳐 D17-N/D17-M(지인 테스트 피드백 버튼 + 세션 로그)을 집었다.
  자세한 내용은 `docs/backlog.md` D16-N~D17-M 항목 참고 — `Assets/Scripts/Diagnostics/FeedbackLog.cs`
  (텍스트 저장 + 클립보드 복사)·`SessionLogger.cs`(포그라운드 구간을 세션으로 봐서 CSV 기록),
  설정 화면에 피드백 입력칸 추가, `docs/design/session-log-format.md` 신규. 둘 다 게임 규칙이
  아니라 Core는 안 건드렸다(CLAUDE.md 1번) — `Core.Tests`는 직접 재실행만 해서 확인
  (**통과 83 / 실패 0**, 이 클라우드 환경도 `dotnet`이 없어서 다시 설치, 매 세션 반복되는 패턴).
- 웹에서 확인할 것: 여전히 없음 — `MainGame.unity`가 아직 생성된 적이 없어서(위 06:30 세션 기록
  그대로) 이번에 만든 설정 화면 피드백 칸도 `GemRacer/7` 메뉴를 눌러 씬을 만들기 전까진 배포
  화면에 안 보인다. 컴파일 확인 필요 — `SettingsPanel.cs`의 새 `TextField`/`GUIUtility.systemCopyBuffer`
  사용은 이 Unity 버전에서 문제없을 것으로 보이나(오래된 표준 API) 에디터 없이는 확신 못 함.
- 이어서 할 것: 여전히 최우선은 `GemRacer/7. 메인 게임 씬 만들기`를 실제로 눌러 `MainGame.unity`를
  만드는 것(PC 세션 몫, D15-M과 같이 처리하면 됨) — 이게 없으면 지금까지 쌓은 화면이 전부 배포에
  안 보인다. 코드 쪽 backlog 다음 순서는 D18-N(리텐션 훅)이나 W2.

### 14:20 세션 (Unity 에디터 연결됨 — 밀린 "아침 확인 필요" 일괄 해소)

Tifania가 Unity MCP를 붙여 줘서, 여러 세션이 "에디터가 없어 컴파일을 못 봤다"고 남긴 것들을
에디터에서 직접 확인했다. Unity 6000.3.10f1, 라이선스는 Personal(HasPro=False).

- **컴파일 에러 0.** 밤새 손으로 쓴 스크립트 전부 통과했다. `TutorialController`, `SettingsPanel` 등
  9/14 daily에 "확인 필요"로 남아 있던 것들 포함. 남은 콘솔 에러는 Unity AI Assistant 패키지의
  구독 없음 경고 하나뿐이고 프로젝트와 무관하다.
- **씬 세 개가 실제로는 없었다.** `BootstrapMainGame` / `BootstrapResponsiveUI` / `BootstrapUpgradeUI`는
  코드만 있고 에디터가 없어 한 번도 실행된 적이 없었다. 그래서 Build Settings 0번이 `RaceCameraSpike`
  (카메라 실험)였고, **Tifania가 아침마다 연 웹 빌드는 게임이 아니라 카메라 실험이었다.**
  메뉴 7·5·6을 실행해 `MainGame.unity`, `ResponsiveUITest.unity`, `UpgradeTest.unity`를 만들었다.
  MainGame이 0번으로 들어갔다.
- **MainGame 씬 구성 확인.** 루트 14개 — 카메라(CameraFollow), 쿼츠 행성, 채굴차(SurfaceMover +
  MiningController), GameFlow, AudioHub, 그리고 UIDocument 여덟 개(HUD·업그레이드·제작·레이스·
  상자·설정·튜토리얼·오프라인 보상). 여덟 개 전부 UXML을 제대로 물었다.
- **Play로 돌려서 런타임 버그를 하나 잡았다.** NullReferenceException 두 개가 났다 —
  `MiningSimulator.RigSpeed`(planet이 null)와 `MiningController`의 `SoundEnabled`(_save가 null).
  원인은 실행 순서다. 패널 여덟 개가 전부 `OnEnable`에서 MiningController의 프로퍼티를 읽는데
  MiningController는 `Awake`에서 `_save`/`_planet`을 채운다. 유니티는 게임오브젝트 사이의
  Awake/OnEnable 순서를 보장하지 않아서 패널이 먼저 깨면 터진다.
  `MiningController`에 `[DefaultExecutionOrder(-100)]`을 달아 고쳤고, 다시 Play해서 예외 0 확인.
  채굴 루프도 돈다 — 원석 0 → 2.000, 단계 MiningVein → Traveling.
- **로고.** `웹 빌드 설정만 적용` 메뉴를 실행하니 `스플래시 show=False, 유니티로고=False,
  템플릿=PROJECT:GemRacer`. **Personal 라이선스인데도 스플래시 끄기가 먹었다** — Unity 6에서는
  Personal도 끌 수 있다는 뜻이다. 로딩 화면 큐브는 새 템플릿으로 이미 빠졌다.

## 넘길 것 — Tifania가 정해야 함

- `companyName`이 아직 `DefaultCompany`다. 웹 빌드와 나중에 스토어에 그대로 나간다. 뭘로 할지.
- `productName`이 `PlanetRacer`라서 브라우저 탭에 영문으로 뜬다. "보석 행성 레이서"로 바꿀지.

### 14:55 — main 승격 + 아침에 여는 주소가 왜 옛날 화면이었는지

**중요한 것을 하나 찾았다.** `.github/workflows/webgl.yml`을 보면 Cloudflare Pages 배포가
`--branch=${{ github.ref_name == 'main' && 'main' || 'dev' }}` 로 갈린다. 즉 `claude/dev` 푸시는
Pages의 **dev 브랜치**(프리뷰)로 가고, Tifania가 아침마다 여는 `planetracer-daz.pages.dev`는
**production = main** 이다. 밤새 claude/dev에 쌓은 것은 main으로 승격되기 전에는 그 주소에 안 나온다.
빌드가 성공했는데도 아침 화면이 그대로였다면 대개 이 이유다.

CLAUDE.md의 "main 승격" 조건 세 개를 확인하고 승격했다.

1. `cd Core.Tests && dotnet run` → **통과 83 / 실패 0**
2. 직전 WebGL 빌드(claude/dev, D08-2) **성공**
3. `origin/main`이 `origin/claude/dev`의 조상이고 fast-forward 가능 (main 0 뒤, dev 3 앞)

**main 승격: 336a6ac..9f6adc2**

이제 production 빌드가 돌면 `planetracer-daz.pages.dev`에 아래가 한꺼번에 올라간다.

- 0번 씬이 `MainGame` — 카메라 실험이 아니라 실제 게임 화면
- 로딩 화면에서 유니티 큐브 제거(GemRacer 템플릿), 실행 직후 스플래시도 꺼짐
- 실행 순서 버그 수정으로 UI 패널 여덟 개가 예외 없이 뜸
