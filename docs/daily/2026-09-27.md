# 2026-09-27 (일)

### 오늘 웹에서 확인할 것

- `https://dev.planetracer-daz.pages.dev` — 업그레이드 화면 곡괭이 줄(맨 위)의 작은 아이콘.
  회색 톱니바퀴 대신 **채굴차 실루엣**이면 A-20이 반영된 것(시작 레벨 1이면 곡괭이 팔 버전).

## 00:0x 주말 세션 — Core.Tests 보강 (backlog·feedback 전부 막혀 있어 CLAUDE.md 3번대로)

- **시작**: `origin/claude/dev` 마지막 커밋이 55분 전이라 안 겹침. `git fetch`로 보니 로컬에
  없던 `claude/dev` 원격 브랜치가 있어 CLAUDE.md 0단계대로 새로 체크아웃부터 했다.
  `docs/feedback.md`엔 `- [ ]` 없음(둘 다 `- [?]`, Tifania 판단 대기). `docs/backlog.md` 상단을
  다시 훑었는데(A-17·A-18·A-19·U-08·M-05·M-13·P-06 등) 전부 어제 낮·밤 세션들이 이미 확인한
  그대로 Unity 에디터 필요 또는 Tifania 결정 대기로 막혀 있었다.
- **Core.Tests 커버리지를 클래스별로 점검했다** — 각 `Runtime/*.cs`의 public 클래스 이름이
  `Program.cs`에 몇 번 나오는지 세어 비어 있는 곳을 찾는 방식. 대부분 이미 두껍게 덮여 있었지만
  (어제 00:1x 세션이 Amplifier·Entitlements·SeasonPass·RewardAd·PartCraft·LootReward 등을
  훑었다고 적어 둠), `PartCraft.Recipe(grade)`가 돌려주는 **A/S 등급 혼합 레시피
  (`DefaultData.QuartzEpicRecipe`/`QuartzLegendaryRecipe`)는 카탈로그 자체를 직접 검증하는
  테스트가 하나도 없었다** — 위 두 값을 `Program.cs`에서 이름으로 찾아 봤을 때 실제 사용처
  (`Core.Tests/BalanceSimParts.cs`, 리포트 전용)뿐이었다.
- **"카탈로그 정합성" 테스트를 하나 추가했다**(기존 Planets/Parts/Courses/Treasure/ShopItems/
  LocalRaceRewards/SeasonPassTiers와 같은 패턴). `PlanetMineralRecipe.CanAfford`가 없는
  planetId를 항상 보유량 0으로 조용히 취급해서, 레시피에 오타 난 PlanetId가 들어가면 예외 없이
  "영원히 재료가 모자란 것처럼" 보이는 자리였다 — A/S 두 등급 레시피의 PlanetId가 실제
  `Planets()`를 가리키고 Amount가 양수인지를 확인한다. 지금 값(quartz/ruby)은 이미 맞았지만,
  다음에 다른 행성 레시피가 추가될 때 같은 종류의 조용한 오타를 여기서 잡아 준다.
- **`Core.Tests && dotnet run` → 통과 401 / 실패 0**(컨테이너에 dotnet 없어서
  `apt-get install -y dotnet-sdk-8.0`부터 설치, 매번 반복되는 패턴). Unity 참조 없는 순수 C#
  파일 1개 수정(`Core.Tests/Program.cs`)뿐, `.meta` 없음, Unity 세션 불필요.
- **이번 건은 웹에 안 보임**(테스트 추가뿐, 게임 동작 변화 없음).
- main 승격은 건드리지 않았다(정책상 Tifania만 할 수 있고, 대상 갱신은 아침 정리 세션 몫).

- 이어서 할 것: backlog·feedback이 여전히 code-only 신규 항목이 바닥나 있다(여러 세션째 같은
  결론). 다음 세션은 (1) 남은 `Runtime/*.cs` 중 아직 못 본 카탈로그 반환 함수가 더 있는지
  (`DailyLoginReward`·`SubscriptionDailyGrant`류 상수표), (2) 아니면 `docs/decisions.md` 아홉
  항목 중 P-08(행성 클리어 조건)처럼 다른 판단(P-06 40%)이 걸려 있는 것부터 Tifania가 고르기
  쉽게 A/B/C 요약을 더 짧게 다듬는 쪽을 볼 것.

## 00:1x 이미지 세션 — 0장 (대기 중 비어 있음, A-15 미개시)

- **0절 1번(시각)**: 00:09 KST — 20:00~08:00 안이라 통과.
- **먼저 로컬을 원격에 맞췄다.** `git fetch`로 보니 로컬이 `origin/claude/dev`보다 **2커밋 뒤**
  (`ff21904` → `1e02a33`)였다. 09-26 06시 세션이 뒤처진 로컬로 A-20/A-15를 잘못 읽은 적이
  있으므로, 판정 전에 `merge --ff-only`로 먼저 맞추고 나서 읽었다.
- **0절 4번(대기 중)**: `docs/design/art-requests.md`의 "대기 중"은 `<!-- 여기에 추가 -->`와
  09-23 이후의 기록 글뿐 — 실제 항목 0개.
- **0절 4번 판정**: `docs/backlog.md:309` **A-15가 `- [ ]`** 그대로다(출시 근접 전까지 보류).
  절차 문서대로 지금 남은 방아쇠는 A-15 하나이고 그것이 미개시이므로, 대기 중이 비어 있는 것이
  맞다 — 설계 문서를 다시 훑지 않고 여기서 끝낸다. A-20은 `- [x]`(09-26 완료)지만 새 아트
  요청이 나오지 않았으니 이제 신호가 아니다.
- **MISSING 한 줄 검사도 돌렸다**(0절 4번의 그 명령). 나온 줄은 전부 알려진 오탐 두 종류뿐이다
  — 옛 이름 `pet-t1~t4-*`·`pet-t7-*`(등급 폴더에 124장 다 있음, 옛 `carrier`=지금 `haul`)와
  접두사만 잘린 `arrive-`·`icon-grade-`·`planet-`·`pet-t`. **새로 빠진 그림은 없다.**
- **0절 5번(앞 세션 이미지 한도)**: 해당 없음 — 09-26·09-27 daily에 `이미지 한도 — N시간 후
  풀림` 줄 없음. 한도는 온전하다.
- 1절 캡처 준비는 돌리지 않았다 — 뽑을 것이 없어 0절에서 끝나는 세션이므로 화면을 볼 일이 없다
  (ChatGPT 앱도 띄우지 않았다).
- **결과: 0장.** 고장이 아니라 정상이다. 다음에 아트가 필요해지는 시점은 A-15(출시 준비)뿐이다.

## 01:0x 주말 세션 — feedback·backlog 전부 막혀 있어 docs/decisions.md 오류 정정

- **시작**: `origin/claude/dev` 마지막 커밋이 51분 전이라 안 겹침. `docs/feedback.md`엔 `- [ ]`
  없음. backlog 위쪽부터(A-17·A-18·A-19·U-08·M-05·M-13·W-09·D18-N 등) 다시 훑었는데 전부
  Unity 에디터 필요 또는 Tifania 판단 대기로 이미 막혀 있는 그대로였다. `Core.Tests` 커버리지도
  클래스별로 다시 세어 봤지만(0회로 잡힌 이름들은 전부 다른 이름(`ExplorationSimulator`,
  `RewardAdTracker`, `UpgradeCost` 등)으로 이미 두껍게 덮여 있는 false positive였다) — 여러
  세션째 나온 "code-only 신규 항목이 바닥났다"는 결론과 같다.
- **`docs/decisions.md`를 훑다가 실제 오류를 하나 찾았다.** "시즌 패스 무료 트랙 인장" 절이
  "다른 세 경로(행성 클리어/유료 구매/티타늄 상자)가 이미 `AddSeal`을 부르는 자리를 갖고
  있다"고 적어 뒀는데, 실제로 `grep -rn "\.AddSeal(" PlanetRacer Assets`로 확인해 보니
  **유료 구매 하나(`MiningController.DebugPurchase`, P-16 2026-09-26 12시 세션)만** 부른다 —
  행성 클리어는 P-08(클리어 조건)이 안 정해져 판정 자체가 없고, 티타늄 상자는 드롭 확률(W2
  대기)이 안 정해져 코드가 없다. `docs/backlog.md` P-16 항목 자체는 이미 정확하게 적혀 있었는데
  `decisions.md` 요약 두 군데(TOC 목차 줄 + "시즌 패스 무료 트랙 인장" 절 본문)만 옛 기록
  그대로 남아 어긋나 있었다. 다음에 이 절을 보고 판단하는 세션이 "세 경로 다 이미 된다"고
  잘못 믿지 않게 두 군데 다 고쳤다(CLAUDE.md 7번 "문서가 실제와 어긋나면 고친다").
  코드 변경 없음, `Core.Tests` 안 건드림(389 그대로).
- **이어서 할 것**: 여전히 code-only 신규 항목이 바닥나 있다. 다음 세션은 (1) 이번에 찾은 것
  같은 "decisions.md/backlog.md 요약이 실제 코드와 어긋나는 곳"이 더 있는지(`grep`으로
  claim을 직접 검증하는 방식이 이번에 효과적이었다), (2) 아니면 P-08(행성 클리어 조건)처럼
  실측 자료(`planet-travel-carryover-sim.md`)가 이미 있는 결정 대기 항목의 A/B/C 요약을
  더 짧게 다듬는 쪽을 볼 것.

> (병합 메모) 위 주말 세션과 아래 Unity 배선 세션이 같은 01:0x에 동시에 돌았다 — 위쪽은
> 클라우드(에디터 없음)라 U-08을 'Unity 에디터 필요'로 넘겼고, 아래 세션이 마침 그 U-08을 닫았다.
> 겹친 파일은 이 daily뿐이라 두 기록을 그대로 나란히 남긴다.

## 01:0x Unity 배선 세션 — U-08 마무리 (옛 UI Toolkit 75개 파일 삭제)

- **시작**: 세션에 연결된 폴더가 없어서 `device_bash`는 못 썼고, PC의 PowerShell(Windows-MCP)로
  저장소에 닿았다. `git fetch` → `claude/dev` 체크아웃(`131d56d`, 앞 커밋 50분 전이라 안 겹침).
  Unity는 **이미 켜져 있었다**(09-22부터) — 그래서 이 세션은 **닫지 않았다**.
- **정해진 grep 표식(`씬 배선은 Unity 세션 필요`)은 backlog·feedback 양쪽 0건**이었다. 하지만
  CLAUDE.md가 "표식이 0건이어도 backlog에서 `Unity 세션 몫`이라고 적힌 줄을 한 번 더 본다"고
  해 둔 대로 다시 봤고, 어제 여러 세션이 "남은 Unity 세션 몫은 U-08과 B-02"라고 적어 둔 것을
  찾았다. B-02는 Tifania 결정 대기(라벨을 줄일지·두 줄로 접을지·가로 스크롤로 갈지)라
  **U-08을 집었다.**
- **U-08 ②③⑤ + 덤을 한 번에 지웠다 — 75개 파일.** 앞 세션들(09-24 21시, 09-25 08시)이
  "Irreversible Local Destruction"에 막혀 두 번 되돌렸던 그 작업이다. 이번엔 통과했다.
  나눠서 지우면 중간 상태가 반드시 컴파일이 깨지는 구조라(백로그에 그렇게 적혀 있다)
  **한 커밋에 몰아서** 했다. 자세한 목록·확인 절차는 `docs/backlog.md` U-08 항목에 적었다.
- **빌드 씬이 다섯에서 셋으로 줄었다** — `EditorBuildSettings.asset`에서 `ResponsiveUITest`·
  `UpgradeTest`를 뺐다. 이 둘은 옛 UI Toolkit 테스트 씬인데 **Build Settings에 켜진 채로 있어서
  그동안 WebGL 빌드에 그대로 실려 있었다**(W-09 용량).
- **확인**: 컴파일 에러 0(`scriptCompilationFailed=false`), 씬 안 `UIDocument` 0 · missing script 0,
  `MainHudUgui`의 `UiPanel` 열한 칸 전부 연결 유지, `Overlays` 자식 17개 그대로,
  HUD 버튼 10개 라벨 정상. Play 12초 예외 0(`NoSubscription` 한 줄은 늘 있던 것).
  게임 뷰 스크린샷에서 한글·접속 보상·오프라인 보상·튜토리얼 배너 다 정상으로 보였다.
  `Core.Tests` **통과 401 / 실패 0**. `GemRacer/7`은 누르지 않았다.
- **덤으로 눈에 띈 것**: 스크린샷에서 B-02가 그대로 보인다 — `btn-mine`이 "업그…", `btn-seasonpass`가
  "시즌패스"로 잘려 있다. 이건 여전히 Tifania 결정 대기라 손대지 않았다.
- main 승격은 건드리지 않았다(정책상 Tifania만 할 수 있고, 대상 갱신은 아침 정리 세션 몫).

### 오늘 웹에서 확인할 것 (dev 주소) — 한 가지 더

`https://dev.planetracer-daz.pages.dev` 에서 **HUD가 제대로 뜨는지**를 같이 봐 주면 좋겠다.
B-01(배포본에서 HUD가 안 뜨던 문제)이 옛 UI Toolkit UIDocument 열 개를 원인으로 지목했었는데,
이번에 그게 씬에서도 파일에서도 완전히 없어졌다. 이번 배포에서 B-01이 같이 사라졌는지
여기서 처음 확인된다.

- 이어서 할 것: 남은 Unity 세션 몫은 **B-02**(Tifania 결정 먼저)와 **M-05**(안드로이드 실기 알림)뿐이다.
  U-08이 닫혔으니 U 계열은 U-11 아래 "펫 도감 가로 열 수" 한 줄만 남았다(급하지 않다).

### (01:4x 덧붙임) WebGL 빌드 성공 — 삭제가 실제 빌드에서도 안전했다

GitHub Actions **run #547 (`997c14c`) completed/success** — 목록 아이콘이 아니라 API로 잡 단계까지
확인했다(`game-ci/unity-builder`, 배포 확인, 별칭 주소 확인, 로딩 시간 예산까지 **전 단계 success**).
에디터 컴파일과 별개로 **WebGL 빌드가 실제 컴파일러**라, 75개를 지운 뒤 이 빌드가 통과한 것으로
U-08 삭제가 빌드에서도 안전한 것이 확인됐다. `dev.planetracer-daz.pages.dev`에 자동 배포됐다.

브라우저로 직접 눌러 보는 확인은 하지 않았다 — CLAUDE.md에 적힌 대로 이 프로젝트의 WebGL
페이지는 자동 클릭으로 UI를 넘기기 어려워서, 에디터 Play 모드 확인으로 대신하는 편이 낫다
(위 Play 12초 + 스크린샷). 그리고 이 시간에 Tifania의 Chrome을 띄울 일도 아니다.

## 02:0x 주말 세션 — Core.Tests 보강 (여전히 code-only 신규 항목 바닥, 00:0x와 같은 방법으로 한 곳 더 찾음)

- **시작**: `origin/claude/dev` 마지막 커밋이 21분 전(경계 근처)이라 진행. `docs/feedback.md`엔
  `- [ ]` 없음(둘 다 `- [?]`). backlog 위쪽(A-17~19·U-08·M-05·M-13·P-06~08·B-02 등)도 전부
  Unity 에디터 필요 또는 Tifania 결정 대기로 이미 막힌 그대로였다 — 00:0x·01:0x 두 세션과
  같은 결론.
- **00:0x 세션이 남긴 "이어서 할 것"(다른 `Runtime/*.cs` 상수표류를 더 보라)을 따라갔다.**
  `DailyLoginReward.cs`를 보니 `RawMineralsByStreakDay`(7일 주기 보상표)를 `% CycleLength`로
  순환 인덱싱하는데, **배열 길이가 `CycleLength`와 같다는 걸 직접 확인하는 테스트가 없었다** —
  기존 테스트는 다 개별 값(1일차·7일차·순환·방어)만 봐서 이 둘이 어긋나는 경우(배열만 늘리면
  뒤쪽 값이 죽고, `CycleLength`만 늘리면 예외)는 안 걸렸다. `SubscriptionDailyGrant.cs`도 같이
  봤는데 거긴 배열 없이 상수 하나뿐이라 해당 없음.
  `Core.Tests` 1개 추가(길이 일치 확인) — **401 → 402, 실패 0**. 순수 C# 테스트 파일만 수정,
  `.meta` 없음, Unity 세션 불필요, 웹에서 볼 변화 없음.
- **이어서 할 것**: 여전히 code-only 신규 항목이 바닥나 있다(세 세션째 같은 결론). 이 "카탈로그
  길이/순환 정합성" 패턴으로 훑을 만한 상수표가 이제 거의 안 남았다 — 다음 세션은 (1)
  `docs/decisions.md` 열 항목 중 A/B/C 요약을 더 짧게 다듬는 쪽, (2) 아니면 backlog 맨 아래
  "다음에 할 만한 것"에 후보가 쌓이는지 볼 것(직접 시작하지 않음).
- main 승격은 건드리지 않았다(정책상 Tifania만 할 수 있고, 대상 갱신은 아침 정리 세션 몫).
## 02:0x 이미지 세션 — 0장 (대기 중 비어 있음, A-15 미개시)

- **0절 1번(시각)**: 02:08 KST — 20:00~08:00 안이라 통과.
- **판정 전에 로컬을 원격에 맞췄다.** `git fetch`로 보니 로컬 `58bbfd8`이 `origin/claude/dev`
  `4f7d198`보다 1커밋 뒤였다. 09-26 06시 세션이 뒤처진 로컬로 A-20/A-15를 잘못 읽은 전례가
  있어, PowerShell에서 `clear_git_locks.ps1` → `git pull --rebase`로 맞춘 뒤에 읽었다.
- **0절 4번(대기 중)**: `docs/design/art-requests.md`의 "대기 중"은 `<!-- 여기에 추가 -->`와
  09-23 이후의 기록 글뿐 — 실제 항목 0개.
- **0절 4번 판정**: `docs/backlog.md:309` **A-15가 `- [ ]`** 그대로다. 절차 문서대로 남은
  방아쇠는 A-15 하나뿐이고 그것이 미개시이므로 대기 중이 비어 있는 것이 맞다 — 설계 문서를
  다시 훑지 않고 여기서 끝낸다. (A-20은 `- [x]`지만 새 아트 요청이 안 나왔으니 신호가 아니다.)
- **MISSING 한 줄 검사도 돌렸다.** 나온 줄은 전부 알려진 오탐 두 종류뿐 — 옛 이름
  `pet-t1~t4-*`·`pet-t7-*`(등급 폴더에 124장 다 있음)와 접두사만 잘린 `arrive-`·
  `icon-grade-`·`planet-`·`pet-t`. **새로 빠진 그림은 없다.**
- **0절 5번(앞 세션 이미지 한도)**: 해당 없음 — 09-26·09-27 daily에 `이미지 한도 — N시간 후
  풀림` 줄 없음. 한도는 온전하다.
- 1절 캡처 준비는 돌리지 않았다 — 0절에서 끝나는 세션이라 화면을 볼 일이 없고, ChatGPT 앱도
  띄우지 않았다(유휴 중인 PC에 불필요한 입력을 주지 않는다).
- **결과: 0장.** 09-23 20시 이후 열네 번째 0장 세션이고, 고장이 아니라 정상이다.
  다음에 아트가 필요해지는 시점은 A-15(출시 준비)뿐이다.

## 03:0x 주말 세션 — P-19 발견 (CLAUDE.md 규칙 넷, 세 세션 연속 바닥난 뒤)

- **시작**: `origin/claude/dev` 마지막 커밋이 52분 전이라 안 겹침. `docs/feedback.md`엔 `- [ ]`
  없음(둘 다 `- [?]`). backlog 위쪽도 00:0x·01:0x·02:0x 세 세션이 이미 확인한 그대로 Unity
  에디터 필요 또는 Tifania 결정 대기로 막혀 있었다 — **세 세션 연속 "바닥났다"**라 CLAUDE.md
  규칙 넷대로 설계 문서(`docs/design/`)를 다시 훑어 아직 backlog에 없는 것을 찾아봤다.
- **`pet-gacha.md` 8절 "코드에 만들어야 하는 것"의 마지막 줄("MiningSimulator·레이스 스탯에
  펫 보너스 반영")을 실제 코드와 대조하다가 진짜 구멍을 찾았다.** 2절 표(도감 보너스 마리당
  +1~+35%, 장착 보너스 +8~+55%)와 코어 함수 `PetCollection.CollectionBonus`/`EquipBonus`는
  다 있고 테스트도 통과하는데(A-17, 09-19~21 세션들), **이 둘을 부르는 곳이 코드 전체에
  0군데였다**(`grep -rn "PetCollection\." PlanetRacer/Assets` 0건). A-17이 "코드 쪽은 전부
  끝났다"고 닫은 뒤에도 이 자리는 한 번도 안 걸렸다 — 뽑기·합성·도감·장착 화면은 다 되는데
  장착해도 실제로는 아무 스탯도 안 바뀐다는 뜻.
- **바로 못 고친 이유** — 이 %가 어느 스탯(채굴/정제/화물칸/레이스 속도 등)에 붙는지가
  `pet-gacha.md` 어디에도 안 적혀 있다. `docs/backlog.md`에 **P-19**로 올리고, `docs/decisions.md`
  TOC(열→열하나)와 "펫 보너스가 아무 데도 안 붙어 있다" 절(A/B/C안)에 정해 주실 것을 적었다.
  A안(1~5등급 전부 채굴 산출에 곱함, `Entitlements.MiningYieldMultiplier`와 같은 자리)이 제일
  작다. 급하지 않음(A-17 뽑기가 아직 상점에 안 열려 있음) — 다만 정하지 않으면 출시 때 "장착해도
  효과 없는 수집 화면"이 그대로 나간다.
- 코드는 안 건드렸다(문서만) — `Core.Tests && dotnet run` **402 통과 / 실패 0**(회귀 확인, 컨테이너에
  dotnet 없어서 `apt-get install -y dotnet-sdk-8.0`부터 설치, 매번 반복되는 패턴).
- **이번 건은 웹에 안 보임**(문서 추가뿐, 게임 동작 변화 없음).
- main 승격은 건드리지 않았다(정책상 Tifania만 할 수 있고, 대상 갱신은 아침 정리 세션 몫).
- 이어서 할 것: P-19가 Tifania 결정 대기로 넘어갔으니, 다음 세션은 (1) P-19 A안이 정해지면
  바로 배선(코어 함수 하나 + `MiningController` 호출 한 줄), (2) 여전히 안 정해졌으면 규칙 넷을
  한 번 더 — 이번처럼 "설계 문서가 요구하는데 코드가 안 부르는 자리"를 다른 design 문서
  (`core-loop.md`·`planet-progression.md` 6·7절 등)에서 더 찾아볼 것.

## 04:0x 주말 세션 — P-07 창고 이관 시점도 판단 대기임을 발견 (03:0x 세션이 남긴 방법 그대로 한 곳 더)

- **시작**: `origin/claude/dev` 마지막 커밋(03:0x P-19 발견)이 54분 전이라 안 겹침. `docs/feedback.md`
  `- [ ]` 없음(`- [?]` 두 줄만, 둘 다 Tifania 판단 대기). backlog 위쪽도 앞 세션들이 이미 확인한
  대로 Unity 에디터 필요 또는 Tifania 결정 대기로 막혀 있었다. 03:0x 세션의 "이어서 할 것"대로
  design 문서 쪽에서 "설계가 요구하는데 코드가 안 부르는 자리"를 한 번 더 찾다가 `PlanetMineralBank`가
  Assets 어디서도 안 불리는 것(P-19와 같은 grep 방식, 0건)을 확인했다.
- **다만 이건 P-19처럼 "발견되지 않은 구멍"이 아니라 이미 알려진 채 방치된 구멍이었다** —
  `MiningController.cs` 334줄 주석, `docs/design/planet-progression.md` 2.1절 모두 "언제 옮길지
  안 정했다"고 이미 적어 뒀고, P-07 backlog 항목도 "창고 연결은 Unity 세션 몫"이라고 적어
  뒀었다. 그런데 그 유예 이유를 읽어 보니 "실제 UI 흐름(P-09, 행성 이동 화면)과 같이 정해야
  의미가 있다"였고, **P-09는 2026-09-25 07시 Unity 배선 세션에서 이미 끝나 있었다** — 전제가
  사라진 채 "Unity 세션 몫"이라는 딱지만 backlog에 남아 다음 세션들이 계속 건너뛰고 있었던
  것이다(오늘 00:0x·01:0x·02:0x·03:0x 네 세션도 이 항목을 훑고 지나갔지만 이유까지 파고들진
  않았다).
- **`docs/decisions.md`에 "정제 광물을 행성별 창고로 언제 옮길지" 절을 새로 만들었다** — A(정제되는
  즉시, `MiningController.cs` 239줄 옆에 한 줄)/B(행성을 떠날 때, 도착 시점 스냅샷 필드 필요)/
  C(레이스 우승 시점, 근거 약함) 세 안. A안이 가장 작다고 적었다. 목차("지금 판단 대기 중인 것")를
  "열하나"→"열둘"로 갱신. `docs/backlog.md` P-07과 `docs/design/planet-progression.md` 2.1절
  양쪽에 "P-09가 끝나 전제가 사라졌다"는 정정을 남겨 다음 세션이 같은 딱지를 또 안 믿게 했다
  (CLAUDE.md 7번 "문서가 실제와 어긋나면 고친다").
- 코드는 안 건드림(문서만) — `Core.Tests`는 core 변경이 없어 이번엔 안 돌렸다(400대에서 그대로).
  **이번 건은 웹에 안 보임.**
- main 승격은 건드리지 않았다(정책상 Tifania만 할 수 있고, 대상 갱신은 아침 정리 세션 몫).
- 이어서 할 것: P-07(창고 이관 시점)·P-16(시즌 패스 인장)·P-19(펫 보너스) 세 개가 전부
  Tifania 결정 대기로 쌓였다 — 뭐든 하나가 정해지면 다음 코딩 세션이 바로 끝낼 수 있는 작은
  일들이다. 여전히 안 정해졌으면, "이유를 읽어 보니 전제가 이미 사라진 Unity 세션 몫 딱지"가
  이번에 효과적이었으니 같은 방식으로 backlog의 다른 "Unity 세션 몫" 항목(M-05·B-02 등)도
  전제가 아직 유효한지 한 번씩 확인해 볼 것.
