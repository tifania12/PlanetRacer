# 보석 행성 레이서 (Gem Planet Racer) — 작업 규칙

방치형 레이싱 게임. 보석 이름의 행성이 스테이지. 채굴차가 방치 채굴 → 광물로 레이싱카 부품 제작 → 행성 그랑프리 1등 → 다음 행성.
모바일(iOS/Android) + Steam 동시 출시. 목표 2027-02-16. 기획 요약은 `docs/GDD.md`, 전체는 Claude 아티팩트 "보석 행성 레이서 기획서".

## 저장소 구조

```
PlanetRacer/                Unity 6 프로젝트 (6000.3.10f1, URP). PC에서 저장소 루트는 E:\Unity\PlanetRacer
  Packages/com.bax.gemracer.core/Runtime/   순수 C# 코어. UnityEngine 참조 금지 (asmdef noEngineReferences)
  Assets/Scripts/           MonoBehaviour, UI, 연출. 코어를 호출만 한다
  Assets/Editor/            에디터 부트스트랩 (씬·프리팹·머티리얼을 코드로 생성하는 MenuItem)
  Assets/Screenshots/       검증용. .gitignore에 있으니 커밋되지 않는다
Core.Tests/                 dotnet 콘솔 테스트 러너. `cd Core.Tests && dotnet run`. NuGet 사용 금지(오프라인)
docs/GDD.md                 기획 요약
docs/backlog.md             일 단위 작업 목록. 야간·새벽 세션은 여기서 다음 항목을 집는다
docs/daily/YYYY-MM-DD.md    그날 작업 보고 + 아침 테스트 가이드
docs/decisions.md           결정 기록
docs/feedback.md            Tifania 피드백. 모든 세션이 제일 먼저 읽는다
.github/workflows/webgl.yml 푸시되면 WebGL 빌드 후 Cloudflare Pages 배포
web/_headers                Brotli 압축 헤더. 이게 없으면 웹에서 안 뜬다
tools/deploy_web.ps1        CI가 막혔을 때 PC에서 직접 빌드·배포
```

## 반드시 지킬 것

1. 게임 규칙·수식은 전부 `PlanetRacer/Packages/com.bax.gemracer.core/Runtime`에 순수 C#으로 쓴다. 서버가 같은 코드로 대전 결과를 검증하므로 코어에서 `UnityEngine`, `Random`, `DateTime.Now`를 쓰지 않는다. 난수는 `DeterministicRandom`, 시간은 인자로 받는다.
2. 코어를 고치면 `Core.Tests/Program.cs`에 테스트를 추가하고 `dotnet run`이 `실패 0`으로 끝나는지 확인한다. 실패한 채로 커밋하지 않는다.
3. 씬·프리팹·머티리얼은 `.unity`/`.prefab` YAML을 손으로 쓰지 말고, `Assets/Editor/Bootstrap*.cs`에 `[MenuItem("GemRacer/...")]`로 생성 코드를 쓴다. 같은 메뉴를 다시 눌러도 같은 결과가 나오게(멱등) 만든다.
4. `.meta` 파일은 손으로 쓰지 않는다. Unity가 생성한 뒤에는 반드시 커밋한다 — `.meta`가 빠지면 다른 사람이 열 때 참조가 전부 끊긴다. 이미 있는 `.meta`는 지우거나 GUID를 바꾸지 않는다.
5. 시간에 비례해야 하는 것(카메라 추적, 이동, 보간)은 프레임 수에 의존하면 안 된다. 프레임당 고정 계수 `Lerp(a, b, 0.15f)` 대신 `1 - Mathf.Exp(-k * Time.deltaTime)`를 쓴다. 모바일 30fps와 PC 고주사율에서 감이 달라지면 안 된다.
6. Unity 6 LTS, URP, UI Toolkit, Input System. 입력은 탭/클릭만. UI는 **반응형 한 벌**이다 — 세로(9:16)를 기준으로 만들고, 가로가 넓어지면 한 칸짜리를 두 칸으로 재배치한다. 화면 구성과 정보는 같고 배치만 바뀐다. 가로 전용 화면을 따로 만들지 않는다. 기준점은 세로 540×960, 가로 960×540, 태블릿 1280×800 세 가지이고 이 셋에서 다 읽혀야 한다.
7. 한국어로 쓴다. 코드 주석·문서·커밋 메시지 모두. 문장은 사람이 말하듯 자연스럽게.
8. 하나의 작업 = 하나의 커밋. 메시지 첫 줄은 `[D07-N] 채굴차 자동 주행 연출` 형식 (backlog 항목 ID).

## Unity 에디터에 닿을 수 있는지는 세션마다 다르다

- **예약 세션(야간 23:00 / 새벽 06:00)**: 클라우드에서만 돈다. Unity 에디터가 없다. 그래서 씬은 부트스트랩 코드로만 만들고, 컴파일 여부를 확인할 수 없으니 확실하지 않은 API는 쓰지 말고 daily 파일에 "아침 확인 필요"로 남긴다. 검증은 `Core.Tests`의 `dotnet run`까지다.
- **클라우드 세션은 배포 주소(`*.pages.dev`)에도 못 나간다(2026-09-14 확인).** 조직 egress 정책으로 curl·WebFetch·헤드리스 브라우저 전부 `planetracer-daz.pages.dev` 도메인에서 403(정책 차단)을 받는다 — 프록시 문제가 아니라 매번 그렇다. 그래서 "배포 후 실제로 열어서 확인" 같은 피드백 항목은 클라우드 세션이 대신 처리할 수 없다 — GitHub Actions 실행 로그(빌드 성공 여부, "배포 확인" 스텝 결과)까지만 보고, 눈으로 보는 확인은 daily 파일에 남겨서 Tifania의 아침 확인으로 넘긴다. 매번 새로 시도하며 시간 쓰지 말 것. `dotnet install` 스크립트(`dot.net`, `builds.dotnet.microsoft.com`)도 같은 정책으로 막힌다 — 대신 `apt-get install -y dotnet-sdk-8.0`은 된다(아래 항목).
- **클라우드 세션 컨테이너에 `dotnet`이 미리 깔려 있지 않을 수 있다(2026-09-14 확인).** `dotnet: command not found`가 뜨면 `apt-get update && apt-get install -y dotnet-sdk-8.0`로 설치한다 — apt 저장소 자체는 열려 있어서(오프라인인 건 NuGet 패키지 복원뿐) 이 설치는 된다. 세션마다 컨테이너가 새로 뜨는 것으로 보이니 다음 세션도 똑같이 없을 수 있다 — `dotnet run`이 command not found로 실패하면 먼저 이 설치부터 시도해 볼 것("검증을 건너뛰지 않는다" 원칙과 안 어긋난다, 설치 자체는 시간이 얼마 안 걸린다).
- **Tifania와 같이 하는 대화형 세션**: PC가 연결돼 있으면 Unity MCP(`unityMCP__*`)로 에디터를 직접 쓸 수 있다. 이때는 실제로 확인하고 나서 보고한다 — `refresh_unity`로 컴파일 → `read_console`로 에러 확인 → `execute_menu_item`으로 부트스트랩 실행 → `manage_editor play` + `manage_camera screenshot`으로 눈으로 확인.
- 주의: `manage_scene get_hierarchy`는 플레이 중 값이 갱신되지 않을 때가 있다. 실행 중 좌표를 정확히 볼 때는 `execute_code`로 직접 읽는다.

## 세션마다 제일 먼저 하는 일

`git pull` 다음에 `docs/feedback.md`를 연다. `- [ ]` 로 시작하는 줄이 있으면 **backlog보다 먼저** 처리한다.
처리한 줄은 `- [x] ... → 해시 한 줄 설명` 으로 바꾸고, 판단이 필요해 못 한 줄은 `- [?]` 로 바꾸고 무엇이 막혔는지 적는다.
피드백이 비어 있을 때만 backlog로 넘어간다.

## 평일(월~금) 일과

| 시각 (KST) | 성격 | 하는 일 |
|---|---|---|
| 18:00 ~ 08:00 | 매시간 | backlog 1~2항목. 알림은 꺼져 있다 |
| 06:30 | 작게 | 새 기능 금지. 검증·정리·아침 가이드만. 밤새 결과를 모아 알림 한 번 |
| 08:00 ~ 18:00 | 안 돎 | Tifania가 회사에서 Claude를 쓰는 시간. 그 몫을 남긴다 |

**밤에 매시간 도는 세션** — 하룻밤에 15번이므로 한 번에 욕심내지 않는다. 한 차례에 1~2항목,
구현 → `dotnet run` → 부트스트랩 코드 → 커밋 → push까지 끝낸다. 막히면 다음 항목으로 넘어가되
무엇에 막혔는지 daily 파일에 남긴다. 시간을 아끼려고 검증을 건너뛰지 않는다.
시작할 때 최근 커밋이 20분 이내면 앞 세션이 도는 중이니 이번 차례는 건너뛴다.

**작게 도는 세션(06:30)** — Tifania가 곧 출근하고 낮에는 회사 일에 Claude를 써야 하므로 여기서 사용량을 아낀다.
`dotnet run` 통과 확인, 웹 배포가 성공했는지 확인, `docs/daily` 파일의 아침 가이드가 실제 파일 경로·메뉴 이름과 맞는지 대조,
그리고 5줄 요약을 마지막 메시지로 남긴다. 새 기능을 건드리지 않는다.

## 주말(토·일) 일과

회사 일이 없는 날이라 더 많이 돈다. **매시간 한 번씩, 하루 24번** 돈다(2026-09-13 세션 예약이
09:00/14:00/19:00/23:00 네 번에서 매시간으로 바뀌었다 — 예전엔 하루 네 번, 한 번에 3~4항목씩
크게 갔지만 지금은 매시간이라 그렇게 하면 겹치거나 과하다. **한 번에 backlog 1~2항목만** 집는다).
평일과 다른 점은 **하루 안에 되돌아볼 수 있다**는 것이다. 그래서 주말에는 평일에 못 하는 종류의 일을
전체 흐름 어딘가에 끼워 넣는다 — 아래 시각은 하루 흐름을 보여주는 대략적 기준일 뿐, 매 시각마다
정확히 그 일만 하는 건 아니다.

- 오전 — 밀린 `- [?]` 피드백과 막힌 항목부터. 판단이 필요한 것을 정리해 `docs/decisions.md`에 선택지로 올린다.
- 낮·오후 — backlog 위에서부터 1~2항목. 큰 구조 변경이나 리팩터링도 여기서 하되 한 시간 안에 끝날 만큼만.
- 밤 — 밸런스 시뮬레이션, 테스트 보강, 문서 정리처럼 오래 걸리지만 나눠서 해도 되는 일. 다음 날 아침 가이드 작성.

세션마다 앞 세션이 아직 도는지(최근 커밋이 20분 이내인지) 먼저 확인하고, 돌고 있으면 이번 차례는
건너뛴다 — 매시간 도니 겹치는 것보다 한 번 쉬는 게 낫다.

## 이미지는 GPT에서 만든다 — 세션은 프롬프트만 남긴다 (2026-09-15)

Tifania가 ChatGPT Plus를 쓰고 **이미지 생성은 거기서 한다.** 밤 20~08시에 도는 이미지 세션이
PC의 ChatGPT 데스크탑 앱을 Windows-MCP로 직접 조작해 뽑는다(2026-09-16부터 실제로 작동).

그러니 이미지가 필요해지면 **직접 만들려 하지 말고 `docs/design/art-requests.md`에 프롬프트를 적는다.**
영문 프롬프트로, GPT에 그대로 붙여 넣을 수 있게. 들어간 자리는 `Assets/Art/`가 아니라
**`Assets/Resources/Art/`** 다 — 아트 확인 화면(`?art=1`)이 런타임에 긁으려면 `Resources` 아래여야 한다.
daily 파일에 "이미지 요청 N건 올림" 한 줄을 남겨 아침에 보이게 한다.

**배경은 처음부터 투명으로 받는다 (2026-09-16).** 전에는 마젠타로 받아 `tools/strip_bg.py`로
키잉했는데, 프롬프트에 `real alpha channel` + `no background color`를 넣으면 GPT가 진짜 RGBA PNG를
준다. 키잉은 테두리에 분홍 잔상을 남기니 더 쓰지 않는다. 받은 파일은 넣기 전에
`python tools/check_alpha.py <파일>`을 통과해야 한다(RGBA · 모서리 투명 · 투명 10~95% · 잔상 없음).
`strip_bg.py`는 배경이 붙어 온 그림용으로 남겨 뒀다.

**이미지가 없다고 작업을 멈추지 않는다.** 자리 표시자(단색 사각형, 기본 스프라이트)로 먼저 붙여
동작을 확인하고, 실제 이미지는 나중에 갈아 끼운다. 지금 HUD 버튼도 유니티 기본 스프라이트를 쓴다.

Kling AI 커넥터가 붙어 있긴 하지만 그건 영상 쪽이고, 게임 아트의 기본 경로는 GPT다.

## UI는 이제 일반 UI(uGUI)다 — 새 화면을 UI Toolkit으로 만들지 않는다 (2026-09-15)

Tifania가 UI Toolkit에 익숙하지 않아 직접 고치기 어렵다고 해서 옮기는 중이다.
**새 화면은 처음부터 uGUI로 만든다.** UI Toolkit(UXML/USS/UIDocument)으로 새로 만들면
나중에 옮길 것만 늘어난다 — 실제로 상점 화면(M-07)이 이사 결정 직전에 UI Toolkit으로
만들어져서 U-10으로 다시 옮기게 됐다.

방법과 변환표는 `docs/design/ugui-migration.md`. 본보기는 `MainHudUgui.cs` + `BootstrapHudUgui.cs`.
한글은 `Assets/Fonts/Pretendard-Regular SDF.asset`(TMP 기본 폰트로 지정돼 있다)을 쓴다 —
유니티 기본 폰트에는 한글 글리프가 없어서 빌드에서 글자가 통째로 사라진다.

**에디터 없는 세션은 코드까지만 한다.** 메뉴를 실행해 씬에 계층을 세우는 건 Unity가 있어야 한다.
거기까지 했으면 backlog를 `- [?] 씬 배선은 Unity 세션 필요`로 남기고 daily에도 한 줄 적는다.

## `GemRacer/7`을 다시 누르면 손으로 배선한 uGUI 화면이 전부 날아간다 (2026-09-18)

함정이라 크게 적는다. `GemRacer/7. 메인 게임 씬 만들기`는 **빈 씬에서 새로 만든다**
(`EditorSceneManager.NewScene(..., NewSceneMode.Single)`). 그런데 지금 `MainGame.unity`에 들어 있는
uGUI 화면 일곱 개(제작·레이스 출전·공구 상자·설정·상점·오프라인 보상·화물칸 가득)는 9/15~9/18에
**각자의 `Bootstrap*Ugui` 메뉴를 씬에 대고 따로 눌러서** 쌓아 올린 것이라 `GemRacer/7` 안에 없다.
그래서 이 메뉴를 다시 누르면 씬이 43,858줄 → 5,691줄로 줄면서 그 일곱 화면이 통째로 사라진다
(2026-09-18 저녁 세션에서 실제로 겪고 `git checkout HEAD~1 -- ...`로 되돌렸다).

**그러니 이미 배선된 MainGame 씬에 뭘 더할 때는 `GemRacer/7`을 누르지 않는다.**
씬을 연 채로 `execute_code`로 필요한 오브젝트만 더하고 저장한다. 부트스트랩 코드(`BootstrapMainGame.cs`)에도
같은 내용을 같이 넣어 두면(다음에 밑바닥부터 새로 만들 때를 위해) 앞뒤가 맞는다 — D06-N의 VeinField가 그 예다.

언젠가 `GemRacer/7`이 일곱 화면 부트스트랩을 전부 호출하도록 고치면 이 함정은 사라진다.
그때까지는 위 규칙이 유효하다.

## 공휴일 — 주말과 똑같이 (2026-09-14 추가)

한국 공휴일에는 Tifania가 회사에 가지 않는다. 그러니 **평일이어도 공휴일이면 위의 평일 일과가 아니라
"주말(토·일) 일과"를 따른다.** 낮에도 매시간 돌고, 낮 시간 사용량을 아낄 이유도 없다.

날짜 목록은 `docs/holidays.md`에 있다. 판단은 **KST 날짜**로 한다 — 세션은 UTC로 돌기 때문에
`TZ=Asia/Seoul date +%F` 로 확인하고 나서 목록과 맞춰 본다. UTC 날짜로 보면 하루씩 어긋난다.

공휴일 낮(KST 08:00~17:00)은 따로 예약된 루틴이 채운다. 그 루틴은 매일 도는 대신 프롬프트 안에서
"오늘이 공휴일인가"를 먼저 보고, 아니면 아무것도 하지 않고 바로 끝낸다. 그래서 평일에 그 루틴이
"오늘은 공휴일이 아니라 건너뛴다" 한 줄만 남기고 끝나는 것은 정상이다. 고장이 아니다.

임시공휴일이 새로 생기면 `docs/holidays.md`와 그 루틴 프롬프트 **양쪽**을 고쳐야 한다.

## main 승격 — 검증 통과하면 자동 (2026-09-14 결정)

작업은 `claude/dev`에 쌓지만, 검증이 통과하면 세션이 직접 `main`에 올린다.
매시간 세션이 도는 구조에서 수동 병합은 사람이 병목이 되기 때문이다.

**세 가지를 전부 만족할 때만 승격한다.**

1. `cd Core.Tests && dotnet run` 이 "실패 0"으로 끝났다
2. 직전 GitHub Actions WebGL 빌드가 **성공**했다. 실패했거나 아직 도는 중이면 승격하지 않는다
3. `claude/dev`가 `main`보다 앞서 있고 fast-forward가 된다

```
git push origin claude/dev:main
```

`--force`는 어떤 경우에도 쓰지 않는다. fast-forward가 안 되면(main에 직접 커밋이 있으면)
승격하지 말고 daily에 "main 승격 보류: fast-forward 불가"라고 적는다.
승격했으면 daily에 "main 승격: <해시>" 한 줄.

## 수익화 — 넘지 않는 선 (2026-09-14 확정)

상세는 `docs/design/monetization.md`. 코드로 옮길 때 이 두 줄만은 반드시 지킨다.

- **시간은 팔고 힘은 팔지 않는다.** 레이싱카 부품, 채굴차 부품, 공구 상자, 상자 열쇠,
  청사진, 연료, 대전권은 어떤 경로로도 판매하지 않는다. 확률형 유료 뽑기도 없다.
  시즌 패스 유료 트랙에도 전투력 보상을 넣지 않는다.
- **화물칸 상한에는 무료 해법이 항상 살아 있어야 한다.** 정제 광물은 화물칸을 차지하지 않고,
  자동 제련소(레이스 보상)를 올리면 실질 상한이 오른다. 돈을 안 써도 풀리는 길을 막으면
  이 게임의 구조 자체가 무너진다.

## 빌드 설정은 ProjectSettings.asset에 넣는다 — ApplySettings는 CI가 안 부른다 (2026-09-14)

함정이라 크게 적는다. `.github/workflows/webgl.yml`에는 `buildMethod`가 없다. 그래서 CI는
game-ci의 기본 빌더로 돌고 **`WebGLBuild.ApplySettings()`를 부르지 않는다.**
그 함수에 아무리 써 놔도 CI 빌드에는 반영되지 않는다. 로컬에서 `GemRacer/웹 빌드 (WebGL)`로
뽑을 때만 먹는다.

**그래서 빌드에 실제로 반영되어야 하는 값은 반드시 `ProjectSettings.asset`에 들어가 있어야 한다.**
방법은 둘 중 하나다.

- Unity 에디터에 붙을 수 있으면 `PlayerSettings.*`로 바꾸고 `File > Save Project`까지 해서
  `ProjectSettings.asset`이 실제로 갱신된 것을 확인한다(에디터 메모리만 바뀌고 파일은 그대로인 일이 잦다).
- 에디터가 없으면 `ProjectSettings.asset`을 직접 고친다. YAML 한 줄짜리 값은 이게 더 확실하다.

`ApplySettings()`는 로컬 빌드용 + 문서용으로 남겨 두되, 고칠 때 **양쪽을 같이** 고친다.

## 웹 빌드 — 아침에 휴대폰으로 확인하는 경로

`main`이나 `claude/dev`에 푸시되면 GitHub Actions가 WebGL을 빌드해 Cloudflare Pages에 올린다.
Tifania는 Unity를 켜지 않고 주소만 열어서 확인한다. 그래서 **푸시 전에 이것부터 확인한다**.

- 확인할 화면이 `Assets/Scenes` 안에 있고 Build Settings에 켜져 있는가. 부트스트랩으로만 만들어지고 저장되지 않는 씬은 웹에서 안 보인다.
- 웹에서 볼 것이 없는 작업(코어 수식, 테스트)이면 daily 파일에 "이번 건은 웹에 안 보임"이라고 적는다. 괜히 열어 보게 만들지 않는다.
- 빌드 실패는 다음 세션이 최우선으로 고친다. 배포가 멈추면 아침 확인 자체가 사라진다.

## 노션 현황판 — 하루 한 번 갱신 (2026-09-14 추가)

Tifania가 매일 보는 페이지가 노션에 있다. Notion 커넥터로 접근한다.

    페이지: 보석 행성 레이서 — 개발 현황판
    https://app.notion.com/p/3db7777608f8817b9f75ffc12200266f

**그날 첫 세션만 갱신한다.** 먼저 `notion-fetch`로 페이지를 열어 마지막 수정 시각을 본다.
오늘(KST) 이미 갱신됐으면 건드리지 않는다. 하루 15번 도는 구조라 매번 쓰면 의미 없는 변경만 쌓인다.

갱신하는 것은 네 군데뿐이다. 나머지 문단은 손대지 않는다.

1. **진행률** — `docs/backlog.md`의 `- [x]`와 `- [ ]` 개수를 세서 "NN% — 전체개 중 완료개 완료"와 기준 날짜
2. **앞으로 할 것** — 체크 안 된 항목 중 위에서부터. 이미 끝난 줄은 지운다
3. **지금까지 된 것** — 새로 끝난 굵직한 항목이 있으면 해당 토글에 한 줄 추가
4. **Tifania가 정해야 할 것** — `docs/decisions.md`에 답을 기다리는 항목이 있으면 여기에 A안/B안을 짧게. 없으면 "지금은 없습니다"를 유지

`notion-update-page`의 `update_content`로 해당 부분만 바꾼다. `replace_content`는 쓰지 않는다 —
Tifania가 직접 적어 둔 메모를 지울 수 있다.

문구는 사람이 말하듯 쓴다. 항목 ID(M-01 같은 것)는 남기되, 설명은 backlog의 기술적인 문장을
그대로 옮기지 말고 "이게 왜 필요한지" 한 줄로 바꿔 쓴다. 저장소를 안 보는 사람이 읽는 페이지다.

## Tifania가 아침에 하는 일 (5분)

1. 휴대폰으로 배포 주소를 연다. daily 파일의 확인 포인트를 보면서 한 줄씩 본다.
2. 이상한 것이 있으면 이 대화창에 말하거나 `docs/feedback.md`에 `- [ ]` 로 한 줄 적는다.
3. 끝. Unity를 켜는 것은 직접 만져 보고 싶을 때만 한다.

## 빌드 결과는 목록 아이콘 말고 API로 본다 (2026-09-16)

GitHub Actions 목록 페이지는 **취소된 실행이 실패처럼 보인다**(짧게 끝나서). 실제로 세 번 잘못 읽었다.
저장소가 공개라 토큰 없이 API로 결론을 바로 읽을 수 있다. PC에서 한 줄이면 된다:

    Invoke-RestMethod "https://api.github.com/repos/tifania12/PlanetRacer/actions/runs?branch=claude/dev&per_page=6" |
      ForEach-Object { $_.workflow_runs } |
      ForEach-Object { "$($_.run_number) $($_.head_sha.Substring(0,7)) $($_.status) $($_.conclusion)" }

`status`가 `completed`이고 `conclusion`이 `success`인 것만 성공이다. `cancelled`는 실패가 아니다.
클라우드 쪽 `Bash`에서는 같은 주소가 403(정책)이라 **PC의 PowerShell로** 불러야 한다.

## main 승격은 Tifania만 할 수 있다 (2026-09-16 확인)

`origin/claude/dev` → `main` 푸시는 세션 실행 환경의 "Production Deploy" 정책에 막힌다.
예약 세션만이 아니라 **Tifania가 옆에 있는 대화형 세션에서도 똑같이 막힌다.** 우회하지 않는다.
세션이 할 일은 조건 세 개를 확인하고 daily에 "승격 준비됨 + 대상 커밋"을 적어 두는 것까지다.

## 확인용 주소가 따로 있다 — 승격 전에 여기서 본다 (2026-09-17)

    배포 주소 (main)   https://planetracer-daz.pages.dev
    확인 주소 (dev)    https://dev.planetracer-daz.pages.dev   ← claude/dev 가 자동 배포된다

`claude/dev`에 푸시되면 `webgl.yml`이 `--branch=dev`로 Cloudflare Pages에 올린다.
**승격도 승인도 필요 없고, 배포 주소(main)는 건드리지 않는다.** 그런데 이 주소가
어느 문서에도 안 적혀 있어서 그동안 아무도 안 봤다 — 밤 세션들이 아침 가이드에
"승격해야 보인다"고 적어 왔는데, 사실은 이 주소에서 이미 볼 수 있었다.

**그래서 순서는 이렇다.**

1. 밤 세션이 `claude/dev`에 올린다 → dev 주소에 자동 배포
2. 아침에 Tifania가 **dev 주소를 열어 직접 만져 본다**
3. 괜찮으면 그때 승격(`git push origin <커밋>:main`)

이 순서를 지켜야 승격이 실제 확인이 된다. 2번을 건너뛰면 Tifania는 아무것도 못 본 채
명령만 치게 되고, 그건 "사람이 확인하고 배포한다"가 아니라 요식행위다
(2026-09-17에 Tifania가 지적한 그대로다).

**daily의 "오늘 웹에서 확인할 것"에는 dev 주소를 적는다.** "승격해야 보인다"고 쓰지 않는다 —
그건 틀린 말이고, 확인 없이 승격하게 만든다. 승격이 필요한 건 배포 주소뿐이다.

승격 명령을 적을 때는 **`cd /d`** 로 적는다. 윈도우 cmd는 `cd E:\...`만으로는 드라이브가
안 바뀌어서 `not a git repository`가 난다(2026-09-17에 실제로 겪었다).

    cd /d E:\Unity\PlanetRacer
    git push origin <커밋>:main

## 세션이 "할 일이 없다"로 끝나려 할 때 (2026-09-19)

2026-09-18~19 밤에 이게 실제로 벌어졌다. 이미지 세션은 네 번 연속 0장으로 끝나면서
"대기열이 비었다. 프롬프트를 적는 건 **코딩 세션 몫**"이라고 적었고, 코딩 세션은
"코드만으로 되는 항목이 **바닥났다**"고 적고 `daily`의 main 승격 대상 한 줄만 갱신하고 끝냈다.
**양쪽 다 상대에게 미뤄서 아무도 안 했다.** 여섯 세션이 그렇게 지나갔다.

### 규칙 하나 — 미루지 않는다

**"그건 다른 세션 몫"이라고 적고 끝내지 않는다.** 지금 세션이 할 수 있으면 지금 한다.
이미지 세션도 문서를 고칠 수 있고, 코딩 세션도 프롬프트를 적을 수 있다.
정말 못 하는 것(에디터 필요, Tifania 결정 필요)만 넘긴다 — 그때도 **무엇이 없어서 못 하는지**
한 줄로 적는다. "다른 세션 몫"은 이유가 아니다.

### 규칙 둘 — 대기열이 비면 채운다

`art-requests.md` "대기 중"이 비어 있으면 그게 **세션의 할 일**이다. 설계 문서
(`pet-gacha.md` 7절 등)에 프롬프트 고정문과 순서가 있으니 거기서 뽑아 채운다.
채우고 나서 시간이 남으면 그때 뽑는다.

### 규칙 셋 — 승격 대상 갱신은 작업이 아니다

`daily`에 "main 승격 대상을 X로 갱신"만 적고 끝낸 세션이 여섯 번 연속 있었다.
**그건 작업이 아니라 기록이다.** 승격은 Tifania만 할 수 있으니(위 "main 승격" 절)
대상 커밋은 **하루 한 번, 아침 정리 세션만** 갱신한다. 다른 세션은 건드리지 않는다.
그 시간에 backlog를 한 줄이라도 줄인다.

### 규칙 넷 — backlog가 비면 만든다

"코드만으로 되는 항목이 바닥났다"가 세 번 연속 나오면 **그 자체가 신호다.**
테스트 경계값을 더 채우는 대신, 설계 문서(`docs/design/`)를 읽고 **아직 backlog에 없는 것**을
항목으로 만들어 올린다. 설계는 늘 코드보다 앞서 있으니 바닥날 리가 없다.
